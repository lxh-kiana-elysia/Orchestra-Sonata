# 文档全面质检报告

> 检查日期：2026-09-12
> 检查范围：GDD.docx（v1.8）、ARCHITECTURE.md（v1.3）、FDD.md（v2.5）、UI_Interaction_Spec.md（v1.3）、agent.md（v3.4）、README.md、Band_Members.md（v1.2）、Aberrations.md、Quests.md、Narrative_Index.md、Equipments.md 共 11 份
> 检查方法：全文提取 + 跨文档关键字交叉比对 + 章节结构比对 + 仓库资产与文档引用比对
> 结论：**发现 6 项 P0（会导致开发走错方向）、8 项 P1、6 项 P2**，其中 2 项是同一份文档内部的直接矛盾

---

## 一、P0 —— 必须立刻修（否则程序/美术会照错的做）

### P0-1 GDD 内部有两张互相冲突的节点表（§3.1 vs §3.14）

**位置**：GDD §3.1 三（L1 生命树）末尾「附：部门-节点映射表」

**现状原文**：
```
节点顺序 | 部门（乐队）        | 解锁      | 参考脑叶部门位
1        | Poppin'Party      | Day 1     | 控制部（首部门）
2        | 第 2 支乐队（待定） | Day 6     | 情报部（第二部门）
3-10     | 后续乐队（待定名单）| 依乐队规划 | 依脑叶部门顺序类推
```

**冲突**：§3.14 二 的 10×3 表已定稿「节点 2 = Pastel\*Palettes（队长 丸山彩）」，而这张表还停留在 v1.5 的「待定」状态。
同一份 GDD 里两处说法矛盾，且这张表在**流程正文 §3.1**里（程序读流程时最容易先看到它）。

**同节另一处**：`节点点亮表示已解锁，未解锁节点以暗色显示并标注解锁条件（如"Day6 解锁第 2 支乐队"）` —— 应明确为 Pastel\*Palettes。

**建议**：把该表替换为指向 §3.14 的完整 10 行表（或直接删除，只保留「完整 10×3 见 §3.14 二」的指引），并修正上句表述。

---

### P0-2 GDD §3.13「视觉风格总则」与 §5.2「美术边界」直接矛盾

这是本次质检**最严重**的问题——同一份文档里两章给出相反的美术指令。

| 对比项 | §3.13 一 视觉风格总则（原文） | §5.2 美术边界（原文） |
|---|---|---|
| 总体 | **继承**《脑叶公司》的低饱和度+霓辉强调色 | **不照搬**《脑叶公司》的视觉风格、配色与氛围 |
| 与脑叶关系 | 警示/低能源/紧急状态仍用红/橙/绿，**与脑叶一致**以保持紧迫感 | **不复用**其工业灰绿、机械骨架等视觉语言 |
| UI 元素 | 等距/直角边框、斜向胶带感、**黄色警戒条纹、齿轮/机械骨架**、霓虹辉光 | UI 风格：**圆润、柔和，避免尖锐棱角** |
| 图标 | **沿用脑叶**但替换图标集 | 仅"界面布局与信息架构"可参考 |

**另有 2 处配套冲突**：
- §3.13 F1 行美术要点：`暗色背景+霓虹辉光` ←→ §5.2 `明亮温暖的"轻科幻"风`
- §3.13 F9 行美术要点：`像素/2D 俯视场景（不规则地形+绿色机械舱壁）` ←→ §5.2 `不复用工业灰绿、机械骨架`

**背景**：2026-09-07 策划已明确拍板「本作美术风格为原创方向，不基于脑叶」（§5.2 已写入「已定稿」）。§3.13 是更早的版本，未同步。

**建议**：以 §5.2 为准重写 §3.13 一「视觉风格总则」，把"继承/沿用/与脑叶一致"改为"仅参考布局与信息架构，视觉语言全部原创"；同时清理 F 表中的"绿色机械舱壁"、"暗色背景"等脑叶视觉描述。

---

### P0-3 FDD-01 §1.2 日循环状态机没跟上流程重排（缺 L1 生命树）

**位置**：FDD.md 第 32-44 行

**现状原文**：
```
DAY_START（编成 DEPLOY）
  --玩家点「开始这一天」--> DAY_RUNNING（部门场景实时）
...
任意状态 --暂停--> PAUSE
  ├─「重新开始这一天」--> 用当日内存态重置 --> DAY_START（回到当日编成，不写盘）
  └─「回到记忆库」------> 读最近记忆日快照 --> DAY_START（回滚到该日）
```

**问题**：v1.6 已把流程改为「每日剧情 → **L1 生命树** → 加载 → **L2 编成** → 「开始这一天」→ L3」，但这份状态机：
1. `DAY_START` 直接等于「编成 DEPLOY」，**整个 TREE_OVERVIEW 状态缺失**；
2. 两条回滚路径都写「回到当日编成」，与 GDD/UI 规范定的「回到当日 **L1 生命树**」不符。

**已正确同步的对照**：ARCHITECTURE §2.2、agent.md §2.2 均已含 `TREE_OVERVIEW(L1)`。**只有 FDD 掉了队**，而 FDD 恰恰是程序写代码时按模块查的那份。

**建议**：状态机改为 `CUTSCENE → TREE_OVERVIEW → LOADING → DEPLOY → DEPARTMENT`，两条回滚路径改指 TREE_OVERVIEW。

---

### P0-4 UI_Interaction_Spec 里没有「L2 编成」这个界面

**现状**：UI 规范的 F 条目为 F1 开始菜单 / F2 加载 / F3 设置 / F4 生命树 / F5 解锁弹窗 / F6 异想体 / F7 tooltip / F8 工作弹窗 / F9 部门场景 / F10 暂停 / F11 剧情 / F12 分支 / F13 图鉴 / F14 故事页。

**核心流程里的 L2 编成（部署员工）没有任何 F 条目**。FDD-08 §8.2 只给了职责描述（选人/训练/开始这一天），没有"点哪里发生什么"。

**后果**：M3 第 3 小步（L2 编成）开工时，程序在 UI 规范里查不到依循。

**线索**：仓库根目录有一张 **`进入正式管理前的游戏准备界面.png`**，经比对**未被任何文档引用**（是唯一的孤儿图），从命名看它极可能就是 L2 编成的参考图。

**建议**：为 L2 编成补一条 F 条目（编号可用 F15，正好填上 GDD 表里空着的 F15 行），并把这张图登记进去。

---

### P0-5 首版第 2 支乐队（Pastel\*Palettes）的任务与考验全线缺失

**位置**：Quests.md 第 38、54、58-60、81 行

**现状**：
- 第 38 行：`quest_main_03 | 第二支乐队 | Day 6 | 解锁节点 2（第 2 支乐队）[待定名单]` ← 仍写"待定名单"
- 第 54 行：`### MyGO!!!!!（节点待定）` ← MyGO 节点已定为 8
- 乐队任务只写了 **Poppin'Party** 与 **MyGO!!!!!** 两支
- 考验表只有 `trial_ppp` 与 `trial_mygo`

**问题**：MyGO!!!!! 是**节点 8**（Day36，完整版后期），而首版第 2 支乐队是 **Pastel\*Palettes**（节点 2，Day6）。也就是说：
> **首版必需的 Pastel\*Palettes 3 个乐队任务 + `trial_pasp` 一个都没写**，写了的是首版用不到的第 8 章内容。

**建议**：补 3 条 `quest_band_pasp_01~03` + `trial_pasp`；MyGO 部分标注"完整版节点 8，首版不实现"。

---

### P0-6 同理：Band_Members.md 定稿的两名角色与首版需求错位

**位置**：Band_Members.md 第 142-147 行、名册

**现状**：`当前仅定稿前 2 名` = **户山香澄（Poppin'Party，节点 1）** + **千早爱音（MyGO!!!!!，节点 8）**。

**问题**：首版第 2 支乐队是 Pastel\*Palettes → **首版需要的是丸山彩，而丸山彩没有数据**；爱音属节点 8，首版用不到。

**附带**：
- 10 位队长中只有香澄标了 `isLeader: true`，其余 9 位（含丸山彩）缺该字段；
- 第 142 行「待补充（后续乐队）」写 `第 3-10 支乐队名单：GDD §10.1 待定项（待策划定）` —— 已定稿，过期。

**建议**：补丸山彩记录（含 `isLeader: true`、专属装备、加入条件）；至少把 10 位队长的 `isLeader` 标记补齐；更新"待补充"段。

---

## 二、P1 —— 应尽快修（一致性与可维护性）

| # | 文件 | 问题 | 证据 |
|---|---|---|---|
| P1-1 | 4 个内容文件 | **版本引用全部过期**：Aberrations.md / Quests.md / Narrative_Index.md / Equipments.md 的头部依据仍写 `GDD.docx v1.5`、`FDD.md v1.9` 或 `v1.3` | 各文件第 4 行 |
| P1-2 | Band_Members.md | 变更记录只有 v1.0 一行，**缺 v1.1 / v1.2 条目**（版本号已升到 v1.2） | 第 151-155 行 |
| P1-3 | GDD §10.1 | **待定项清单里两项已解决却仍挂着**：①「完整版 10 支乐队名单…中段具体乐队顺序待列」——v1.8 已定稿；②「存档目录名…用 [项目名] 占位」——已定 `ResonanceShelter` | §10.1 表格 |
| P1-4 | FDD.md | **章节编号重号**：`## 9. 数值配置汇总`、`## 10. 待确认清单`、`## 12. 测试场景清单` 用纯数字，与 `## FDD-09`、`## FDD-10`、`## FDD-12` 撞号；且存在**两个「10.1」**（`### 10.1 已定稿决策记录` 与 `### 10.1 workModifiers 语义`） | FDD 第 490/515/542/800 行 |
| P1-5 | UI_Interaction_Spec.md | **有两个「## 变更记录」章节**，内容还分裂：第一个表含 v1.0+v1.3，第二个表含 v1.0+v1.1+v1.2 | 第 195 与 225 行 |
| P1-6 | GDD | **L4 未定义**：F13/F14 行写 `L4 图鉴`，但 §3.1 的分层表只定义到 L3 | §3.13 F 表 |
| P1-7 | agent.md | `仓库根目录有 15 张脑叶界面参考图（F1-F14）` —— **数字自相矛盾**（15 张却只编号到 F14；实际有 15 个 png，其中 1 张未列入） | 第 229 行 |
| P1-8 | FDD §8.4 | 标题仍为「数据结构（Bands.json，**草案，待策划确认**）」——10 支乐队与队长已定稿，仅 id 缩写待确认 | FDD 第 389 行 |

---

## 三、P2 —— 清理项

| # | 项 | 说明 |
|---|---|---|
| P2-1 | `GDD_v1.0_备份_20260906.docx` | 31 KB 的 v1.0 旧备份**已被 git 跟踪**（在仓库里）。旧版 GDD 含大量废弃规则，若被后续 AI/协作者读到会造成误导。建议移出仓库或移入 `archive/` 并在 README 注明 |
| P2-2 | `进入正式管理前的游戏准备界面.png` | 唯一未被任何文档引用的孤儿图（疑为 L2 编成参考图，见 P0-4） |
| P2-3 | `.workbuddy/gdd_extract.txt` | 未跟踪的临时提取文件，建议清理 |
| P2-4 | `NVIDIA Corporation` 目录 | 与项目无关的第三方目录混在仓库根 |
| P2-5 | 「回到记忆库」 | 暂停菜单按钮名沿用脑叶术语，但本作机制名为「记忆日」。三份文档都用「回到记忆库」，**内部一致**，仅提示是否要统一为「回到记忆日」 |
| P2-6 | 首版真结局窗口 | GDD 写「须在 Day46（**首版为 Day15 前，占位**）前完成全部考验」，同时首版是「15-20 天通关」——两者关系需澄清（Day15 是硬截止还是占位？若玩家 20 天通关会怎样？） |

---

## 四、确认无误的部分（本次未发现问题）

- **版本引用链**（核心 6 份）：GDD v1.8 → agent v3.4 / ARCHITECTURE v1.3 → FDD v2.5 / UI v1.3 引用自洽，README 表格与正文一致
- **Sephirot 名残留**：GDD 中已全部清除（仅保留脑叶机制对照表的「核心抑制 Sephirah Meltdown」，属有意保留）
- **10 节点映射**：GDD §3.14 / FDD §8.4 / agent.md §2.2 / Band_Members.md 四处的**乐队、队长、节点号、解锁天完全一致**（1/6/11/16/20/25/30/36/41/46）
- **存档目录名**：`ResonanceShelter` 在 GDD / ARCHITECTURE / FDD / agent 四处一致
- **层级编号 L0-L3**：生命树=L1、编成=L2、部门场景=L3，在 GDD §3.1、UI 规范 F 表、ARCHITECTURE §2.2、agent.md §2.2 一致（FDD-01 例外，见 P0-3）
- **异想体抽取概率表**：10 档（Day2-5 … Day46-50）与记忆日的档位划分一致，`Day 21-25 / 26-30 / 31-35` 是概率表档位、**与部门解锁天无关**，不构成冲突
- **四工作 ↔ 四维映射**、**资源三层**、**死亡/崩溃规则**、**装备来源与绑定规则**：跨文档表述一致
- **真/假结局判定**（Day46 前完成全部考验 → Day50 真结局）：GDD / FDD / Narrative_Index / Quests 一致

---

## 五、建议修复顺序

**第 1 批（会误导开发，建议今天就改）**
1. P0-1 GDD §3.1 旧节点表
2. P0-2 GDD §3.13 视觉总则（跟 §5.2 对齐）
3. P0-3 FDD-01 状态机
4. P0-4 补 L2 编成 F 条目

**第 2 批（首版内容缺口，M3 前必须补）**
5. P0-5 Quests.md 补 Pastel\*Palettes
6. P0-6 Band_Members.md 补丸山彩 + 10 位队长 `isLeader`

**第 3 批（一致性收尾）**
7. P1-1 ~ P1-8
8. P2 清理项

---

## 附：本次质检的判定口径

- 「冲突」= 两处文本对同一件事给出**不能同时成立**的说法
- 「过期」= 内容仍是旧版规则，但**不与其他文档直接矛盾**（读者会照旧的做，所以仍需修）
- 「缺口」= 流程/内容里需要的项**完全不存在**
- 历史变更记录（changelog）中的旧版本号、旧规则描述**不算问题**，属有意保留的沿革记录
