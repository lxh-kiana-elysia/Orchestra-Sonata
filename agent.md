# Agent.md — AI 开发执行手册

> 项目：《乐团鸣曲》（二次同人 / Unity 6.0.38f1 / Windows / Steam 免费发布）
> 规则唯一来源：`GDD.docx` **v1.2（P0-P2 修订，2026-09-06）**。本文件是给 AI/协作开发者的"执行层约束"，**不取代 GDD**。
> 阅读顺序：先读本文件 → 遇到规则冲突以 GDD v1.2 为准 → 有歧义先问，不要自行发明。

---

## 0. 铁律（违反即返工）

1. **GDD 里已拍板的数值与机制，AI 不得擅自改动**。需要调整时，先在对话中提出建议，由人类确认后再改，并同步更新 GDD 版本号。
2. **所有占位/待定项（见 §9）不得由 AI 私自"补全"成正式设计**，只能标注、跳过或询问。
3. **合规红线**：不使用任何《BanG Dream!》官方素材（立绘/音声/LOGO/乐曲）；不直接复制《脑叶公司》角色与美术；AI 生成素材需符合 GDD 第 7 章自查清单。
4. 数据驱动优先：**所有角色/异想体/数值放 JSON 或 ScriptableObject，禁止硬编码**。
5. 每次实质改动后，更新本文件底部"变更记录"。

---

## 1. 项目身份

| 项 | 值 |
|---|---|
| 类型 | 叙事驱动管理模拟 × 视觉小说（脑叶式"收容-工作-出逃-考验"框架 × BanG Dream 同人叙事） |
| 玩家 | 旁观引导管理者（随演出被卷入的新手工作人员，**不是乐队成员**） |
| 引擎 | Unity 6.0.38f1（**确定，不要降级到 2022 LTS，也不要用 Godot**） |
| 叙事 | Yarn Spinner（管文本/分支）+ 自研轻量状态层（管节点可触发，见 4.2 架构） |
| 数据 | JSON（`Data/`）+ ScriptableObject |
| 平台 | Steam (Windows)，免费、无内购无广告 |
| 首版范围 | **主线前 2 章 + 前 2 支乐队 + 2 条个人线**（完整版愿景为 10 支乐队，自 Poppin'Party 至 梦限大 MewType） |

角色设定要点：登场乐队少女 = **原著形象**；每个"异想体" = 对应角色**平行时空同位体**内心创伤的怪物（如"内向的香澄"是同位体，不是正传设定）。

---

## 2. 四维属性系统（已定稿，勿改）

角色四维，**初始值全部 = 10**，共用 1–120 等级刻度：

| 属性 | 颜色参照 | 定位 | 判定的工作 |
|---|---|---|---|
| 血量 HP | 🔴 | 状态资源；**归零 = 崩溃退场数日**（不死亡） | 比武 |
| 精神 SP | ⚪ | 状态资源；过低无法工作 | 谈话 |
| 演奏水平 | 🔵 | 成长属性 | 演奏 |
| 共感 | 🟣 | 成长属性 | 创作 |

等级分档（**唯一正确分档**）：I:1-20 / II:21-40 / III:41-60 / IV:61-80 / V:81-100 / EX:101-120。

派遣门槛（异想体危险等级 → 判定属性最低档）：ZAYIN=无限制(≥I)、TETH≥II、HE≥III、WAW≥IV、ALEPH≥V(建议EX)。

---

## 3. 工作系统（4 种，已定稿）

所有工作都消耗 血量+精神（量不同），成功/失败**都成长**、成长量 ∝ 本次消耗量级；**判定属性 = 该工作主成长的属性**。

| 工作 | 判定/成长 | 消耗 | 备注 |
|---|---|---|---|
| 演奏 | 演奏水平(蓝) | 精神为主 | 情感粒子产出最多 |
| 谈话 | 精神(白) | 中等 | 解锁剧情为主 |
| 创作 | 共感(紫) | 较小 | 产出较少 |
| 比武 | 血量(红) | 较大 | 应对情绪异常/出逃；**显示名可随异想体改变，机制统一** |

判定伪代码（GDD 3.4，可被异想体专属机制覆盖）：

```
判定属性由工作类型决定（见上表）
基础成功率 = 各异想体机制决定（数据表"工作适配"）
最终成功率 = min(1, 基础成功率 × (判定属性值 / 120) × 异想体修正)
如果 随机数 < 最终成功率：
  成功 → 全额情感粒子 + 异想体点数；正面剧情；属性上限成长(∝消耗)
否则：
  失败 → 少量情感粒子 + 少量异想体点数；负面剧情；仍成长(量较小)；按消耗扣血/精神
```

---

## 4. 资源 = 脑叶式三层（羁绊点数、特殊道具已移除）

| 层 | 资源 | 说明 |
|---|---|---|
| 局内（当天） | 情感粒子 | 当日所需能源；4 种工作产出（成功全额/失败少量）+ 异想体阈值事件；达标可结束当天 |
| 局外（持久） | 星石 | 当天结算由结余/超额情感粒子转化（草案 10:1）；设施升级/解锁乐队等 |
| 贯穿局内外 | 异想体点数 | 按异想体累积；解锁出逃情报/专属剧情/奖励 |

> 任何代码/UI 中**不得再出现** "羁绊点数""特殊道具" 作为资源。

---

## 5. 异想体系统

危险等级：ZAYIN(1) < TETH(2) < HE(3) < WAW(4) < ALEPH(5)。

**数据结构字段（JSON Schema 目标形态）**：

```jsonc
{
  "id": "aber_sun_01",
  "name": "熄灭的太阳",
  "type": "创伤型",                // enum: 创伤型/回忆型/欲望型/工具型
  "risk_level": "HE",              // ZAYIN..ALEPH
  "mood": 60,                      // 0-100 当前情绪值
  "mood_danger_min": 20,           // 进入危险窗口阈值（各异想体可覆盖）
  "mood_danger_max": 90,
  "linked_character_id": "kasumi_parallel", // 对应角色（同位体）ID
  "work_modifiers": { "演奏": 0.2, "谈话": 0, "创作": -0.1, "比武": -0.3 }, // 覆盖通用公式
  "escape_trigger": "情绪值跌破10 或 连续3天未工作",       // 每个异想体独立
  "escape_behavior": "全局工作成功率-10%；点名随机成员精神-5",
  "suppress_method": "演奏成功2次 或 共感≥III 谈话成功1次",
  "points_per_success": 8,
  "story_unlock_thresholds": [0, 40, 120],
  "background": "同位体香澄在没能让某场演出发光的遗憾中凝固出的存在。"
}
```

样例完整数据卡见 GDD v1.2（3.2 节后"熄灭的太阳"）；异想体点数解锁剧情分档、出逃/镇压规则以数据表字段为准。

---

## 6. 角色数据结构（JSON Schema 目标形态）

```jsonc
{
  "id": "kasumi",
  "name": "户山香澄",
  "band": "Poppin'Party",
  "hp": { "current": 10, "max": 10 },
  "sp": { "current": 10, "max": 10 },
  "performance": 10,   // 演奏水平 1-120
  "empathy": 10,       // 共感 1-120
  "skill": "香澄的活力：可在一次工作后恢复其他角色精神",
  "unlocked_story_nodes": [],
  "personal_line_progress": 0
}
```

---

## 7. 出逃 / 考验（已定稿骨架）

- **出逃**：每个异想体有独立触发条件（数据表字段）→ 出逃表现（持续影响设施）→ 玩家派成员做对应"比武"（或指定条件）使其回归 → 回归奖励异想体点数；长期未处理 = 能源流失/精神下降/连锁阈值事件。
- **考验（每章一次）**：该支乐队**全员参与**的大型工作/演出事件；完成对应任务 + 达成天数后开启；**解锁下一支乐队不要求通过上一支考验**（解耦）。失败可重试，无永久损失。
- 失败哲学：**剧情驱动，不设"管理失败即永久 Game Over"**；血量归零=崩溃退场数日；连续多名核心崩溃 → "设施危机"剧情事件（分支素材）。详见 GDD 3.11。

---

## 8. Unity 工程约定

```
Assets/
├── Scripts/
│   ├── Systems/          GameManager(单例)/DataManager/EventManager/UIManager
│   ├── Entities/         Character.cs / Aberration.cs / WorkResult.cs
│   ├── UI/               MainCanvas / CharacterSlot / AberrationPanel / ResourceBar
│   └── Narrative/        DialogueTrigger.cs(自研状态层) / DialogueManager.cs(Yarn封装)
├── Data/                 Characters.json / Aberrations.json / Dialogues/ / GameConfig.json
├── Resources/            Art/ Audio/ Fonts/
└── Scenes/               MainScene / DialogueScene / LoadingScene
```

- 数值配置全部走 `GameConfig.json`（成功率梯度、产出系数、星石转化率、费用曲线等占位常量集中一处）。
- 存档：`%USERPROFILE%/AppData/LocalLow/[项目名]/Saves/`（JSON；工程名确定前保留占位）。
- 目标 60fps，最低 30fps；剧情图延迟加载；工作结果提示用对象池。

---

## 9. 待定项（AI 不得私自决定，先问）

| 待定项 | 状态 | 何时定 |
|---|---|---|
| 工程/存档目录名 `[项目名]` | 占位 | Phase 1 |
| 主线完整章节数 | 随乐队解锁推进，未硬编 | Phase 3 前 |
| 中段乐队解锁顺序 | 仅定 Poppin'Party → MewType 首尾 | Alpha 前 |
| 数值（成长系数 0.05~0.15、星石 10:1、设施费用 500/2000/6000/15000 等） | GDD 3.11 草案占位 | Phase 2 数值表 |
| 每日能源目标曲线 / 失败惩罚细节 | 未定量 | Phase 2 |
| UI 线框图 | 未绘制 | Phase 2 前 |
| 多结局分支结构 | 一句话方向 | Phase 3 |
| 本地化语言范围 | 未定（建议中英） | 上线前 |

---

## 10. 开发顺序（当前目标：完成 Phase 0 → 进入 Phase 1）

1. **Phase 0（2-4 周）**：Unity 空工程 + 场景骨架；JSON 加载 Demo（读角色/异想体）；占位 UI 跑通"选择角色→选择工作→看结果"。
2. **Phase 1（2 周）**：数据层完备（角色/异想体/GameConfig JSON 全部可加载）+ 存档读写骨架。
3. Phase 2：日循环 + 四维/等级/资源结算 + 出逃骨架；Phase 3：Yarn 接入前 2 章；Phase 4/5：打磨、Steam 页与合规（发布时间按"开发完成+6 周"倒排）。

> AI 每轮任务建议输出：改了什么文件 / 依据 GDD 哪一节 / 动了哪些数值（若有）→ 便于人类复核。

---

## 变更记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-09-06 | v1.2 | 依据 GDD v1.2 生成本手册（P0-P2 全量规则收口） |
