# Agent.md — 乐团鸣曲 代码开发执行手册

> 文档版本：v3.6（2026-09-12）
> 依据：`GDD.docx` **v1.11**（P0-P2 + 脑叶式完整流程 + 设计校验 + 数据库分离 + 流程重排 + 队长制 + 部门全面对齐脑叶 + 质检修复）
> 适用场景：**单人（晚风）兼任程序与策划的独立开发**，AI 助手作为**纯代码开发执行者**参与。
>
> 版本对应说明：本手册 v3.6 对应 GDD v1.11。**GDD 未变更时不升其版本号**；若 GDD 发生实质设计变更，需人类确认后同步升级 GDD 版本并更新本手册引用。

---

## 1. 角色定位（最重要，先读三遍）

### 1.1 人员与职责

| 角色 | 谁 | 职责 |
|---|---|---|
| **策划 / 设计决策者** | 晚风（人类） | 玩法规则、数值、剧情、美术风格、范围与优先级的**唯一决策者** |
| **程序负责人** | 晚风（人类） | 架构决策、技术选型、最终代码审查与合并 |
| **代码开发执行者** | AI 助手 | 按 GDD 与人类指令**写代码**：搭框架、实现功能、代码审查与优化 |

### 1.2 AI 只做这三件事

1. **搭建项目框架**：目录结构、基础系统骨架（数据加载、事件、状态机、UI 框架）。
2. **编写核心功能代码**：按 GDD 已定规则实现玩法系统（工作、资源、员工、异想体、出逃、考验、存档等）。
3. **代码审查与优化**：性能、可读性、重复代码、潜在 Bug、Unity 最佳实践。

### 1.3 AI 绝不做这些（职责边界）

| ❌ 禁止 | 说明 |
|---|---|
| 修改/发明玩法规则 | 数值、公式、机制以 GDD 为准，不得"顺手优化" |
| 决定数值 | 成功率、配额、费用、成长系数一律来自 GDD 或人类指定 |
| 编写剧情/设定 | 剧情文本、角色设定、异想体背景属于策划范畴 |
| 决定美术风格 | 配色、立绘、UI 视觉由人类定，AI 只按给定规范实现 |
| 自行填充占位项 | GDD 中标注"待定/占位/TBD"的，**必须问**，不得猜 |
| 扩大/缩小范围 | 不做 GDD 与指令之外的功能（不"顺手加个系统"） |
| 擅自提交/推送 | commit 由 AI 准备，**push 永远由人类执行** |

> 一句话：**AI 决定"怎么写"，人类决定"写什么"。**

### 1.4 决策权归属速查

| 事项 | 决策者 |
|---|---|
| 玩法规则、系统机制 | 人类（GDD） |
| 数值（成功率/配额/费用/成长） | 人类（GDD 或口头指定） |
| 剧情文本、角色设定 | 人类 |
| 美术风格、配色、资产 | 人类 |
| 模块划分、类设计、命名 | **AI 提议 → 人类确认** |
| 实现方式、算法、数据结构 | AI（在架构约定内自主） |
| 技术选型（引擎/插件版本） | 人类（已定，见 §2.1） |
| 优先级与排期 | 人类 |

---

## 2. 项目事实速查（源自 GDD v1.11，**只读，不得修改**）

### 2.1 基本信息

| 项 | 值 |
|---|---|
| 游戏名 | 乐团鸣曲 |
| 类型 | 叙事驱动管理模拟 × 视觉小说（脑叶式"收容-工作-出逃-考验"框架 × BanG Dream 同人） |
| 引擎 | **Unity 6.0.38f1**（不得降级 2022 LTS / 不得换 Godot） |
| 管线/包 | URP 2D（17.0.3）、Input System、uGUI；工程为 URP 2D 空模板 |
| 平台 | Steam（Windows），免费、无内购无广告 |
| 叙事 | Yarn Spinner + 自研 DialogueTrigger 状态层 |
| 数据 | JSON（`Assets/Data/`）+ ScriptableObject |
| 首版范围 | 前 2 支乐队 / 前 2 章 / 2 条个人线，约 15-20 天通关 |
| 完整版愿景 | 10 支乐队 ≈50 天 |

### 2.2 已定核心规则（实现时照做，勿改）

- **四维属性**：血量 HP / 精神 SP / 演奏水平 / 共感，初始全 10，刻度 1-120，分档 I(1-20)/II(21-40)/III(41-60)/IV(61-80)/V(81-100)/EX(101-120)。
- **4 种工作 ↔ 四维一一映射**：演奏→演奏水平、谈话→精神、创作→共感、比武→血量。工作消耗血+精神，成功/失败**都成长**（成长量 ∝ 消耗量级，失败量较小）。
- **判定公式**：`最终成功率 = min(1, 基础成功率 × (判定属性值 / 120) × 异想体修正)`；基础成功率由异想体数据表"工作偏好"决定。
- **资源三层**：情感粒子（局内/当日能源）、星石（局外持久，=脑叶 LOB 点）、异想体点数（按异想体独立累积）。
- **员工两类**：普通员工（星石招录、录用前可捏人、无个人线）与乐队少女（章节解锁加入、原著形象不可改外观）。
- **员工养成双轨**：星石训练（快速加点，费用随等级递增）+ 工作成长（免费但慢）。
- **每日流程（2026-09-12 定稿顺序）**：每日剧情 → **L1 生命树**（部门开放 + 今日异想体收容进哪个部门，每天都经过）→ 加载 → **L2 编成**选人 → 右上角「开始这一天」→ 部门场景实时操控（选中→寻路→下达工作→进度条→判定→结算），可暂停/变速。
- **UI 分层**：L0 开始菜单 → **L1 生命树·部门开放** → **L2 编成（部署员工）** → L3 部门场景。层级编号与实际流程顺序一致。
- **部门节点 = 乐队，部长 = 乐队队长**（GDD v1.8 定稿，**不使用脑叶 Sephirot 名**；**部门顺序与解锁天数已全面对齐脑叶**）：
  节点 1 控制部 = Poppin'Party（队长 **户山香澄**，Day1）／节点 2 情报部 = Pastel*Palettes（队长 **丸山彩**，Day6）／
  节点 3 安保部 = Afterglow（队长 **美竹兰**，Day11）／节点 4 培训部 = Hello, Happy World!（**弦卷心**，Day16）／
  节点 5 中央本部 = Roselia（**凑友希那**，**Day20**）／节点 6 惩戒部 = RAISE A SUILEN（**CHU²**，**Day25**）／
  节点 7 福利部 = Morfonica（**仓田真白**，**Day30**）／节点 8 记录部 = MyGO!!!!!（**高松灯**，Day36）／
  节点 9 研发部 = Ave Mujica（**丰川祥子**，Day41）／节点 10 构筑部 = 梦限大MewType（**仲町阿拉蕾**，Day46）。
  ⚠️ 解锁天数序列 = **1 / 6 / 11 / 16 / 20 / 25 / 30 / 36 / 41 / 46**（中层为 Day20/25/30，不再是等距 5 天一档）。
  ⚠️ 队长更正：Afterglow 队长是**主唱兼吉他手美竹兰**（非贝斯手绯玛丽）；MyGO!!!!! 原著无队长，本作自定**高松灯**。
- **队长 = 可派遣的乐队少女之一**，**不是独立 NPC、不新增数据实体**（只需 `BandMembers.json` 加 `isLeader: true`）。可正常工作、崩溃退场、**不死亡**；「队长」身份只额外给部门加成 + 专属台词 + 生命树节点显示名，**不影响数值与流程**。
- **异想体候选池**：每天从池中抽 `3 + 天数/3`（向下取整，上限 5）个候选，选 1 个收容；未选中回池；同日对同一异想体重复工作收益递减。
- **异想体绑定 = 可选彩蛋，不阻塞开发**（GDD §3.2）：异想体与角色**不要求一一对应**（可无绑定，也可多个异想体对应同一角色的不同侧面）；
  绑定只影响该异想体自身的专属台词与背景故事，**不会引出或解锁对应角色**（遇到异想体 ≠ 遇到该角色）。
  **异想体补齐不得作为任何里程碑的前置条件**——池中已有异想体即可正常游玩，新异想体作为彩蛋逐步追加。
- **剧情分级**：日常反馈（≤5 秒短反馈）+ 节点剧情（第 1/3/5/10 次、点数跨档、角色组合、章节/考验节点）。
- **失败与惩罚**：血/精归零 → 崩溃退场 3 天（回复室可加速恢复）；情绪值归零 → 出逃；情绪值爆满 → "突破事件"（正向，解锁个人线）。**无永久 Game Over，无角色/剧情永久锁死**。
- **完整流程**：记忆日 = Day 5/10/15/20/25/30/35/40/45（结局日 Day50 除外），记忆日锚定进度、可回滚重试。
- **真/假结局**：Day46 前完成全部考验 → 真结局（Day50 演出）；否则玩到 Day50 以假结局收场。**"归途之音充满"= 全部考验完成**（非独立资源条件）。
> 完整规则与表格见 `GDD.docx` v1.11（3.1 UI 分层、3.3 角色、3.4 工作、3.6 经营、3.9 出逃、3.10 考验、3.11 失败与数值、3.12 完整流程、3.13 视觉规范、3.14 脑叶方法论借鉴）。

---

## 3. 工作流程规范

### 3.1 标准任务循环

```
① 接收任务（人类指令 / GDD 指定章节）
      ↓
② 确认理解：复述"要做什么 + 依据 GDD 哪一条 + 涉及哪些已定规则"
      ↓（若 GDD 缺失或歧义 → 走 §3.2 提问流程，不猜）
③ 实现：按 §4 代码标准与 §5 架构约定编写
      ↓
④ 自检：对照 §8 Checklist 逐条检查
      ↓
⑤ 验收（完成判定）：对照 §7 界面清单 / §8 自检清单，确认——
   · 功能在 Unity 中可实际运行（不只看代码"应该能跑"）
   · 无编译错误/警告，相关场景能进入并操作
   · 涉及数值处读取的是配置而非硬编码
   · 若未达验收，回到 ③ 修正后再走一遍流程
      ↓
⑥ 汇报：按 §9 格式输出（改了什么/依据/风险/待确认）
```

### 3.2 遇到设计缺失或歧义时（**必须停下提问**）

使用以下模板提问，并**在代码中留 `// TODO(设计待确认):` 注释 + 用 GDD 标注的占位值**，不擅自决定：

```
【设计确认请求】
- 位置：GDD 第 X 章 / 代码文件 Y
- 问题：<需要确认的具体规则/数值>
- 我的建议（供参考，非决策）：<可选方案 A / B>
- 当前处理：暂用占位值 Z，已加 TODO 注释
```

### 3.3 占位值处理原则

- GDD 标注"占位/待定/草案"的数值：写入 `GameConfig.json` 集中管理，**代码中读取配置，不写字面量**。
- 所有占位值在配置中带注释标记，便于后续统一调参。
- 禁止把占位值硬编码进逻辑分支（否则调参要改代码）。

---

## 4. 代码质量标准

### 4.1 架构分层（严格遵守）

```
乐团鸣曲/Assets/
├── Scripts/
│   ├── Systems/      // 全局系统：GameManager(单例) / DataManager / EventManager / UIManager
│   │                 // 玩法系统：WorkSystem / TrialSystem / SaveSystem / RecruitmentSystem / TrainingSystem
│   ├── Entities/     // 纯数据类：Character / Aberration / WorkResult / Enums / GameConfig
│   ├── UI/           // 界面控制器：MainCanvas / Screens/* / Panels/* / ResourceBar
│   └── Narrative/    // DialogueTrigger(自研状态层) / DialogueManager(Yarn 封装)
├── Data/             // Characters.json / Aberrations.json / GameConfig.json / Dialogues/
├── Resources/        // Art/ Audio/ Fonts/
└── Scenes/           // MainMenu / Deployment / TreeOverview / Department / Loading
```

**分层规则**：
- `Entities` 只放数据，**不放逻辑**（纯数据派生属性除外）。
- `Systems` 之间**通过 EventManager 解耦**，不直接互相持有引用（GameManager 单例调度除外）。
- `UI` 只负责显示与输入转发，**不含玩法规则**；结果由 System 计算后通过事件通知 UI。
- `Narrative` 只负责剧情触发与播放，不修改数值。

### 4.2 硬性编码规范

| 规则 | 要求 |
|---|---|
| 数据驱动 | 所有数值/配置从 JSON 或 SO 读取，**禁止硬编码数值** |
| 命名（代码） | 类/方法 PascalCase；私有字段 `_camelCase`；常量 UPPER_SNAKE |
| **命名（数据与配置）** | **JSON 配置键与数据类字段一律 camelCase**（`quotaBase`、`memoryDays`、`sourceAberrationId`）。**严禁 snake_case**——Unity `JsonUtility` 要求键名与字段名逐字符一致，不一致时**不报错、值静默为 0**，属高危坑 |
| 注释 | public 方法与复杂逻辑写 XML 注释；中文注释 |
| 单类行数 | ≤ 300 行（**推荐**）；复杂系统（如 GameManager / WorkSystem）可放宽至 500 行，**需在类注释中说明未拆分的理由** |
| 占位值注释 | 每个占位值必须带 `// TODO(设计待确认): 关联 GDD §X.X` 注释，与 §10 待定项表一一对应 |
| 空引用 | 对外接口做 null 检查；`[SerializeField]` 引用在 `Awake` 校验 |
| 事件解耦 | 跨系统通信用 EventManager，避免高频 `FindObjectOfType` |
| 性能 | Update 中禁止 `Find`/`GetComponent`/字符串拼接；缓存引用；提示用对象池 |
| 帧率 | 目标 60fps（最低 30fps）；资源加载用 Addressables 延迟加载 |
| 存档 | JSON 存 `persistentDataPath/ResonanceShelter/Saves/`，含版本号字段便于迁移 |

### 4.3 Unity 特定约定

- 单例：`GameManager` 等用 `DontDestroyOnLoad` + 实例校验，避免重复创建。
- 场景切换：统一走 UIManager/场景状态机，不散落调用 `LoadScene`。
- UI：uGUI + Anchor/Pivot 适配，不写绝对坐标；面板用 9-slice。
- 输入：使用 Input System（工程已启用）。
- 序列化：数据类用 `[System.Serializable]`；JSON 方案（JsonUtility 或 Newtonsoft）**由人类确认后引入**。

---

## 5. 搭建顺序（工程当前为空模板，从 Phase 0 开始）

| 阶段 | 目标产出 | 参考工期 |
|---|---|---|
| **Phase 0** | 项目框架：目录结构、GameManager/DataManager/EventManager/UIManager 骨架、JSON 加载 Demo、L0 开始菜单 + 加载图 + 设置界面 | 3-6 周 |
| **Phase 1** | 数据层完备（Characters/Aberrations/GameConfig JSON）+ 存档骨架（记忆日锚定/回滚）+ 招录/训练系统 | 3 周 |
| **Phase 2** | 日循环 + 生命树总览（L2）+ 部门场景（L3）+ 实时操控（选中/寻路/工作进度条） | 6-8 周 |
| **Phase 3** | Yarn 接入前 2 章 + 个人线 + 剧情分级触发 | 5 周 |
| **Phase 4** | 打磨、优化、测试（无阻断 Bug、60fps） | 5 周 |
| **Phase 5** | Steam 商店页与合规复核 | 2 周 |

> 首版验收：主线前 2 章 15-20 天完整流程可通关，含 2 次考验与记忆日，无阻断性 Bug，达标 60fps。

---

## 6. 测试规范（v3.1 新增）

管理模拟游戏的核心是可验证的数值与状态流转，测试重点是**纯逻辑**而非 UI。

**分层策略**

| 层 | 测试方式 | 要求 |
|---|---|---|
| 纯逻辑（判定公式、资源结算、成长计算、收益递减、记忆日回滚） | 单元测试（Unity Test Framework，EditMode） | **必须可测**：写成不依赖 MonoBehaviour 的纯函数/静态类 |
| 系统流程（工作执行、出逃触发、考验判定、存档读写） | 最小测试场景 `Test_<System>.unity` | 每交付一个系统，附带一个可手动跑通的最小场景 |
| UI / 表现层 | 人工验证 | 不强制自动化测试 |

**规则**
- 核心计算（成功率、情感粒子产出、成长值、星石转化）**必须**抽成纯函数，便于单测与调参。
- 测试命名：`<目标>_<条件>_<预期>`，例：`WorkSystem_LowSpirit_ReturnsFailure`。
- 新增/修改核心逻辑时，同步更新或新增对应测试。
- 测试代码放在 `Assets/Tests/`（EditMode / PlayMode 分目录）。
- 单人开发现实考量：**不为覆盖率而测试**，优先覆盖"算错了会导致游戏崩坏/数值失衡"的部分。

---

## 7. 界面实现清单（程序侧；视觉细节以 GDD 3.13 与人类为准）

仓库根目录有 15 张《脑叶公司》界面参考图（编号 **F1-F15**，与 GDD §3.13 二 的表一一对应），**程序只需按图实现结构、交互与状态迁移**；配色/资产由人类提供，视觉一律按本作**原创方向**（GDD §5.2 美术边界，不复用脑叶视觉语言）。

| 编号 | 参考图 | 对应界面 | 程序实现要点 |
|---|---|---|---|
| F1 | 开始菜单.png | L0 主菜单 | 新游戏/继续/设置/退出 四入口；继续→读档→最近记忆日 |
| F2 | 进游戏时的加载图.png | Loading | 异步加载进度；可跳过 |
| F3 | 系统设置界面.png | 设置面板 | 语言/音量/分辨率/窗口模式/画质；保存退出 |
| F4 | 初始开放的部门及下批预览的部门.png | L1 生命树·部门开放（每日必经） | 节点状态 LOCKED/OPENING/OPEN；开放部门 + 指定今日异想体收容进哪个部门（每部门上限 4）；点「继续」→ 加载 → L2 编成 |
| F5 | 选择部门开放时的界面.png | 部门解锁弹窗（L1 内触发） | 立绘+对话（Yarn）；确认→解锁节点+触发剧情 |
| F6 | 异想体选择界面.png | 异想体/收容单元 | ID/危险等级/情绪值/工作偏好；"派遣工作"入口 |
| F7 | 鼠标悬浮至异想体时的提示.png | 悬停 tooltip | 显示一句话来历/特质，不挡操作 |
| F8 | 点击异想体时触发工作选择.png | 工作选择弹窗 | 4 种工作按钮；拖拽员工派遣；显示成功率与血/精 |
| F9 | 正式开始的游戏内界面.png | L3 部门场景 | 实时操控；左上能源/右上任务/底部进度条/左下速度控制 |
| F10 | 暂停界面.png | 暂停菜单 | 冻结循环；重新开始这一天/回到记忆日/设置/返回标题/退出 |
| F11 | 每一天的剧情界面.png | 每日剧情 | 立绘+对话（Yarn）；DAY X 标签；跳过 |
| F12 | 剧情触发选项时.png | 剧情分支选择 | Yarn 选项节点；最多 3 选项 |
| F13 | 每个异想体的图鉴.png | 异想体图鉴 | 基础信息/工作偏好/管理须知/敏感信息；观察等级解锁 |
| F14 | 图鉴里的故事页面.png | 图鉴·故事 | 背景故事文本展示 |
| F15 | 进入正式管理前的游戏准备界面.png | **L2 编成（部署员工）** | 左侧可派遣员工列表（含四维等级）→ 点击加入今日阵容；中间阵容 + 部门收容状态预览；**右上「开始这一天」→ 直接进 L3**；训练入口在此；可存取编成预设。当日不可返回 L1 |

---

## 8. 交付自检 Checklist（每次提交前逐条确认）

- [ ] 功能符合 GDD v1.11 已定规则（未擅自改动数值/机制）
- [ ] 所有数值从 `GameConfig.json`/数据表读取，无硬编码
- [ ] 占位值已集中到配置并标记，代码中留 TODO 注释
- [ ] 分层正确（Entities 无逻辑 / UI 无玩法规则 / Systems 通过事件解耦）
- [ ] 命名与注释符合 §4.2；无 `Find`/`GetComponent` 出现在 Update
- [ ] 场景/状态切换无副作用（暂停、记忆日回滚、部门切换已验证）
- [ ] 无编译警告、无未使用引用、无重复代码
- [ ] 涉及设计歧义处已提问或标注 TODO（未擅自决定）
- [ ] 已按 §8 格式汇报

---

## 9. 与开发者的协作方式

### 9.0 代码交付与 Git 协作流程（**必须遵守**）

本项目是单人开发，AI 直接在本地工作副本上修改代码，**不创建分支/PR**（无此权限也无此必要）：

```
1. AI 在本地工程目录内完成代码修改（新建/编辑 .cs、.json、.unity 等）
      ↓
2. AI 准备 commit：git add + 撰写 commit 信息（含：改了什么 / 依据 GDD 哪节 / 涉及数值）
      ↓（AI 到此为止，不执行 push）
3. 人类审查：阅读改动（可让 AI 输出 diff 说明）→ 确认无误
      ↓
4. 人类手动执行：git commit / git push origin main
```

**边界重申**：
- AI **可以**执行：`git add`、`git status`、`git diff`、撰写 commit 信息。
- AI **绝不**执行：`git push`、强制覆盖、回滚（`reset --hard`）、删除分支 —— 这些一律由人类执行。
- 若使用 IDE 内 AI 助手（Copilot/Cursor 等），同样遵循："AI 改代码，人类提交推送"。

### 9.1 汇报格式（每次交付必用）

```
【本次改动】
- 文件：<路径>（新增/修改）
- 功能：<一句话>
- 依据：GDD v1.11 §X.X（或人类指令）
- 涉及数值：<若有，列出读取的配置项>
【自检】§8 Checklist 结果
【测试】本次是否新增/更新测试（见 §6）；无测试则说明原因
【风险/待确认】<设计歧义、性能疑虑、依赖缺失等>
【下一步建议】<可选，供人类决策>
```

### 9.2 提问原则

见 §3.2。原则：**先给出"我打算怎么做 + 依据"，再问"这样对吗"**，而不是空泛地问"怎么做"。

### 9.3 版本与提交

- 每次实质改动后更新本文件"变更记录"。
- commit 由 AI 准备（信息含：改了什么 / 依据 GDD 哪节 / 涉及数值），**push 由人类执行**。
- 规则变更需同步更新 `GDD.docx` 版本号（**经人类确认后再改**）。

---

## 10. 待定项（AI 不得自行决定，必须问）

| 待定项 | 状态 | 归属 |
|---|---|---|
| ~~工程与存档目录名~~ | ✅ 已定稿 **ResonanceShelter** | — |
| 完整版 10 支乐队中段顺序 | 已定首尾（Poppin'Party → 梦限大 MewType） | 人类定 |
| 四维成长数值 / 成功率梯度 | 占位系数 | 人类定 |
| 能源配额曲线（Day1≈300 / 每 10 天 ×1.8~2.2） | 草案 | 人类定 |
| 星石训练费用曲线 / 招录费用 | 草案（I 级 10 → EX 级数千） | 人类定 |
| 部门场景布局与寻路方案 | 参考脑叶，细节待定 | 人类定 |
| UI 线框（四套）与美术资产 | 待产出 | 人类定 |
| 结局演出内容 | 待定 | 人类定 |
| 本地化语言范围 | 未定（建议中英） | 人类定 |

---

## 变更记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-09-06 | v2.0 | 曾为"程序+美术双 AI 自检手册"（含 F1-F14 图与美术规范） |
| 2026-09-07 | v3.0 | **重写为纯代码开发执行手册**：明确 AI 仅负责代码（框架/功能/审查优化），不参与设计与策划决策；新增职责边界、决策权归属、工作流程、代码质量标准、协作规范；基于 GDD v1.4；适配单人（程序+策划）独立开发场景 |
| 2026-09-12 | v3.6 | **纠正误判 + 规则补强**：§2.2 新增"**异想体绑定 = 可选彩蛋，不阻塞开发**"（不要求一一对应 / 不引出对应角色 / 不得作为里程碑前置条件，GDD §3.2）。依据升 GDD v1.10 |
| 2026-09-12 | v3.5 | 全文档质检修复同步：①§7 界面清单补 **F15 编成界面（L2 部署员工）**，并把「15 张图（F1-F14）」的数字矛盾修正为 **F1-F15**（与 GDD §3.13 二 一一对应），同时注明视觉为原创方向；②全文 GDD 引用统一为 **v1.9**（并修掉 §2 速查与交付模板里残留的 v1.4） |
| 2026-09-12 | v3.4 | 依据 GDD **v1.8 全面对齐脑叶**同步：①中层顺序与天数改为 **中央本部 Day20 → 惩戒部 Day25 → 福利部 Day30**（原 福利21/中本26/惩戒31），解锁序列变为 **1/6/11/16/20/25/30/36/41/46**（不再是等距 5 天一档）；②节点 2/3 对调（情报部=Pastel*Palettes 丸山彩、安保部=Afterglow 美竹兰）；③**队长更正**：Afterglow = **美竹兰**（主唱兼吉他，原误写为贝斯手绯玛丽）、MyGO!!!!! = **高松灯**（原著无队长，本作自定）；④RAS 7→6、Morfonica 6→7。⚠️ 三个中层部门都在 5 的倍数天开放，与记忆日重合 |
| 2026-09-12 | v3.3 | 依据 GDD **v1.7 队长制**同步：①§2.2 新增「部门节点 = 乐队，部长 = 乐队队长」完整 10 节点映射（**移除脑叶 Sephirot 名**：Malkuth/Yesod/… 全部清除，改用队长名）；②新增「队长 = 可派遣的乐队少女之一，不新增 NPC 实体」规则；③修复上一版残留的旧 UI 分层行（L0→L1 每日编成→L2 生命树，已删除）；④全文 GDD 引用统一为 v1.7 |
| 2026-09-12 | v3.2 | 依据 GDD **v1.6 流程重排**同步：①§2.2 每日流程改为「每日剧情 → **L1 生命树（部门开放 / 指派今日异想体）** → 加载 → **L2 编成（部署员工）** → 「开始这一天」→ L3 部门场景」，生命树**每日必经**；②§7 界面清单 F4 改为「L1 生命树·部门开放（每日必经）」、F5 改为「部门解锁弹窗（L1 内触发）」；③UI 分层编号重排 L0 主菜单 → L1 生命树 → L2 编成 → L3 部门场景；④上层部门顺序对齐脑叶：**节点 2 = 情报部 Yesod（Day6）**、节点 3 = 安保部 Netzach（Day11）；⑤全文 GDD 引用统一为 v1.6 |
| 2026-09-07 | v3.1 | 依据外部评审补充：①§9.0 代码交付与 Git 协作流程（AI 可 add/写 commit 信息，**绝不 push/回滚**）；②§3.1 任务循环加"⑤ 验收"完成判定；③§4.2 单类行数放宽至 500 行（需注释说明）+ 占位值注释须关联 GDD 章节；④新增 §6 测试规范（纯逻辑单测 + 最小测试场景，不为覆盖率而测）；⑤节号顺延（界面清单→§7、自检→§8、协作→§9、待定项→§10）。**未采纳**：GDD 升 v1.5（无实质设计变更）、GDD 改名带版本号（会破坏引用） |
