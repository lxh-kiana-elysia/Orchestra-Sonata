# 2026-09-11

## M2 第 2 小步（F3 设置面板）进行中
- 晚风已写 `Entities/SettingsData.cs`（3 个音频字段 + ResetToDefault）与空的 `Systems/SettingsSystem.cs`。
- 复审问题：SettingsData **又误继承 MonoBehaviour 并写了 Start()**（数据类不能是组件，JsonUtility 无法反序列化 MonoBehaviour）→ 已再次纠正，并说明"默认值应由 SettingsSystem 在无存档文件时调用 ResetToDefault"，数据类不应有生命周期方法。
- SettingsSystem 晚风表示"没看懂" → 已从职责/分层/static 原因/文件读写四要点（persistentDataPath、Directory.CreateDirectory、File.Exists、JsonUtility.ToJson/FromJson）逐条讲解，并给出**仅含空方法体的骨架**让他自行实现。
- 约定：设置存 `persistentDataPath/ResonanceShelter/settings.json`（文档未规定，属设计空白，晚风可改）；Load 暂时可由面板/主菜单触发，待第 3 小步状态机完成后挪到 BOOT 阶段。
- 复审第二轮：`SettingsData.cs` 已完全正确（去 MonoBehaviour/Start，字段与默认值方法保留）。
- `SettingsSystem.cs` 存在 5 类错误，均为新手典型：①类未加 static；②Load 的判定依据写成"内存里 Current 是否为 null"（应为 `File.Exists(FilePath)`）；③`File.ReadAllText` / `JsonUtility.ToJson` 的**返回值被丢弃**（未接住字符串），且 FromJson 误传文件名当内容；④`Directory.CreateDirectory` 误传**文件路径**（会建出名为 settings.json 的文件夹）；⑤Save 误用 `Current.ToString()`（写出类名而非 JSON）且声明为 private（UI 无法调用）。核心教学点沉淀：**有返回值的函数必须接住返回值**。
- 第三轮：5 处已全部修正（static 类 / File.Exists / 接住 ReadAllText+FromJson / GetDirectoryName / public Save / ToJson 接住）。剩 **1 处新引入的空引用 bug**：`Current = FromJson(...)` 后若为 null，直接 `Current.ResetToDefault()` → NullReferenceException，须先 `Current = new SettingsData()`。
- 已交付 UI 层任务（SettingsPanel）：副本机制、面板开关、Slider 回调、主菜单接线；提示 Slider 的 OnValueChanged 要选 float 重载、赋值 value 会触发回调。
- UI 层复审：`SettingsPanel.cs` 有 4 处必修——①`_draft` 只声明未 `new`（空引用崩溃）；②`Open()` 方向反了（应从 Current 复制到 _draft 再刷新 Slider，他写成从 Slider 读进 _draft）；③三个回调返回 `SettingsData`（UnityEvent 只认 void 方法，下拉列表里会找不到）；④`OnSave` 未把 _draft 写回 Current 且未关闭面板、`OnCancel` 未关闭面板。`MainMenuScreen` 接线正确。
- 第四轮：4 处全部改对（_draft 在声明处 new、Open 复制→刷新顺序正确、回调 void、OnSave 三步齐全、OnCancel 调 Close）。仅剩 `ResetToDefault()` 未刷新 Slider 显示（草稿已改但界面无反馈）。等晚风跑完 6 项 Play 验收后进 M2 第 3 小步（状态机 + 屏幕栈）。
- **验收失败排查（两处绑定问题，均已定位）**：①「保存后重新打开不显示新值」→ 查场景序列化，**三个 Slider 的 On Value Changed 全部未绑定**（无 OnMasterChanged/OnMusicChanged/OnSfxChanged），故拖动不更新 _draft，保存等于存旧值；②「恢复默认无响应」→ **方法从 `ResetDefault` 改名为 `ResetToDefault` 后，Inspector 绑定仍是旧名**，绑定失效（UnityEvent 按方法名字符串保存）。教训沉淀：改 public 方法名必须回 Inspector 重新绑定。
- **第二轮验收失败（"保存后再打开全是 0"）排查**：①已探明磁盘 `AppData/LocalLow/DefaultCompany/乐团鸣曲/ResonanceShelter/settings.json` 内容确为 `{"masterVolume":0.0,...}`；②滑块三处绑定已补齐 ✅；③「恢复默认」绑定**丢失**（旧名绑定被清，需重绑）；④场景滑块 Min0/Max1 正常，排除 clamp。根因两条：**(a) `OnSave()` 顺序错**——先 `Save()` 后 `ApplyDraftToCurrent()`，导致写盘的是上一次的 Current（永远慢一拍）；**(b) `_draft` 在字段初始化时 `new`（全 0）**，若未 Open 就触发保存，`Apply` 会把 Current 清成 0，再保存即写全 0。修法：Apply 必须在 Save 之前；`_draft` 改为在 `Open()` 内创建 + OnSave 判空。测试前需删除已污染的 settings.json。
- **第三轮排查（"打开面板滑块仍全 0"）**：代码已全部正确（_draft 在 Open 内创建、OnSave 顺序 Apply→Save→Close、三滑块与 ResetToDefault 绑定均已就位），且磁盘文件仍为 `{"masterVolume":0.0,...}` → **根因是遗留的坏存档数据**：Load() 忠实地把 0 读进 Current，与代码无关。结论沉淀：**改代码不会自动修复已被写坏的数据**；排查顺序应为"先验证输入数据，再看代码"。优先用「恢复默认 → 保存」让程序自愈并顺带验证该按钮；备选是直接删除该文件。注意音量 0 是合法值，不可用"=0 即损坏"做校验。
- **第四轮排查（仍未解决）**：①磁盘文件写入时间 18:41:24 落在测试期间 → 证明 `Save()` 确实执行、且当时 `Current` 为全 0 → 即保存瞬间 `_draft` 是 0（"恢复默认"设的值在保存前丢失）；②SettingsPanel 场景中唯一一份、初始 `m_IsActive: 0` ✅；③三滑块 `OnValueChanged` 绑定模式 `m_Mode: 4`（Float）正确、目标组件正确；④`Assembly-CSharp.dll`(18:37:17) 比源码(18:37:16) 新 → 代码已编译，非旧版本问题。静态阅读已到极限 → 改为**加日志观测运行时值**（Open / ResetToDefault / OnSave 三处打印 _draft 与 Current），并要求先删除污染文件以隔离变量。
- **根因确认（日志 + 场景序列化双重验证）**：日志显示 `[Open] Current=1/0.8/0.8 draft=0/0/0`、`[Reset] draft=0/0/0` → `_draft` 每次被写回 0。场景里三个 Slider 的 `OnValueChanged` 绑定均为 **`m_Mode: 4`（Float = 使用常量参数）+ `m_FloatArgument: 0`**，即回调每次收到**常量 0**，而非滑块实时值（动态绑定应为 `m_Mode: 0` EventDefined）。因果链：Open 里 RefreshSliders 赋值→回调传 0→草稿清零；ResetToDefault 同样被清零；保存即写 0。
- 教训（重要，已入项目记忆候选）：**UnityEvent 绑定要选"动态参数"版本（用事件实时值），不要误选带常量输入框的静态参数版本**；更稳的做法是**代码里 AddListener**，避免改名失效与选错模式两类坑。
- 已按方案 B 修复：`SettingsPanel.OnEnable/OnDisable` 里 `AddListener/RemoveListener` 三个滑块，晚风确认功能恢复正常。
- 复审遗留：**场景里三条 Inspector 旧绑定未删除**（OnMasterChanged/OnMusicChanged/OnSfxChanged 仍在，m_CallState=2 运行时会触发）→ 目前是**双重触发**（常量 0 一次 + 代码真实值一次），能正常只因调用顺序恰好"常量先、真值后"，属侥幸，须删除旧绑定。另：调试用的 3 行 Debug.Log 待 M2 收尾时清理。
- 晚风反馈"已删滑块绑定 + 已清 Debug"：核实 `SettingsPanel.cs` 的三行日志**已删除** ✅；但场景文件中三条滑块 Inspector 绑定**仍在**（337/1238/2068 行）→ 判断为**删除后未 Ctrl+S 保存场景**（或删错位置：绑定挂在 Slider 上而非 SettingsPanel 上）。待其保存后复核。
- ✅ **M2 第 2 小步完成**：晚风保存场景后核实，`MainMenu.unity` 仅剩 7 条绑定（主菜单 4 按钮 + 设置面板 3 按钮），三条滑块 Inspector 绑定已清除；`SettingsPanel.cs` 调试日志已删。设置面板全套验收通过（打开/拖动/保存/持久化/取消回滚/恢复默认）。
- 已交付 **M2 第 3 小步（最后一步）：状态机 + 屏幕栈**——GameState 枚举（先实现 4 个）、GameManager.ChangeState 统一入口 + 返回栈、UIManager 按状态切换界面；UI 不再互相直接调用。EventManager 广播留到 M3。

## 查阅 M3「生命树」内容（文档侧汇总）
- 规格出处：GDD §3.1 四（L2 定义与部门-节点映射简表）、GDD §3.14 二（10×3 层完整映射：控制部/Malkuth/Day1、安保部/Netzach/Day6、情报部/Yesod/Day11、培训部/Hod/Day16、福利部/Chesed/Day21、中央本部/Tiphereth/Day26、惩戒部/Geburah/Day31、记录部/Hokma/Day36、研发部/Binah/Day41、构筑部/Kether/Day46）、FDD-08（编成与部门节点，仅 3 条）、UI_Interaction_Spec F4/F5、ARCHITECTURE §2.2（TREE_OVERVIEW 状态）与 §5.2 数据流、agent.md §5 Phase 2 与 §7 的 F4/F5。
- **发现 3 个阻塞/待补问题**：
  1. **GDD 内部冲突**：§3.1 简表写「节点 2 = 情报部」，§3.14 写「节点 2 = 安保部 Netzach、节点 3 = 情报部 Yesod」。建议以 §3.14 为准，回头改 §3.1。
  2. **工程缺部门节点数据源**：`Assets/Data/` 只有 Characters/BandMembers/Aberrations/GameConfig 四件套，**没有 Bands.json（或等价的部门节点配置）**——生命树无数据可渲染。M3 需先补配置（建议字段：id / band / sephirot / layer / unlockDay / defaultState）+ 数据类。
  3. **FDD-08 过薄**：无状态机、无数据结构、无配置项、无边界条件（如当天进行中能否从 L3 回到 L2 切换部门未定义），M3 直接照写会缺依据。
- 代码侧现状：无 TreeOverview/Department 场景，`GameManager.cs` 仍是 M1 单例骨架，无状态机（M2 第 3 小步进行中）。