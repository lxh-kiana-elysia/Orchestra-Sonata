# 2026-09-06

## Phase 0 游戏框架落地（基于 agent.md）
- 工程：Unity 6000.0.38f1（`乐团鸣曲/乐团鸣曲`），空工程，uGUI 2.0.0 可用，activeInputHandler=2(Both)。
- 按 agent.md §8 创建目录：Scripts/{Systems,Entities,UI,Narrative}、Data/Dialogues、Resources/{Art,Audio,Fonts}。
- 数据层：`Assets/Data/` 下 GameConfig.json（全部占位系数集中于此）、Characters.json（香澄/多惠）、Aberrations.json（"熄灭的太阳"，§5 Schema）。
- 代码：命名空间 `YuetanMingqu`；实体层 Character/Aberration/WorkResult/GameConfig/Enums（I-EX 分档、派遣门槛）；系统层 GameManager 单例（三层资源：情感粒子/星石/异想体点数）、DataManager、EventManager、WorkSystem（GDD 3.4 判定公式）、UIManager；UI 为程序生成的占位 uGUI（GameBootstrap 自动引导，空场景直接 Play）；叙事层 DialogueTrigger（状态层）+ DialogueManager（Yarn 封装占位，Phase 3 接入）。
- JsonUtility 不支持 Dictionary → 四种工作的 JSON 键用固定中文字段（合法 C# 标识符）。
- 已更新 agent.md 变更记录。未改任何 GDD 已定稿数值；待定项（§9）保持占位。
- 注意：agent.md 铁律——Git 推送由用户自行执行；待定项 AI 不得私自补全。
- 修复编译错误：GameBootstrap.cs 缺 `using UnityEngine.UI;`（CanvasScaler/GraphicRaycaster 在该命名空间下）。排错技巧：Unity 安装路径不在 C 盘默认位置（在 E:/03_开发编程/Unity引擎/），编译错误可从 `%LOCALAPPDATA%/Unity/Editor/Editor.log` 用 `grep "error CS"` 直接提取，无需用户进 Safe Mode 手抄。
- 用户称呼：晚风。

## v1.3 框架开发（agent.md 升级后，仅框架/无美术）
- v1.3 要点：员工体系（normal/band）、天数轴、记忆日（5/10/15…45 锚定+回滚）、能源配额曲线（Day1≈300，每10天×2.0，考验日×1.3）、真/假结局（完整版 Day46 窗口/Day50；首版占位 Day15 窗口/Day20/2 考验）。
- 落地：Character+Enums 加 EmployeeType；GameConfig 加 DayLoopDto/TrialConfigDto/TrialState + GetDailyQuota；新增 TrialSystem.cs、SaveSystem.cs（JSON 存档 persistentDataPath/Saves，SaveData 快照结构与记忆日共用）；GameManager 重写日循环（锚定/回滚/结局判定）；UI 加存档/读档/回滚按钮与配额/考验显示。
- 注意：GameConfig.json 被晚风手动调过工作产出值（30/20/15/20 等），尊重其改动，只追加字段。
- agent.md 变更记录已补两行（设计同步 + 开发落地）。

## v1.3 第二轮：招录/捏人 + 星石训练（agent.md §7.1 新增条目）
- appearance 字段：普通员工 {hair_style,hair_color,eye_style,expression}；乐队少女 null 不可改。
- RecruitmentSystem：50 星石生成候选（随机姓名/外观/单项四维倾向+2~5）、AdjustCandidate 捏人 API、Hire/Discard、槽位上限=设施等级映射 [2,4,6,8]。
- TrainingSystem：费用按目标档 [10,30,100,300,800,2000]，每次 +2 点（HP/SP 加上限+当前，演奏/共感加值，上限 120），复用 CharacterData.ApplyGrowth。
- GameManager：SpendStarStones / FacilityLevel(1-4) / UpgradeFacility（facility_costs 草案）。
- 存档兼容招录员工：SaveData 存完整角色信息（含外观），RestoreState 按 id 重建缺失员工、移除快照后新招员工（回滚语义）。
- UI：招录/录用/训练×4/升级设施按钮，资源栏显示设施 Lv 与员工数。
- 编译修复：RecruitmentSystem `Random` 歧义（UnityEngine.Random vs System.Random）→ 显式 System.Random。

## v1.3 第三轮：L0-L3 界面分层 + 脑叶式实时工作
- agent.md 新增 §1.1（UI 分层）+ §3 执行方式（实时操控、进度条、中断无收益、暂停/变速）。
- UIManager 重写为 UILayer 切换（UIScreen 基类）；4 个 Screen：StartMenu/Formation/TreeOverview/Department，全程序生成占位 UI；旧 MainCanvas.cs 已删除。
- RealTimeWorkSystem（MonoBehaviour）：任务进度 += deltaTime，完成→GameManager.PerformWork 结算；中断/换人/EndDay 清空均无收益。TimeControlSystem：timeScale 档位 [0,1,2,3]。
- EmployeeAgentView：占位方块（蓝=乐队/灰=普通/红=崩溃/金=选中），点地移动（直线，A* Phase 2），头顶进度条用锚点宽度实现（Filled 需 sprite）。
- GameConfig 新增 departments（节点1=PPP Day1、节点2 Day6）、lineup_max=4、work_duration_seconds=5、time_speeds。
- GameManager：TodayLineup/StartDay/DayInProgress；EndDay 清编成并中断工作。
- Unity 批处理编译在本机不可用（UPM 起不来 + 资源刷新卡死），编译验证靠用户 GUI 打开 + 我读 Editor.log 的 "error CS"。

## v2.0 重写：F1-F14 全部界面 + 状态机（程序侧框架）
- agent.md v2.0 = 程序+美术双 AI 手册（F1-F14 参考图、§10 状态机、UIConfig 驱动、§13 自检）。
- GameState 枚举替代 UILayer；UIManager 覆盖态（PAUSE/SETTINGS/GLOSSARY）+ReturnState+全局 ESC；GameManager.LoadAll 后 BOOT→LOADING。
- UIConfig.json（palette/工作色/节点色/能源阈值/速度名/加载步骤/移动速度）+ StylePalette.json（美术镜像）；Data.UI 访问器。
- 新屏：LoadingScreen(F2)、SettingsScreen(F3+SettingsSystem settings.json)、PauseScreen(F10 冻结/5项)、CodexScreen(F13/14 观察等级解锁+PE-BOX)、CutsceneScreen(F11/12 占位≤3选项)。
- 生命树三态+解锁弹窗；unlocked_nodes 进存档；无开放部门→编成确认引导 L2。
- L3：F7 悬停 tooltip、F8 工作弹窗（WorkSystem.EstimateSuccessRate）、F9 能源时间条 EnergyColor。
- 坑：GameBootstrap 在 GameManager.Start(LoadAll) 前建屏 → Build 阶段不能碰 Data.UI（用 null 守卫/OnShow 里取）；初始 Show(LOADING) 由 GameManager.Start 触发。

## v2.0 自检（§13.1 + 铁律）
- 通过：违禁命名 0、编译错误 0、数据文件 5 个齐备、已定稿数值（120/10/0.05-0.15/10:1/派遣门槛/配额 300）未动、参考图 15 张 PNG 在仓库根目录、状态机/F1-F14 全覆盖、变更记录已更新。
- 已知偏差①：UI 结构占位色 58 处硬编码（语义色已走 UIConfig；占位灰待 Phase 2 线框图+美术替换时一并迁入 UIConfig）。
- 已知偏差②：git 尚有未提交改动（等晚风确认编译全绿后整理 commit）。

## 美术素材接入（晚风供图）
- 素材归档：背景→Art/BG/department/bg_ingame.jpg（1200×571）；10 立绘→Art/Char/band/{id}_stand（~640×1000 竖版）。id 映射：tomori/anon/rana/taki/soyo（MyGO!!!!!）、sakiko/uika/umiri/mutsumi/nyamu（Ave Mujica）。
- Characters.json 补 10 名乐队少女（全 10 初始，技能占位；全员可见仅框架演示，章节解锁顺序仍待定）。
- ArtManager：Resources.Load<Texture2D> + Sprite.Create（**免 .meta 导入器 Sprite 设置**，免编辑器操作）+ 缓存 + 失败 null。
- UI：CharacterSlot 头像 preserveAspect；EmployeeAgentView EnvelopeParent+Mask；Cutscene 立绘；主菜单/L3 背景 AddFullScreenBackground（EnvelopeParent+Mask+raycast off）。兜底=色块/占位字。
- 注意：立绘合规（不得为官方素材）由晚风负责。
