# 2026-09-08

## 导师制重启开发
- 晚风以新提示词重启：AI 任 Unity 编程导师，M0-M5 分步教学，每次只给一步，四件套（目标/手动/AI生成/验证）。
- 已通读全部新文档：GDD v1.4（抽取到 .workbuddy/gdd_extract.txt，511 行）、agent.md v3.1、ARCHITECTURE v1.0、FDD v1.2 与 UI 规范章节结构。
- 已交付 M0 指导（工程跑通：Hub 开工程→SampleScene→Play），零代码。等"M0 完成"回执。
- M1 预告需用户拍板：JSON 库选型（JsonUtility vs Newtonsoft，agent.md §4.3）+ 命名空间是否沿用 YuetanMingqu。
- 环境：python venv 已建于 binaries/python/envs/default（含 python-docx）；注意给 Windows 程序传路径须用 C:\ 风格，/c/ 风格会静默失败。
- M1 已交付。拍板：JSON=JsonUtility（讲透优缺点后晚风选定）、命名空间=YuetanMingqu。
- 重要发现：晚风自学先行——已自建 MainMenu.unity（删了 SampleScene 并注册 Build Settings）、写了 GameManager 单例（质量好，已保留逻辑仅加命名空间，文件原名"Game Manager.cs"带空格已改名）、DataManager stub（Application.dataPath 直读，打包会崩，已替换为静态类版）、GameConfig.json（gain_per_train:2 与 GDD §3.6 的 +1 冲突，已按 GDD 修正并加 _note 占位注释）。整合方案经确认后执行，M1 演示改在 MainMenu 场景。
- M1 文件：Data 三件套（香澄/有咲四维全10、熄灭的太阳全 GDD §3.2 数据、GameConfig 带 _note）+ Entities 4 个 + Systems/DataManager + DataLoadDemo。有咲 skill 留空=待定（策划内容不发明）。

## 教学模式切换（同日 17:20）
- 晚风明确要求：不要替他写完代码，要"逐步引导我自己动手实现"；提问时讲原因+方法，不给完整答案。
- 已将我生成的 M1 全部文件（3 JSON + 6 .cs + meta）移出工程到 `.workbuddy/reference_m1/{json,scripts}` 作为参考答案；工程内仅留晚风自写的 `Assets/Scripts/Systems/GameManager.cs`。
- 已交付**第 1 步：数据层——亲手把 GDD 三张表写成 JSON**（Characters/Aberrations/GameConfig），含目标/核心概念/流程作用/任务/提示/验收清单。第 2 步待做：C# 数据类 + 读取。
- 协作约定已写入用户级与项目级 MEMORY.md（教学方式优先级最高）。

## 文档升版：GDD v1.5 + FDD v1.6（同日 17:50）
- 重读全部文档。M1 数据层按 v1.5 重讲：4 个数据文件（Characters/BandMembers/Aberrations/GameConfig），乘数语义，specialEffects，minDay/weight，Day1 固定异想体。
- 发现文档冲突/未定（已列为需晚风拍板）：①配置键命名 FDD camelCase vs ARCHITECTURE snake_case；②训练加点 GDD §3.6 示例 +1 vs FDD §9 +2；③`pointsPerSuccess` 语义 GDD §3.2=异想体点数 vs FDD §10.3 注释=情感粒子；④乐队少女"专属装备/加入条件"字段名 GDD 未给。
- 晚风工程里的 3 个 JSON 目前为 0 字节（只建了空文件），尚未写入内容。

## 文档体系二次改版（同日 22:30）
- 新增独立策划文件：`Band_Members.md` / `Aberrations.md` / `Equipments.md` / `Quests.md` / `Narrative_Index.md`；FDD→v1.9、ARCHITECTURE→v1.1。
- 已解决我提的 4 项：①配置键统一 **camelCase**（agent.md §163 强制，禁 snake_case）；②四维初始值进配置、训练按级提升；③`pointsPerSuccess` 拆分（energy/points/accumulated）；④`personalLineProgress`/`unlockedStoryNodes` 移至运行时状态。
- 晚风写出第一版 BandMembers.json（香澄），已按新文档复审：主要问题为 JSON 语法（键名引号/尾随逗号/equipment 结构）+ 数值与 Band_Members.md 定稿不一致 + 装备应只存 id（Equipments.md 已有权威数据）。
- 决策：晚风采纳"装备只存 ID"建议；ID 一律取 Equipments.md（`ego_weapon_sun_01` / `ego_weapon_score_01` 等），不自创 `eq_xxx`。
- **Characters.json 结构属文档空白**（普通员工为每日随机招录生成，无名单文件），已给两个方案待晚风定：①只放示例员工（够验证读取）②放招录素材库（namePresets/appearancePresets）。
- 复审 Aberrations.json（熄灭的太阳）：顶层未包壳、数字写成字符串、workModifiers 写成裸对象、字符串内嵌英文双引号、escapeTrigger/escapeBehavior 混淆——已逐条反馈。
- 发现文档不一致待晚风定：Aberrations.md 图鉴①写太阳 minDay=7，但同文件抽取速查写"默认 minDay=1、不再按等级设硬门槛"。→ **晚风裁定：统一 minDay=1**（文档待同步改）。
- Characters.json 结构：晚风选**方案 A**（只放 1 个示例员工，够验证读取；招录素材库等 M4 再做）。
- 第二轮复审：两文件均已正确包壳；workModifiers 数组写法掌握；爱音装备 ID 正确。剩余问题集中在：字符串内嵌英文双引号、数字写成字符串、数组后缺逗号、`_note` 误用为字段值（应是兄弟字段）、香澄装备 ID 未换成文档 ID。
- **第三轮：BandMembers/Aberrations/Characters 三文件均通过 JSON 解析**（aberrations[1]/bandMembers[2]/characters[1]）。Characters.json 由晚风按方案 A 写出，规范（camelCase + _note + 无运行时字段）。
- 已交付 GameConfig.json 写法讲解（概念/结构/取值来源 FDD §9 + ARCHITECTURE §4.4 + Aberrations.md 抽取概率表）。要点：训练已改为按级提升，费用 5 档 [10,30,100,300,800]，无 gainPerTrain。
- ✅ **第 1 步完成**：4 个数据文件全部通过 JSON 解析；drawRates 10 行逐行验算合计均为 1.0；乐章 weight=0、修正顺序（创作1.1/谈话1.0）已修正。
- 已交付**第 2 步**：C# 数据类（Character/BandMember/Aberration+WorkModifier/GameConfig 及壳类）+ DataManager + DataLoadDemo，要求 Play 后在 Console 打印。强调 JsonUtility 铁律：只认 public **字段**（不认属性）、键名逐字符一致否则静默为 0。
- 晚风完成 Character / BandMember / Aberration 三个数据类（复审：曾误用 `: MonoBehaviour`、壳类字段名缺 s、workModifiers 写成单对象、情绪值用 int、SpaceAttribute 误用、target 拼错——均已修正）。
- 已按晚风要求为三个数据类**补全中文注释**（每字段含义+用途+文档出处+是否占位/运行时），并移除未使用的 `using UnityEngine`。字段与类型一字未改。
- 教学要点沉淀：晚风曾误以为 `[Serializable]` 只用于壳类 —— 已纠正为"每一层被读写的类都要加"。
- ✅ **第 2 步第 1 小步完成**：4 个数据类（Character / BandMember / Aberration / GameConfig，含 WorkModifier·SpecialEffect·Equipment·DrawRate 与 3 个壳类）全部定稿并与各自 JSON 逐字段对齐。
- ✅ **M1 收官**：DataManager（静态类，LoadAll + GetBandMember/GetAberration/GetCharacter）+ DataLoadDemo 完成；晚风确认 Unity 无报错无警告、Console 打印符合预期（员工1/少女2/异想体2/配额300/香澄+aber_sun_01）。代码已复审通过。
- M2 已开讲（F1 主菜单）：Canvas + EventSystem + 4 按钮（新游戏/继续/设置/退出）+ MainMenuScreen 脚本；继续游戏无存档时置灰；按钮先只 Debug.Log（UI 只转发意图，状态切换归 GameManager）。依据 UI_Interaction_Spec F1 / GDD §3.1 L0。
- **M1 已提交：d63f707**（feat(M1): 数据层跑通）。暂存时排除 .workbuddy/gdd_extract.txt 与 reference_m1（缓存/参考答案）。推送由晚风执行：`git push origin main`（本地 ahead 15，无分叉，无需 force）。
