# 2026-09-12

## 两项设计定稿（用户拍板），已同步全部文档

### 1. 上层部门顺序对齐脑叶：节点 2 = 情报部
- 联网核实：脑叶上层 Asiyah 官方顺序 = 控制部 Malkuth → **情报部 Yesod** → 安保部 Netzach → 培训部 Hod；开放日 Control Day1 / Information Day6 / Safety Day11 / Training Day16。
- GDD §3.14 二 的 10×3 表原写「节点2=安保部 Netzach Day6、节点3=情报部 Yesod Day11」→ **已对调**：节点2=情报部/Yesod/Day6，节点3=安保部/Netzach/Day11，培训部 Hod/Day16 不变。GDD §3.1 简表原本就是情报部，两处现已一致。
- 遗留（未擅自改）：中层 GDD 现为 福利 Day21 / 中央本部 Day26 / 惩戒 Day31，与脑叶（中本 Day20-22 → 惩戒 Day25 → 福利 Day30）不一致，待用户决定是否对齐。

### 2. 流程顺序重排：生命树（L1）→ 编成（L2）→ 部门场景（L3）
- 用户定稿流程：**进游戏 → 加载动画 → 剧情 → 生命树（选择开放部门）→ 加载动画 → 部署员工界面 → 准备就绪进入正式游戏**。
- 关键澄清（用户原话）：**生命树每天都经过**，因为每个部门异想体最多 4 个，而每天都会选异想体（**5 的倍数天除外**），「部门开放选择」控制的是**异想体具体放哪个部门**。
- 层级编号按用户选择**重排**（编号 = 实际顺序）：L0 开始菜单 / **L1 生命树·部门开放** / **L2 编成·部署员工** / L3 部门场景。
- 「开始这一天」按钮归属 L2 编成，点击后**直接进 L3**（不再经过生命树）。

## 文档改动清单
| 文件 | 版本 | 改动 |
|---|---|---|
| GDD.docx | v1.5（未升版，待用户确认） | §3.1 流程总览句、L0-L3 层级表、三/四两节整体互换重写（三=生命树含收容分配规则、四=编成）、新游戏/继续游戏目标、暂停菜单两条回滚路径、F4/F6 层级标注、记忆日锚定界面、UI 线框待办；§3.14 二 情报部↔安保部对调 |
| FDD.md | **v2.3** | FDD-08 重写为 8 小节：8.1 生命树职责表 / 8.2 编成 / 8.3 节点状态机 / 8.4 `Bands.json` 草案 / 8.5 边界与待确认 / 8.6 验收点 |
| UI_Interaction_Spec.md | **v1.2** | F4 重写（入口=每日剧情后、收容分配、x/4、5 倍数天跳过、继续→加载→L2）、F5 入口、F1 新游戏/继续目标、F9 入口、F10 回滚目标、界面流转总览重排 |
| ARCHITECTURE.md | v1.0 | §2.1 屏幕顺序、§2.2 状态机（CUTSCENE→TREE_OVERVIEW→LOADING→DEPLOY→DEPARTMENT）、§5.2 数据流 ①② 互换重写 |
| agent.md | v3.1 | §2.2 每日流程与 UI 分层两条、§7 F4/F5 程序要点 |
| README.md | — | 修正版本漂移（GDD v1.5 / FDD v2.3 / UI 规范 v1.2），当前阶段改为 M1 已过、M2 进行中 |

## 新暴露的待确认项（已写入 FDD-08 §8.5）
1. **工程缺部门节点数据源**：`Assets/Data/` 只有 Characters / BandMembers / Aberrations / GameConfig，**没有 `Bands.json`**，M3 生命树开工前必须补（字段草案见 FDD-08 §8.4）。
2. **L3 内如何切换到另一个已开放部门**未定义（建议：L3 侧栏加部门切换入口）。
3. 所有已开放部门均满 4 个、当天仍抽到新异想体时如何处理（建议：允许暂不收容，异想体回池）。

## 版本号升级（用户确认「更改版本号」）
| 文件 | 旧 | 新 | 说明 |
|---|---|---|---|
| GDD.docx | v1.5 | **v1.6** | 「文档版本：v1.6（流程重排：生命树前置为 L1 且每日必经 + 上层部门顺序对齐脑叶）」；修订日期改 2026-09-12 |
| ARCHITECTURE.md | v1.0 | **v1.1** | 新增变更记录条目（流程重排同步 4 项） |
| agent.md | v3.1 | **v3.2** | 新增变更记录条目（流程重排 + 部门顺序 + GDD 引用统一 v1.6） |
| FDD.md | v2.3 | v2.3 | 仅头部「上游依据」升 GDD v1.6 / 架构 v1.1 / 手册 v3.2，§0 数值来源升 v1.6 |
| UI_Interaction_Spec.md | v1.2 | v1.2 | 仅头部依据升 GDD v1.6 / 架构 v1.1 / FDD v2.3 |
| README.md | — | — | 文档表与阶段行统一为 GDD v1.6 / 架构 v1.1 / FDD v2.3 / UI v1.2 / 手册 v3.2 |

**GDD 内一并修掉的陈旧引用（v1.6 附带）**：
- §1.3 核心循环（1）→ 补「在 L1 生命树里决定收容进哪个已开放且未满的部门（每部门上限 4，草案）」
- §1.3 核心循环（2）→ 「编成界面见 3.1-L1」改为 **3.1-L2**
- §3.13 F4 程序实现要点 → 「OPEN 节点点击进入 L3」改为「OPEN 节点点击=指派今日异想体（4/4 拒绝）；点「继续」→ 加载 → L2 编成」

**未动**：5 个内容策划文件（Band_Members / Aberrations / Quests / Narrative_Index / Equipments）头部「依据 GDD v1.5」保留——本次流程重排未涉及其内容，改动它们会误导为已按 v1.6 复核。

**技术备注**：GDD.docx 的 MCP 编辑器通道（`editor_sdk` / `doc_find_and_replace`）在本轮不可用（报 `document is not open`）。改用**直接改 zip 内 `word/document.xml` 再重写 zip** 的方式完成，前提是目标字符串必须落在**单个 `<w:t>` run** 内（已校验：5 处全部命中 1 次）。备份 `GDD.docx.bak_v15` 已生成后删除（git 里有 v1.5 版本可回溯）。

---

# 第三项设计定稿：部门部长 → 乐队队长（GDD v1.7）

## 用户拍板的三项决定
1. **10 支乐队节点顺序 = 按剧情/情感深度递进**（不是官方登场顺序）：
   Poppin'Party → Afterglow → Pastel*Palettes → Hello Happy World → Roselia → Morfonica → RAISE A SUILEN → MyGO!!!!! → Ave Mujica → 梦限大MewType
2. **队长 = 可派遣的乐队少女之一**，不是独立 NPC。有专属台词 + 部门加成，可工作、崩溃退场 3 天、不死亡，不额外占用派遣名额。
3. **Sephirot 代号完全移除**（Malkuth/Yesod/... 全部清除，UI 只显示队长名）。

## 队长名单（已联网核实原著）
| 节点 | 脑叶部门 | 乐队 | 队长 | 天 |
|---|---|---|---|---|
| 1 | 控制部 | Poppin'Party | 户山香澄 (Vo&Gt) | Day1 |
| 2 | 情报部 | Afterglow | **上原绯玛丽 (Ba)** ⚠️ | Day6 |
| 3 | 安保部 | Pastel*Palettes | 丸山彩 (Vo) | Day11 |
| 4 | 培训部 | Hello, Happy World! | 弦卷心 (Vo) | Day16 |
| 5 | 福利部 | Roselia | 凑友希那 (Vo) | Day21 |
| 6 | 中央本部 | Morfonica | 仓田真白 (Vo) | Day26 |
| 7 | 惩戒部 | RAISE A SUILEN | **CHU² 玉田千裕 (DJ)** ⚠️ | Day31 |
| 8 | 记录部 | MyGO!!!!! | **待定（原著无队长）** ⚠️ | Day36 |
| 9 | 研发部 | Ave Mujica | 丰川祥子/Oblivionis (Key) | Day41 |
| 10 | 构筑部 | 梦限大MewType | 仲町阿拉蕾 (Vo) | Day46 |

**三个坑（以后别再踩）**：
- Afterglow 队长是**贝斯手上原绯玛丽**，**不是美竹兰**（兰是主唱兼吉他、乐队中心，但不是队长）
- RAS 队长**有争议**：CHU²（组建者/全词曲）vs LAYER（台前主唱）→ 现取 CHU²，标注可推翻
- MyGO!!!!! **原著官方未设队长** → 节点 8 留空，建议高松灯，UI 需容错

## 文档改动
| 文件 | 版本 | 改动 |
|---|---|---|
| GDD.docx | **v1.7** | 10×3 表列头 Sephirah→乐队队长、10 行乐队名+队长名全填；F1-F15 表 F4 美术要点"Sephirot 文字背景层"→"队长名文字背景层"、F5"Malkuth 主持开放控制部"→"户山香澄主持开放 Poppin'Party 部门"；表后新增队长机制说明段 |
| FDD.md | **v2.4** | §8.4 Bands.json 补全 10 节点（`sephirot` 字段删除 → `leader` + `leaderId`）；新增 §8.4.1 队长机制；§8.6 补 3 条验收点 |
| agent.md | **v3.3** | §2.2 新增完整 10 节点映射 + 队长规则；**修掉 v3.2 残留的旧 UI 分层行**（L0→L1 每日编成→L2 生命树，重复且过时） |
| UI_Interaction_Spec.md | **v1.3** | F4 节点信息加队长名 + 队长角标（崩溃置灰）+ 空值容错「队长未定」 |
| Band_Members.md | **v1.1** | 新增 `isLeader` 字段说明、队长速查表（含三处风险点备注）；香澄条目标 `isLeader: true` |
| ARCHITECTURE.md | **v1.2** | 上游依据升 GDD v1.7 / agent v3.3；§7 相关文档表；§6.2 引用；明确"架构分层/状态机/接口/数据流均无变化" |
| README.md | — | 文档表与阶段行统一 v1.7 / v1.1→v1.2 / v2.4 / v1.3 / v3.3 |

## 踩坑记录（可复用）
**docx 表格定位不能用通用关键字**：`edit_table` 用 `'Malkuth'` 匹配表格时，**F1-F15 界面表（在文档更靠前）也含 Malkuth**，导致 22 处改动全写进错误表格，把"脑叶界面"列头改成"乐队队长"。
→ 修复：①匹配关键字必须**只在该表出现**（改用列头 `'本作乐队节点（暂定）'`）；②加 `len(hits)!=1` 断言；③加 `expect_rows` 行数断言（10×3 表=11 行，F1-F15 表=16 行）；④**先备份再改**，出错立刻 `shutil.copyfile(BAK, SRC)` 回滚。
（`'Sephirah'` 同样不可用——脑叶机制对照表里有 "核心抑制(Sephirah Meltdown)"。）

## 仍未解决
- ~~中层部门顺序~~ **已解决（v1.8）**
- 乐队 id 缩写（`band_aglw`/`band_pasp`/`band_ros`/`band_moni`/`band_amu`/`band_mwt`）为**建议值，待策划确认**。

---

# 第四项：全面对齐脑叶 + 队长更正（GDD v1.8）

## 用户指令
1. 全面更改对齐脑叶（含中层顺序与解锁天数）
2. 情报部 = 丸山彩；惩戒部 = CHU²；记录部 = 高松灯
3. 追问后补充：**Afterglow 队长是美竹兰，不是上原绯玛丽**；**天数也改成 Day20/25/30**

## 最终 10 节点（v1.8 定稿）
| 节点 | 部门 | 乐队 | 队长 | 解锁 |
|---|---|---|---|---|
| 1 | 控制部 | Poppin'Party | 户山香澄 | Day1 |
| 2 | 情报部 | **Pastel*Palettes** | **丸山彩** | Day6 |
| 3 | 安保部 | **Afterglow** | **美竹兰** | Day11 |
| 4 | 培训部 | Hello, Happy World! | 弦卷心 | Day16 |
| 5 | **中央本部** | Roselia | 凑友希那 | **Day20** |
| 6 | **惩戒部** | **RAISE A SUILEN** | **CHU²** | **Day25** |
| 7 | **福利部** | **Morfonica** | 仓田真白 | **Day30** |
| 8 | 记录部 | MyGO!!!!! | **高松灯** | Day36 |
| 9 | 研发部 | Ave Mujica | 丰川祥子 | Day41 |
| 10 | 构筑部 | 梦限大 MewType | 仲町阿拉蕾 | Day46 |

相对 v1.7 的变化：节点 2/3 对调；Roselia 部门名 福利→中央本部（节点不变）；RAS 7→6；Morfonica 6→7；中层天数 21/26/31 → **20/25/30**。

## ⚠️ 重要教训：队长以策划拍板为准
我联网查到「Afterglow 队长 = 贝斯手上原绯玛丽」（萌娘百科中/繁 + 百度百科均如此写），并在 v1.7 里采信、还特意标注"不是美竹兰"。**策划（晚风）直接推翻：应该是美竹兰。**
→ **原著设定考据结果只作为参考，最终以策划拍板为准**；写文档时应把"核实来源"与"策划决定"分开标注，且策划决定优先级更高。已写入 MEMORY.md 提醒后续会话不要再去"纠正"。

## ⚠️ 副作用（已记录但需留意）
三个中层部门（Day20/25/30）**都落在 5 的倍数天 = 记忆日** → 当天不抽新异想体，生命树只做"开放部门"一步。
好处：部门开放失败可借记忆日回滚。已在 FDD §8.4 / Band_Members.md 标注，用户接受该方案。

## 文档改动（v1.8）
| 文件 | 版本 | 改动 |
|---|---|---|
| GDD.docx | **v1.8** | 10×3 表 15 处单元格（节点2/3 对调、中层部门名与天数、RAS/Morfonica 换位、MyGO 队长填高松灯）；表后说明改为「Afterglow=美竹兰（非绯玛丽）、MyGO=高松灯」；首版解锁说明改为「第6天解锁第2支（Pastel*Palettes）」 |
| FDD.md | **v2.5** | Bands.json 10 条全量重排；§8.6 验收点改为按 10 个 unlockDay 升序校验；新增副作用说明 |
| agent.md | **v3.4** | §2.2 十节点映射 + 天数序列 + 队长更正 |
| Band_Members.md | **v1.2** | 队长速查表重排；风险点备注改写（强调策划拍板） |
| ARCHITECTURE.md | **v1.3** | 仅依据与引用（架构无变化，已注明） |
| UI_Interaction_Spec.md / README / MEMORY.md | — | 版本对齐 |

---

# 第五项：全文档质检（2026-09-12）

产出：`质检报告_2026-09-12.md`（项目根目录）。方法：全文提取 + 跨文档关键字交叉比对 + 章节结构比对 + 仓库资产与文档引用比对。
结果：**6 项 P0 / 8 项 P1 / 6 项 P2**。

## 6 项 P0（必须立刻修）
1. **GDD 内部两张节点表冲突**：§3.1 三 末尾「附：部门-节点映射表」仍是旧版（`2 | 第 2 支乐队（待定） | Day 6 | 情报部`、`3-10 | 后续乐队（待定名单）`），与 §3.14 定稿的「节点2 = Pastel*Palettes / 丸山彩」矛盾。**改 §3.14 时漏改了这一处**。
2. **GDD §3.13 视觉风格总则 与 §5.2 美术边界 直接矛盾**（最严重）：§3.13 写"继承脑叶的低饱和度+霓辉强调色""与脑叶一致""UI 元素：等距/直角边框、斜向胶带感、黄色警戒条纹、齿轮/机械骨架""沿用脑叶"；§5.2 写"不照搬脑叶视觉/配色/氛围""不复用工业灰绿、机械骨架""圆润柔和、避免尖锐棱角"。另 F1 行"暗色背景"与 §5.2"明亮温暖"冲突、F9 行"绿色机械舱壁"违反边界。**§5.2 是 2026-09-07 策划定稿，以 §5.2 为准，§3.13 是更早版本未同步**。
3. **FDD-01 §1.2 状态机掉了队**：仍写 `DAY_START（编成 DEPLOY）`、两条回滚路径都写"回到当日编成"，**缺 TREE_OVERVIEW 状态**。ARCHITECTURE §2.2 与 agent.md §2.2 已正确，只有 FDD 没同步。
4. **UI 规范缺「L2 编成」的 F 条目**（F1-F14 里没有），FDD-08 §8.2 只有职责描述。线索：仓库里 `进入正式管理前的游戏准备界面.png` 是唯一未被任何文档引用的孤儿图，疑似 L2 编成参考图 → 建议补 F15。
5. **Quests.md 首版缺口**：只有 PPP 与 **MyGO**（节点8/Day36，首版用不到）的任务；**首版第 2 支 Pastel*Palettes 的 3 个乐队任务与 `trial_pasp` 完全没写**。另有 line38「第2支乐队（待定名单）」、line54「MyGO!!!!!（节点待定）」未更新。
6. **Band_Members.md 同样错位**：定稿 2 名 = 香澄(PPP) + **千早爱音(MyGO 节点8)**，但首版第 2 支是 PasPale → **缺丸山彩**；10 位队长只有香澄标了 `isLeader: true`；line142「第 3-10 支乐队名单待定」已过期。

## P1（8 项）
- 4 个内容文件版本引用过期（Aberrations/Quests/Narrative_Index/Equipments 仍写 GDD v1.5、FDD v1.9/v1.3）
- Band_Members.md 变更记录缺 v1.1/v1.2 条目
- GDD §10.1 待定项两项已解决仍挂着（乐队顺序已定稿、存档目录名已定 ResonanceShelter）
- FDD 章节编号重号（§9/§10/§12 与 FDD-09/10/12 撞号；**两个「10.1」**）
- UI 规范**有两个「## 变更记录」**（195行、225行），内容还分裂
- GDD 里 L4（图鉴）在 §3.1 分层表中未定义
- agent.md 写「15 张参考图（F1-F14）」数字自相矛盾
- FDD §8.4 标题仍写"草案，待策划确认"

## P2
`GDD_v1.0_备份_20260906.docx` **已被 git 跟踪**（旧规则会误导）/ 孤儿图 `进入正式管理前的游戏准备界面.png` / `.workbuddy/gdd_extract.txt` / `NVIDIA Corporation` 目录 / 「回到记忆库」vs「记忆日」术语 / 首版真结局窗口「Day15 前」占位待澄清

## 确认无误
版本引用链（核心 6 份）、Sephirot 已清干净、10 节点映射四处一致、存档名 ResonanceShelter 四处一致、L0-L3 层级一致（FDD-01 例外）、异想体概率表 10 档不冲突、四工作↔四维/资源三层/死亡规则/装备规则/结局判定 均一致。

## 质检方法备忘（可复用）
- docx 全文提取要**两级**：段落级（`<w:p>`）用于看结构，**全文级（去掉所有标签）用于查关键字**——只做段落级会漏掉表格单元格。
- 判定口径分三类：**冲突**（不能同时成立）/ **过期**（照旧的做会错）/ **缺口**（完全不存在）；changelog 里的旧版本号不算问题。
- 必查的"跨文档锚点"：版本引用链、层级编号、天数序列、节点-乐队-队长映射、存档目录名、资源/属性/死亡/装备规则、F 编号覆盖、仓库资产是否被文档引用。

---

# 第六项：质检修复（用户"全部一起更改"，2026-09-12）

6 项 P0 + 8 项 P1 + 全部 P2 **已全部修复**。版本：GDD **v1.9** / FDD **v2.6** / UI规范 **v1.4** / agent **v3.5** / ARCHITECTURE **v1.4** / Band_Members **v1.3** / Aberrations·Quests·Narrative_Index·Equipments 各 **v1.1**。

## GDD v1.9 修了什么
1. **§3.1 末尾「部门-节点映射表」**（4 行，节点2 仍写"第 2 支乐队（待定）"）→ 重建为 **11 行完整 10 节点表**（乐队+队长+解锁天+脑叶部门位）。这是 v1.7/v1.8 两次都漏改的地方。
2. **§3.13 一 视觉风格总则 全段重写为原创方向**：删掉"继承脑叶的低饱和度+霓辉强调色""与脑叶一致""等距/直角边框、斜向胶带感、黄色警戒条纹、齿轮/机械骨架""沿用脑叶但替换图标集"。
3. **F 表美术要点去脑叶视觉**（比质检报告列的多）：F1 暗色背景→深色主题底+粉紫渐变；F3 绿色霓虹→粉紫渐变；F6 机械骨架→原创视觉符号；F8 衬衣 icon→工作图标；F9 绿色机械舱壁→原创场景资产；F10 青绿色→粉紫渐变 + 主管手册→**管理者手册**；F13 齿轮徽章/斜向胶带式侧栏→等级徽章/斜切式侧栏。F2 主管→管理者。
4. **F15 行填实**：`进入正式管理前的游戏准备界面.png` → **L2 编成（部署员工）**（原来那行是空占位，而这张图是仓库里唯一的孤儿图）。
5. **§3.1 分层表补 L4 行**（图鉴：独立界面，不属日循环）——原来 L4 只在 F13/F14 出现、分层表没定义。
6. **§3.1 解锁条件句** → "Day 6 解锁第 2 支乐队 Pastel*Palettes"。
7. **§3.14 一 对照表**：脑叶记忆库天数原误填为本作旧解锁天（Day1/6/11/16/21/26/31/36/41）→ 改 **Day5/10/15/20/25/30/35/40/45（每 5 天）**；「主管指令」→「**管理者指令**」（含 §3.14 三 的小标题）。
8. **§10.1 待定项消解 2 项**：存档目录名（已定 ResonanceShelter）、完整版 10 支乐队名单（已定稿）→ 均标 ✅ 已消解。
9. **暂停菜单按钮名统一**：「回到记忆库」→「**回到记忆日**」（GDD 里保留 1 处描述脑叶原界面的表述；FDD/UI规范/agent/ARCHITECTURE 共 5 处同步）。

## 其他文档
- **FDD v2.6**：①修 **FDD-01 §1.2 状态机**（补 TREE_OVERVIEW，入口改「生命树→加载→编成」，两条回滚路径改回当日 L1 生命树）；②修**章节重号**——`§9/§10/§12` 改 `附录 A/B/C`，`10.1 已定稿决策记录`→`B.1`（原来与 FDD-10 的 `10.1` 撞号）；③§8.4 去"草案"；④§8.2 补 F15 引用。
- **UI 规范 v1.4**：①**新增 F15 编成界面（L2 部署员工）**——这是核心流程里唯一没有交互规范的界面；②修 F11 出口笔误（"进入 L1 编成"→"进入 L1 生命树"）；③**合并重复的两个「变更记录」章节**；④流转总览改用 F15 编号。
- **agent v3.5**：§7 补 F15 行；「15 张图（F1-F14）」→ **F1-F15**；全文 GDD 引用统一 v1.9（修掉 §2 速查与交付模板里残留的 **v1.4**——这两处从 v1.4 时代起一直没更新）。
- **Quests v1.1**：主线 `quest_main_03` 更正为 Pastel*Palettes；**补 `quest_band_pasp_01~03` + `trial_pasp`**（首版第 2 支乐队原本完全没有）；MyGO 标注"节点 8 / 完整版，首版不实现"。
- **Band_Members v1.3**：**新增丸山彩记录**（`band_pasp_aya`, `isLeader: true`）——首版第 2 支队长原本缺失；爱音改标节点 8 并补 `isLeader: false`；新增 isLeader 标记进度（已标 2 / 待标 8）；重写"待补充"段。
- **README**：版本表与阶段行统一。

## 修复中**新发现**（已写入质检报告"附二"）
1. 🔴 **Pastel*Palettes 根本没有同位体异想体** —— 这是 P0-5/P0-6 的共同根因。`Aberrations.md` 只有两个异想体（绑香澄 PPP / 绑爱音 MyGO）。按"不发明设定"原则，只补了**结构**并留 `[待定异想体]` 占位，等策划定。
2. ⚠️ **教学异想体叙事错位**：`未奏响的乐章` 定为 Day 1 固定教学，却绑 MyGO 的爱音（节点 8 / Day36）。机制上不冲突（绑定=彩蛋），但玩家第 1 天会遇到第 8 章角色的异想体。
3. GDD 里的脑叶视觉残留比报告列的多（F3/F6/F8/F10/F13 等），已一并清理。

## P2 清理
- `GDD_v1.0_备份_20260906.docx` → `git rm --cached`（**文件保留在磁盘**，只是不进仓库）；`.gitignore` 加 `GDD_v*_备份_*.docx` + `*.bak` + `.workbuddy/tmp/` + `gdd_extract.txt`
- `NVIDIA Corporation` 目录：**未动**（系统/第三方目录），仅报告
- `.workbuddy/gdd_extract.txt` 已删（回收站）

## 仍未决（需策划拍板）
1. **首版真结局窗口「Day15 前」占位** —— 与"首版 15-20 天通关"的关系需澄清
2. PasPale 的同位体异想体（首版阻塞）
3. 教学异想体的绑定指向是否调整

---

# 第七项：★纠正误判 —— 同位体异想体是「可选彩蛋」而非「阻塞项」

## 我犯的错
第六项质检补内容时，我把两件事标成了 **🔴 首版阻塞项 / ⚠️ 需调整**：
- 「PasPale 没有同位体异想体」
- 「教学异想体『未奏响的乐章』绑的是 MyGO 角色（节点 8）」

**用户澄清（3 点）**：
1. 同位体异想体**仅作彩蛋**出现，**并非一定要一一对应**
2. 绑定**不影响遇到的角色**，因为**只是同位体的剧情**
3. 同位体异想体**不应该阻塞游戏流程进度**，应该作为**彩蛋登场**

## 错误根源：GDD §3.2 早有成文规则（v1.5 修订）
> 【重要：异想体与角色的绑定关系 + 抽取池规则（v1.5 修订）】
> 一、绑定性质：彩蛋，不影响流程
> 每个异想体**可**绑定一名乐队少女（其"同位体"），此绑定仅用于叙事彩蛋……
> **绑定绝不产生数值或流程影响**：不因"是否拥有对应少女"而改变成功率、产出、解锁条件，**也不阻塞任何任务与剧情**。
> 因此，即使某异想体先于其对应角色出现（或角色尚未加入），玩法完全正常——**这属于设计预期，不是 Bug**。

→ **规则写得很清楚。我在补内容时违反了它（还新造了"每个异想体需配一名对应角色"这类约束），并把符合规则的设计当成缺陷上报。**

## ★★ 教训（最重要，务必遵守）
**给任何"缺口"定级之前，必须先回查 GDD 是否已有成文规则。「缺失」≠「阻塞」。**
- 不要凭直觉发明约束（"每个异想体必须配角色""异想体缺失会阻塞任务"都是错的）
- 发现"某个表/字段是空的"时，先问：**这东西在 GDD 里是必需的吗？** 而不是直接标 🔴
- 补内容时若发现规则冲突，应报告"与 GDD §X.X 冲突"，而不是自建一套新规则

## 已做的修复
| 文件 | 改动 |
|---|---|
| GDD.docx **v1.10** | §3.2 补两条规则：①「**不要求一一对应**——异想体可以没有对应角色，同一角色的不同侧面也可对应多个异想体」；②「绑定只作用于该异想体自身的情境，**不会引出、解锁或影响玩家遇到对应角色的时机与内容——遇到异想体 ≠ 遇到该角色**。异想体的补齐与绑定关系**不得作为任何里程碑或流程的阻塞项**，一律作为彩蛋登场」 |
| FDD.md **v2.7** | §8.5 增加「异想体池未补全**不影响**生命树与日循环运行，**不得作为 M3 或任何里程碑的前置条件**」 |
| agent.md **v3.6** | §2.2 新增同名规则条目（程序侧明确「不阻塞开发」）；修掉残留的 GDD v1.4 引用 |
| Quests.md **v1.2** | PasPale 的 🔴阻塞 → ⏳待补（非必须）；写明"程序按通用结构实现即可，**不需要等策划确定**" |
| Band_Members.md **v1.4** | 丸山彩的 🔴阻塞 → ⏳待补；填写规则第 3 条补"呼应关系**可选**" |
| Aberrations.md **v1.2** | 「每个异想体需配一名对应角色」→「绑定角色为**可选**彩蛋」；教学异想体那条改为 ℹ️ 设计预期说明 |
| Equipments.md **v1.2** | 丸山彩装备 → 待补、不阻塞 |
| 质检报告 | 新增「附五：本次误判的纠正（AI 自查）」，含教训 |

## 版本全景（2026-09-12 收尾）
**GDD v1.10** / FDD **v2.7** / agent **v3.6** / Band_Members **v1.4** / Aberrations·Quests·Equipments **v1.2** / ARCHITECTURE·UI规范 **v1.4** / Narrative_Index **v1.1** / README 同步

## 仍未决（仅剩 1 项）
**首版真结局窗口「Day15 前」占位** —— 与"首版 15-20 天通关"的关系需澄清（Day15 是硬截止还是占位？玩家 20 天通关会怎样？）

---

# 第八项：首版真结局窗口确认为「占位」（用户 2026-09-12）

用户明确答复：**「占位，等后面的天数做出来便去掉的」**

## GDD v1.11 改动
- **§3.10 表述**：`唯一硬性截止点是真结局窗口——须在 Day46（首版为 Day15 前，占位）前完成全部考验才能进入真结局（见 3.12）。`
  → `唯一硬性截止点是真结局窗口——完整版须在 Day46 前完成全部考验才能进入真结局（见 3.12）。首版该窗口暂以「Day15 前」占位，待完整天数轴做出来后移除该占位（已登记于 10.1 待定项清单）。`
- **§10.1 待定项清单新增一行**（13 → 14 行）：`首版真结局窗口天数 | 占位「Day15 前」；完整版为「Day46 前完成全部考验」。待完整天数轴做出来后移除首版占位 | 首版天数轴定稿后`
- 版本 GDD **v1.10 → v1.11**；FDD / ARCHITECTURE / UI规范 / agent / README / 4 个内容文件的「依据」引用同步升 v1.11（这些文档自身内容未变，**不升自身版本**）

## 注意：GDD 里有两处"唯一硬性截止点是真结局窗口"
- **§2.3**（`…真结局窗口（详见 3.12）`）—— 本来就没提 Day15，**未动**
- **§3.10**（`…真结局窗口——须在 Day46（首版为 Day15 前，占位）…`）—— 本次改的就是这处
- 校验时 `t.find()` 返回的是第一次出现（§2.3），一度以为改错了，实为**误读**。教训：校验多义锚点时要 `finditer` 列全部出现处，不能只看第一个。

## 状态
**质检报告的未决项已全部清零**：6 P0 + 8 P1 + P2 全修，3 项待策划确认事项（PasPale 异想体 / 教学异想体绑定 / 首版真结局窗口）均已澄清。


## 文档更新复核（本会话末，晚风报「文档更新了」）
- 新增提交：9f79d32（流程重排 + 部门顺序）、1c7e166（GDD 升 v1.6 + 架构 v1.1 / 手册 v3.2）。
- 复核结论：**对代码无破坏性影响**，但改变了 M3 的界面顺序与数据需求。
  - 状态机新增 CUTSCENE（每日剧情）作为 TREE_OVERVIEW 的前置；LOADING 出现两次（进 L3 前、进 L2 编成前）。
  - 界面顺序：MainMenu · TreeOverview · Deployment · Department（架构 §2.1 已改）。
  - M3 必须先补 `Assets/Data/Bands.json` + 数据类（FDD-08 §8.4 草案字段：id/band/sephirot/layer/nodeIndex/unlockDay/defaultState；camelCase；容量 4 建议入 GameConfig）。
- 已重抽 GDD 全文到 `.workbuddy/gdd_extract.txt`（585 行）；差异快照 `.workbuddy/docs_diff.txt`。
- 更新项目记忆 MEMORY.md：文档版本对齐、v1.6 流程重排、上层部门顺序、M3 六小步。

## M2 第 3 小步完成（状态机 + 屏幕栈 + UIManager）+ 注释补完 + 编码统一

### M2 第 3 小步成果（用户实机 5 项测试全过）
- `Entities/GameState.cs`（新）：13 个状态，显式 `= GameState.Boot` 防漂移。
- `Systems/GameManager.cs`：单例 + 状态机 + `StateChanged` 事件；**折中版去重定稿**——`ChangeState` 不去重（程序调用，重复=bug 应暴露）、`PushState` 去重（玩家点击，防连点压两层）；日志统一 `[State] {prev} -> {next}`；挂 `[DefaultExecutionOrder(-100)]`。
- `Systems/UIManager.cs`（新）：订阅 `StateChanged`，**订阅放 `Start`**、退订放 `OnDisable`；`[SerializeField] GameObject _settingsRoot`；纯管开关。
- `UI/Screens/MainMenuScreen.cs`：`OnClickSettings` → `PushState(Settings)`，其余按钮留 `TODO(M3)`。
- `UI/Panels/SettingsPanel.cs`（从 Screens 挪入）：拆出 `PrepareDraft()` 放 `OnEnable`（方案 A），删 `Open()` / `Close()`（甲），Slider 用代码 `AddListener` 绑定。
- 场景 `MainMenu.unity`：`_UIManager` 挂**根级**、`_settingsRoot` 指向 SettingsPanel 的 GameObject（初始 inactive）。

### 踩坑实录（写进 MEMORY.md 了）
1. **`OnEnable` 早于别的物体 `Awake`**：实测 `[UIManager] GameManager 尚未初始化` 报在 `GameManager 单例已初始化` **之前** → 订阅必须挪到 `Start`。
2. **`ChangeState`/`PushState` 去重策略**：先选"都不去重"（乙），后因"M3 连点结束当天会一天过两次"改为折中版。

### 注释补完（用户授权"你帮我补吧，太多了"）
5 个文件全部补上类注释 + 方法注释 + `TODO(M3)` 标记；`GameState.cs` 顶部加流程图与 `TODO(M3 补回)`（Loading 中转先简化）。

### ★编码事故（重大，已写入 MEMORY.md 规范）
- **起因**：用 `Write` 工具整文件覆写时，`GameManager.cs` / `UIManager.cs` / `MainMenuScreen.cs` 的中文被落成 **GBK**；既有 5 个 M1 手写文件（DataLoadDemo / Aberrations / BandMember / Character / DataManager）也是 GBK。
- **影响**：C# 编译器按 UTF-8 解读，GBK 注释属侥幸通过 → 注释乱码、git diff 全红、合并冲突。
- **修复**：8 个文件全部 GBK→UTF-8 无 BOM（保留 CRLF），转换前确认无 U+FFFD 坏字符（否则会固化乱码）。**现全项目 13 个 .cs 统一 UTF-8，审计 0 异常。**
- **工具教训**：本环境 `Write` 写中文可能落 GBK → 优先 `Edit` 局部改；整文件重写后**必须**做编码校验。

### 清理的环境事故残留
- 发现并清除 **4 个 0 字节垃圾文件**（`2026-09-12 00:46` Bash 故障时把文档段落当参数误建的碎片）：`上游依据：`、`定位：面向开发者的**技术架构说明**——…`、`文档版本：v1.0（2026-09-07）`、`文档版本：v1.1（2026-09-12）`。位置在 `Desktop\乐团鸣曲\`（工程外一层）。
- **处理方式**：`SHFileOperationW` + `FOF_ALLOWUNDO` 送入回收站（非硬删），执行前 dry-run 打印完整路径。

### Bash 环境故障
- 全天反复出现 `dirname: command not found` / `ls`·`head`·`grep` 缺失，**报错与实际命令无关**（stdout 仍有效）；PowerShell 工具本会话不回显输出。**一律降级到 Python 或 Read/Grep/Glob 工具。**

### 待办
- M2 commit 待准备（用户确认范围后 AI 只 `git add`，push 由用户执行）。
- 加载屏 F2 讨论延后。
- M3 七小步（含 M3-0 补 `Bands.json` + `BandNodeData`）。

## M2 提交完成（4 个 commit，方案 2 拆分）

用户拍板"方案 2（拆多次）+ 字体资源必须进"，AI 只 `git add` + 写 message，**push 留给晚风**。

| # | 哈希 | 类型 | 内容 |
|---|---|---|---|
| 1 | `dd497fb` | chore | 数据类统一 UTF-8 —— Aberrations/BandMember/Character（纯编码，87 增 87 删等量替换） |
| 2 | `92b90c0` | feat(M2) | 状态机 + 屏幕栈 + UIManager —— 16 files, 4142+/87-（含 GameState/UIManager/SettingsData/SettingsSystem/UI 目录/GameManager/MainMenu.unity） |
| 3 | `11c246a` | docs(M1) | 数据层注释补全 —— DataLoadDemo/DataManager（含编码转换） |
| 4 | `1e10313` | chore | TMP + Fondamento 字体 —— 86 files, 83054+ |

**提交前的重要发现**：`DataLoadDemo.cs`/`DataManager.cs` 除编码外**还有 M1 遗留未提交的 XML 注释补全**，所以没并进"纯编码"commit，单独拆为 Commit 3。

**踩坑**：`git show HEAD:<路径>` 必须用 **repo 根**相对路径（`乐团鸣曲/Assets/...`），漏掉工程子目录前缀会取到空内容、误判为"内容有改动"。

**远端**：`origin = github.com/lxh-kiana-elysia/-.git`（GitHub 无凭据，push 需晚风自行认证）。

**剩余未提交**：仅 `.workbuddy/` 下工作记忆 + `gdd_extract.txt`（工具产物，不入代码库）。

## M3-0 启动：`Bands.json` 内容已从 GDD 定位（下午）

晚风跳过 F2 加载屏，直接进 M3-0。问"Bands.json 内容写什么"→ 已查清**内容 90% 由 GDD 定死**。

**数据源：`GDD §3.14 二` 部门-节点映射表 + `GDD` 行 53「附：部门-节点映射表」**（`gdd_extract.txt` 行 587-598 为完整表格）。10 行定稿数据：

| nodeIndex | layer | sephirot | 部门 | unlockDay | 乐队 |
|---|---|---|---|---|---|
| 1 | Asiyah | Malkuth | 控制部 | Day1 | Poppin'Party |
| 2 | Asiyah | Yesod | 情报部 | Day6 | 待定 |
| 3 | Asiyah | Netzach | 安保部 | Day11 | 待定 |
| 4 | Asiyah | Hod | 培训部 | Day16 | 待定 |
| 5 | Briah | Chesed | 福利部 | Day21 | 待定 |
| 6 | Briah | Tiphereth | 中央本部 | Day26 | 待定 |
| 7 | Briah | Geburah | 惩戒部 | Day31 | 待定 |
| 8 | Atziluth | Hokma | 记录部 | Day36 | 待定 |
| 9 | Atziluth | Binah | 研发部 | Day41 | 待定 |
| 10 | Atziluth | Kether | 构筑部 | Day46 | 梦限大 MewType |

**待晚风拍板的 3 点**：①`id` 命名（FDD 示例 `band_ppp`/`band_02`，但与 Band_Members 的 `band_ppp_kasumi` 前缀重叠、易混，建议 `node_01` 等）；②**FDD §8.4 字段清单缺"部门中文名"**，但 F5 弹窗要显示"开放控制部"——要么加 `department` 字段，要么代码硬编码 sephirot→部门名 映射（后者违反数据驱动禁硬编码，推荐加字段）；③`layer`/`sephirot`/`defaultState` 用**枚举还是字符串**。

**另一发现**：`GameConfig.json` 有重复 `"_note"` 键且缩进跑偏（31/33/35 行的 `_note` 已到对象外），M1 遗留，不阻塞 M3-0。

**待补充（不阻塞）**：`Band_Members.md` 提到"完整版 10 支乐队（Poppin'Party → 梦限大 MewType），每支 5 名"，第 3-10 支名单在 GDD §10.1 仍为待定项。
