# 2026-09-10

## 文档再次升版
- FDD → **v2.2**。两条实质变更：①**v2.1** 日循环状态机废止「未达标强制结束（forced=true）」分支，并区分两条回滚路径（重新开始这一天=当日内存态重置不写盘；回到记忆库=读最近记忆日快照）；②**v2.2 新增 FDD-12 相机控制系统**（正交相机、WASD/中键平移、滚轮缩放 0.5x~2.0x、边界钳制、LateUpdate 插值平滑、部门场景 40×12 单位草案、Input System 的 Camera/Pan·Zoom·Reset）。
- GDD 仍为 v1.5（585 行），agent.md v3.1，ARCHITECTURE v1.1。
- 计划同步：M2 不变（补状态枚举须与 ARCHITECTURE §2.2 的 12 状态对齐）；**M3 由 5 小步增至 6 小步**（新增"相机控制"插在场景 HUD 之后、工作闭环之前；结算小步改为"未达标禁用结束"）。

## M2 第 1 小步（F1 主菜单）
- 晚风完成 MainMenuScreen.cs（4 个 public 方法 + Debug.Log）+ Canvas + EventSystem + 4 个按钮。
- 复审：Button 组件 4 个正确；「继续游戏」初版误用"禁用 GameObject"导致被 Vertical Layout Group 排除、只显示 3 个按钮 → 正确做法是**取消 Button 组件的 Interactable**（物体保持激活）。
- 场景结构：`Btn` 是 4 个按钮的**父容器**（挂 Vertical Layout Group 控制垂直排列），非残留，保留（建议改名 BtnGroup 之类便于识别）。

## 注释补充
- 已为 M1 两个代码文件补全中文注释：`Systems/DataManager.cs`（静态类职责、LoadAll 参数/返回值/为何用 TextAsset.text、判空到列表层的原因、三个 Get 的返回与调用方判空约定）、`DataLoadDemo.cs`（临时脚本定位、[SerializeField] 作用、Start 三步流程、实参顺序）。逻辑未改动。
