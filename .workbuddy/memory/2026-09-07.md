# 2026-09-07

## 项目彻底还原（晚风亲自确认）
- 晚风要求清理 AI 生成的全部文件、还原初始模样。经二次确认后执行：
  1. 完整备份到 `桌面\乐团鸣曲_备份_20260907\`（525 文件/19.12MB，含 .git 全历史、agent.md v2.0、GDD.docx 当前版、全部代码与美术）
  2. `git reset --hard 351fb19`（基线：项目文档与 Unity 工程基线提交），历史只剩 deddafc + 351fb19
  3. 从备份恢复 15 张参考图（根目录）与 .workbuddy/（现为未跟踪状态）
  4. 永久删除残留 Assets\Scripts / Data / Resources（回收站被沙箱拦截，晚风确认后 Remove-Item）
- 验证：Assets 与基线树逐文件 diff 完全一致
- 恢复途径：① 备份目录含完整 .git；② 当前仓库 reflog 内仍有旧 HEAD（621d371，约 30 天窗口期），`git reset --hard 621d371` 可找回
- 教训/规则记录：本项目沙箱拦截 Add-Type 与 COM，回收站删除不可用；危险操作只能"robocopy 备份 + Remove-Item"，必须先验证备份再删
- 已完成：晚风新建提交 `051f8c8 回退全部`（15 张参考图 + .workbuddy 记忆）并 force-with-lease 推送成功，远程 main 与本地同步。最终历史：deddafc → 351fb19 → 051f8c8。还原收尾完毕
