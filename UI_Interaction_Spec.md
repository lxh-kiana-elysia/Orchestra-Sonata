# UI_Interaction_Spec.md — 界面交互规范

> 文档版本：v1.0（2026-09-07）
> 依据：`GDD.docx` v1.4（§3.1 UI 分层、§3.13 界面参考图）、`ARCHITECTURE.md` v1.0、`FDD.md` v1.1
> 目的：把 15 张脑叶界面参考图（F1-F14）中的**交互行为**用文字固化，程序按本文件实现"点哪里会发生什么"，无需从图片推测。
>
> ⚠️ 本文档只描述**交互与结构**，**不定义美术风格**（项目美术不基于脑叶，视觉方向见 GDD §5.2 美术边界）。

---

## 0. 全局交互约定（所有界面通用）

| 约定 | 规则 |
|---|---|
| 弹窗层级 | 同一时刻只允许一个模态弹窗；新弹窗覆盖旧弹窗并压入栈 |
| ESC 键 | 关闭当前最上层弹窗；无弹窗时在 L3 打开暂停菜单（F10） |
| 点击空白处 | 关闭非模态面板（如 tooltip、详情面板）；**不关闭**模态弹窗（需显式按钮或 ESC） |
| 右键 | 用于"命令"操作（如选中员工后右键异想体=下达工作/镇压），与脑叶一致 |
| 悬停 | 显示 tooltip（F7 样式），不触发跳转 |
| 禁用态 | 不满足条件的按钮置灰并显示原因 tooltip（如"员工崩溃中，不可派遣"） |
| 返回路径 | 每个界面须有明确返回入口（按钮或 ESC），不允许死路 |
| **数值显示** | **员工四维只显示等级（I / II / III / IV / V / EX），不显示具体数值**（如"演奏水平 III"）；内部仍用 1-120 数值计算，仅在 UI 层转换（GDD §2） |

---

## F1 开始菜单（L0 MainMenu）

- **入口**：游戏启动加载完成后进入
- **元素与行为**
  - 「新游戏」→ 若有存档则二次确认（会覆盖）→ 进入序章 → 到 F11 剧情 → 再进入 L1 编成（Day 1）
  - 「继续游戏」→ 读取最近存档/记忆日 → 进入对应天的 L1 编成；无存档时置灰
  - 「设置」→ 打开 F3 设置面板（覆盖层，ESC 或「返回」关闭）
  - 「退出游戏」→ 二次确认 → 退出应用
- **快捷键**：无
- **返回**：无（根节点）

## F2 加载图（Loading）

- **入口**：启动、读档、进入部门场景时
- **行为**：显示异步加载进度（0-100%）；加载完成自动进入目标界面
- **交互**：允许"按任意键跳过"（仅当加载对象为过场，且不跳过必要数据加载时）
- **超时**：超过 N 秒显示错误提示并提供"重试/返回主菜单"

## F3 系统设置（Settings，覆盖层）

- **入口**：F1「设置」、F10 暂停菜单「系统设置」
- **分组**：游戏（语言/字幕/文本速度）、音频（主音量/音乐/音效）、视频（分辨率/窗口模式/画质）
- **行为**：修改即时预览；「保存」生效并关闭；「取消/ESC」放弃修改并关闭；「恢复默认」需二次确认
- **返回**：关闭后回到上一层（主菜单或暂停菜单）

## F4 生命树部门总览（L2 TreeOverview）

- **入口**：L1 编成点「开始这一天」后进入
- **节点行为**
  - `Open` 节点：点击 → 进入该部门场景（L3）
  - `Locked` 节点：点击 → 显示解锁条件 tooltip（如"Day 6 解锁"），不跳转
  - `Opening` 节点：播放解锁过渡，不可点击
- **警示标识**：当前部门存在出逃/考验/剧情事件时，节点显示警示角标；点击仍可进入
- **顶部信息**：Day N、当日能源配额与已收集
- **返回**：无（当天进行中不可返回 L1；通过暂停菜单 F10 选择"重新开始这一天"回到 L1）

## F5 部门解锁弹窗（模态）

- **入口**：满足条件时（如 Day 6）自动弹出
- **元素**：角色立绘 + 对话文本（Yarn 节点）
- **行为**：「确认」→ 解锁该节点（状态转 Open）+ 触发对应剧情；「取消」→ 不解锁，可后续再触发
- **关闭**：ESC 等同「取消」

## F6 异想体 / 收容单元（L3 内）

- **入口**：在部门场景点击某个收容单元
- **行为**
  - 点击异想体卡片/单元 → 展开详情面板（ID、危险等级、情绪值、工作偏好、镇压方式）
  - 详情面板中「派遣工作」→ 弹出 F8 工作选择弹窗
  - 点击空白处 / ESC → 关闭详情面板（不关闭部门场景）
- **悬停**：显示 F7 tooltip（一句话来历/特质）
- **状态显示**：情绪值以进度条呈现；进入危险窗口（低于 min 或高于 max）时显示警示

## F7 悬停提示（Tooltip）

- **触发**：鼠标悬停于异想体/员工/按钮
- **行为**：延迟 ~0.3s 显示；跟随或固定锚点；不拦截点击（穿透）
- **关闭**：鼠标移出即消失

## F8 工作选择弹窗（模态）

- **入口**：F6 详情面板「派遣工作」，或选中员工后右键异想体
- **元素**：4 个工作按钮（演奏/谈话/创作/比武）+ 已选中员工信息（血/精、预计成功率）
- **行为**
  - 点击某工作 → 对当前选中员工下达该工作命令 → 关闭弹窗 → 员工进入 WORKING（进度条推进）
  - 拖拽员工头像到工作格 = 等价下达命令
  - ESC / 点击「取消」→ 关闭，不下达命令
- **禁用**：员工不满足门槛（GDD §3.8）或血/精不足时，对应按钮置灰并提示原因

## F9 部门场景主界面（L3 Department）

- **入口**：F4 点击 Open 节点
- **固定 HUD（结构参考脑叶，视觉自创）**
  - 左上：当日能源配额 / 已收集 / 危险警示
  - 右上：任务区（当前可推进的剧情或考验任务）
  - 左下：速度控制（暂停 / 1x / 2x）
  - 底部：当日进度条（能源达标进度）
  - 侧栏/下方：编成快捷入口、「结束当天」按钮（仅达标时可用）
- **核心交互（实时操控）**
  - 左键点击员工 → 选中（高亮）
  - 选中后左键点地面/路点 → 移动
  - 选中后左键点异想体 → 打开 F6 详情 → 再到 F8 下达工作
  - 选中后右键异想体 → 直接下达工作/镇压命令（快捷路径）
  - 出逃时：选中员工右键出逃体 → 移动并压制
- **结束当天**：能源达标后可点；未达标时按钮禁用并提示"未达标，无法结束当天"（GDD §3.11）

## F10 暂停菜单（覆盖层）

- **入口**：L3 中按 ESC 或点暂停按钮
- **行为（游戏循环冻结）**
  - 「重新开始这一天」→ 用当日内存态重置，回到 L1 编成（不写盘）
  - 「回到记忆库」→ 读取最近记忆日快照，回到该日 L1 编成
  - 「系统设置」→ 打开 F3
  - 「返回标题画面」→ 二次确认（提示未保存进度将回退到最近记忆日）→ 回 F1
  - 「离开公司」→ 二次确认 → 退出应用
  - 「继续」/ ESC → 关闭并继续游戏

## F11 每日剧情（Cutscene）

- **入口**：每日开始（或关键节点）触发
- **元素**：角色立绘 + 对话框 + 左上 `DAY X` 标签
- **行为**：点击/空格推进下一句；「跳过」按钮（剧本已读过时可用）；播放完成后进入 L1 编成
- **期间**：游戏循环暂停；不响应部门场景操作

## F12 剧情分支选择（模态）

- **入口**：Yarn 节点触发选项
- **元素**：2-3 个选项按钮（最多 3）
- **行为**：点击选项 → 按选项 ID 跳转到对应节点；未选择前不可推进
- **关闭**：不可 ESC 跳过（必须做出选择）

## F13 异想体图鉴（Codex）

- **入口**：主菜单或暂停菜单「图鉴」
- **布局**：左侧列表（已解锁异想体）+ 右侧详情（基础信息 / 工作偏好 / 管理须知 / 敏感信息）
- **行为**：点击列表项切换详情；信息按观察等级（异想体点数）逐步解锁，未解锁部分显示占位"???"
- **返回**：关闭回上一层

## F14 图鉴·故事页

- **入口**：F13 详情中切到「故事」标签
- **行为**：展示该异想体背景故事文本（可滚动）；与 F13 共用左侧列表与返回逻辑

---

## 界面流转总览

```
F1 开始菜单
 ├─ 新游戏 → F11 剧情(Day1) → L1 编成
 ├─ 继续   → L1 编成（对应天）
 ├─ 设置   → F3（返回 F1）
 └─ 图鉴   → F13 → F14
L1 编成 → 「开始这一天」→ F4 生命树总览
F4 → F5（解锁弹窗）→ F4
F4 → 点击 Open 节点 → F9 部门场景
F9 → F6 详情 → F8 工作弹窗 → 回到 F9（员工工作中）
F9 → ESC → F10 暂停菜单（可回 L1 / 记忆库 / F3 / F1 / 退出）
F9 → 达标「结束当天」→ 次日 → L1 编成
任意 → F11/F12 剧情（触发时）
```

---

## 变更记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-09-07 | v1.0 | 初版：F1-F14 界面的文字化交互规范（元素/行为/快捷键/返回路径）+ 全局交互约定 + 界面流转总览。只定义交互与结构，不含美术风格 |

---

## 补充：员工属性与训练界面交互

### 员工信息面板（编成 / 部门场景内查看员工）
- **显示内容**：姓名、所属乐队（普通员工显示"普通员工"）、**四维等级**（如"演奏 III / 共感 II"）、状态（正常 / 崩溃中 N 天）、装备槽位。
- **不显示**：四维的 1-120 具体数值（GDD §2 显示规则）。
- **悬停**：等级上悬停显示该等级的数值区间 tooltip（如"II 级：21-40"），作为辅助信息，不破坏"主界面只看等级"的原则。

### 训练（编成界面内）
- **入口**：编成界面选中员工 → 「训练」按钮。
- **行为**：
  - 点击某属性的训练按钮 → 显示确认（"消耗 X 星石，将该属性提升至 [等级] 级"）。
  - 确认 → 扣星石 → 该属性**提升一个等级**，数值设为新等级下限（UI 只反映为等级变化）。
  - 费用按**目标等级**递增：I→II 10 / II→III 30 / III→IV 100 / IV→V 300 / V→EX 800 星石（草案）。
- **禁用态**：
  - 星石不足 → 按钮置灰 + tooltip"星石不足"。
  - 已达 **EX** → 按钮置灰 + tooltip"已达最高等级（EX）"。
  - 员工崩溃/死亡 → 不可训练。
- **反馈**：训练成功后等级标记变化（如 II→III），播放短反馈；不显示数值跳动。

---

## 变更记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-09-07 | v1.0 | 初版：F1-F14 的文字化交互规范（元素/行为/快捷键/返回路径）+ 全局交互约定 + 界面流转总览。只定义交互与结构，不含美术风格 |
| 2026-09-08 | v1.1 | 新增「数值显示」全局约定（四维只显示等级）与「员工属性与训练界面交互」章节（等级显示、悬停区间提示、训练确认/禁用态/反馈） |
