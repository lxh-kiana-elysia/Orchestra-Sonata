# FDD.md — 乐团鸣曲 详细功能设计文档

> 文档版本：v1.3（2026-09-08）
> 上游依据：`GDD.docx` v1.4 · `ARCHITECTURE.md` v1.0 · `agent.md` v3.1
> 定位：把 GDD 中的规则**拆解为可实现的技术规格**——状态机、流程图、数据结构、配置项、边界条件。
>
> **与 GDD 的分工（重要，避免重复维护）**：
> - **GDD = 规则的唯一权威**：玩法描述、数值、公式**只在 GDD 定稿**（如判定公式见 GDD §3.4）。
> - **FDD = 实现规格**：不重新定义规则，只描述"怎么实现"——函数签名、状态机、边界条件、配置项来源、验收点。
> - 本文档中出现的公式/数值一律标注 GDD 出处（如 `[GDD §3.4]`）；**若与 GDD 冲突，以 GDD 为准并回报修正本文档**。
> - 维护原则：GDD 改规则 → 同步 FDD 的"实现"部分；FDD 只调实现方式时不改 GDD。
>
> ⚠️ 数值说明：本文所有数值**均来自 GDD v1.4**。GDD 标注为"占位/草案/待定"的，此处一律标 `TODO(设计待确认)` 并注明 GDD 出处，**不擅自填值**。
> ⚠️ 美术说明：本文**不涉及美术风格**（项目美术不基于脑叶参考图），仅定义 UI 的**结构与交互**。

---

## 0. 通用约定

- **状态机图例**：`状态 A --事件/条件--> 状态 B`
- **数值来源标注**：`[GDD §X.X]` = 已定稿；`[GDD §X.X 占位]` = 草案待确认
- **接口签名**：与 `ARCHITECTURE.md §4` 保持一致
- **纯函数**：标记 🔬 的逻辑必须实现为不依赖 MonoBehaviour 的纯函数（可单测）

---

## FDD-01 日循环与能源系统

### 1.1 职责
管理"一天"的开始、进行、结算与结束；维护当日能源（情感粒子）配额与收集进度。

### 1.2 状态机
```
DAY_START（编成 DEPLOY）
  --玩家点「开始这一天」--> DAY_RUNNING（部门场景实时）
  --能源达标 & 玩家点「结束当天」--> DAY_SETTLE（结算）
  --未达标 & 玩家强制结束--> DAY_SETTLE（标记 forced=true）
DAY_SETTLE --结算完成 & 天数<50--> DAY_START（天数+1）
DAY_SETTLE --天数=50--> ENDING（真/假结局）
任意状态 --暂停--> PAUSE --回到记忆库--> DAY_START（回滚到记忆日）
```

### 1.3 关键数据
| 字段 | 类型 | 说明 |
|---|---|---|
| `currentDay` | int | 当前天数（1-50） |
| `todayQuota` | int | 当日能源配额 |
| `todayEnergy` | int | 当日已收集情感粒子 |
| `starStones` | int | 星石（持久） |
| `isDayEnded` | bool | 当日是否已结算 |

### 1.4 关键逻辑 🔬
```csharp
// 当日配额（考验日上调 30%）
int CalcQuota(int day, bool isTrialDay) {
    float q = config.quotaBase * Mathf.Pow(config.quotaGrowthPer10Days, day / 10f);
    if (isTrialDay) q *= 1.3f;          // 考验日上调 30%【已定稿】
    return (int)q;
}

// 是否达标
bool IsQuotaMet(int todayEnergy, int quota) => todayEnergy >= quota;

// 日终结算：结余转星石
int ConvertSurplusToStarStone(int surplus) => surplus / 10;   // [GDD §3.7 占位 10:1]
```

### 1.5 边界与异常
- **未达标不得结束当天**【已定稿，GDD §3.11】：玩家须继续工作直至达标；若局面无法挽回（员工大量崩溃等），只能「重新开始这一天」回到当日编成重打。**不提供"提前结算/强行结束"选项**。
- 配额必须 > 0；`day > 50` 不允许进入 DAY_RUNNING。
- 记忆日（Day 5/10/…/45）在进入 DAY_START 时**自动锚定快照**（见 FDD-06）。

### 1.6 验收点
- [ ] 能从 Day1 连续跑到 Day50 不崩
- [ ] 配额随天数正确递增
- [ ] 达标/未达标两条结算路径均可走通
- [ ] 记忆日自动锚定

---

## FDD-02 工作系统（核心）

### 2.1 职责
处理员工对异想体执行 4 种工作的完整链路：校验 → 移动 → 进度 → 判定 → 结算 → 成长。

### 2.2 状态机（单个员工）
```
IDLE（空闲/在场景中）
  --玩家选中并下达移动--> MOVING
  --到达收容单元--> READY
  --玩家选择工作类型--> WORKING（进度条推进）
  --进度完成--> RESOLVING（判定）
  --判定完成--> IDLE（回到空闲）
  --玩家中断/员工崩溃/异想体出逃--> IDLE（中断无进度收益）
```

### 2.3 判定规则
> 规则权威见 **GDD §3.4**，此处仅为实现映射。

| 工作类型 | 判定属性 | 出处 |
|---|---|---|
| 演奏 | 演奏水平 `performance` | GDD §3.4 |
| 谈话 | 精神 `sp` | GDD §3.4 |
| 创作 | 共感 `empathy` | GDD §3.4 |
| 比武 | 血量 `hp` | GDD §3.4 |

实现时通过 `WorkRules.GetJudgeStat()` 与 `WorkRules.CalcSuccessRate()` 两个纯函数完成，公式与系数以 GDD 为准。

### 2.4 关键逻辑 🔬
```csharp
// 判定属性
int GetJudgeStat(Character c, WorkType t) => t switch {
    WorkType.Performance => c.performance,
    WorkType.Talk        => c.sp,
    WorkType.Creation    => c.empathy,
    WorkType.Combat      => c.hp,
};

// 成功率
float CalcSuccessRate(float baseRate, int statValue, float mod)
    => Mathf.Min(1f, baseRate * (statValue / 120f) * mod);

// 收益递减（同日对同一异想体第 n 次工作）【已定稿】
float Diminishing(int n) => 1f / (1f + 0.2f * Mathf.Max(0, n - 1));
// 效果：第1次100% → 第2次83% → 第3次71% → 第5次56% → 第10次36%

// 成长值（成功/失败都成长）【已定稿系数】
int CalcGrowth(int consumed, bool success)
    => Mathf.RoundToInt(consumed * (success ? 0.10f : 0.03f));
// 例：消耗10点 → 成功 +1、失败 +0.3（取整后为 0，建议失败保底 +1 待确认）
```

### 2.5 产出规则
| 结果 | 情感粒子 | 异想体点数 | 消耗 | 成长 |
|---|---|---|---|---|
| 成功 | 全额 | 全额 | 按工作类型扣血/精 | 较大 |
| 失败 | 少量（约 25% [占位]） | 少量 | 按工作类型扣血/精 | 较小 |

### 2.6 边界与异常
- 员工 hp 或 sp 为 0 → 不可派遣（崩溃状态）。
- 门槛校验：异想体危险等级要求判定属性达指定档（GDD §3.8）。
- 中断工作：不发放任何进度收益，但已扣消耗不返还。
- 同一员工同日可多次工作（**不禁止**），仅产出递减。

### 2.7 验收点
- [ ] 4 种工作均能正确取到对应判定属性
- [ ] 成功率公式与 GDD 一致（单测覆盖边界：0/120/超上限）
- [ ] 递减生效且不影响派遣本身
- [ ] 中断无收益、消耗不返还

---

## FDD-03 异想体与情绪值系统（含出逃 / 突破）

### 3.1 职责
维护异想体情绪值，驱动出逃（归零）与突破事件（爆满）两条分支。

### 3.2 状态机
```
CONTAINED（收容中）
  --情绪值 <= 0--> ESCAPED（出逃，具象化活动）
  --情绪值 >= 100--> BREAKTHROUGH（突破事件·正向）
  --工作影响--> CONTAINED（情绪值变化）
ESCAPED --玩家派员工镇压/安抚成功--> CONTAINED（+异想体点数奖励）
ESCAPED --长时间未处理--> 持续负面影响（能源流失/精神下降/连锁阈值）
BREAKTHROUGH --播完剧情--> CONTAINED（解锁个人线下一章）
```

### 3.3 关键数据
| 字段 | 来源 |
|---|---|
| `mood` 0-100 | GDD §3.2 |
| `moodDangerMin/Max` | GDD §3.2（危险窗口） |
| `escapeTrigger` / `escapeBehavior` / `suppressMethod` | 异想体数据表 |
| `points` / `storyUnlockThresholds` | GDD §3.2 |

### 3.4 规则要点
- **出逃（归零）**：离开收容单元、按 `escapeBehavior` 持续影响设施；镇压成功 → 回归 + 点数奖励；长期未处理 → 能源流失/精神下降/连锁触发其他异想体阈值变化。**可恢复，无永久损失**。
- **突破（爆满 100）**：**正向事件** —— 揭示深层故事、解锁对应角色个人线下一章、额外奖励。与出逃形成正负两极。
- 每日候选池：从池中抽 `3 + 天数/3`（向下取整，上限 5）个候选，选 1 个收容，**未选回池** [GDD §1.3(1)]。

### 3.5 验收点
- [ ] 情绪值归零触发出逃、达 100 触发突破
- [ ] 镇压成功回归并奖励点数
- [ ] 候选池抽取数量符合公式，未选回池
- [ ] 无任何"永久锁死"逻辑

---

## FDD-04 员工系统（招录 / 捏人 / 训练 / 崩溃）

### 4.1 状态机（员工生命周期）
```
CANDIDATE（候选，未录用） --玩家调整外观并确认录用--> ACTIVE
ACTIVE --被编成--> DEPLOYED
ACTIVE/DEPLOYED --hp或sp归零--> COLLAPSED（崩溃退场 N 天）
COLLAPSED --恢复计时结束 或 回复室加速--> ACTIVE
```

### 4.2 招录与捏人
- 消耗星石生成候选：姓名 / 发型 / 发色 / 眼睛样式 / 表情 / 初始属性倾向**随机** [GDD §3.3]
- 录用前可自由调整外观与姓名（普通员工）；**乐队少女为原著形象，不可改外观**
- 招录数量受收容室设施等级限制 [GDD §3.6]

### 4.3 训练（星石养成）🔬
```csharp
// 按当前属性档位计费，每次 +N 点
int TrainingCost(int statValue) => statValue switch {
    < 20  => 10,  < 40  => 30,  < 60 => 100,
    < 80  => 300, < 100 => 800, _    => 2000,
};  // TODO(设计待确认): 关联 GDD §3.6（占位）
```
- 普通员工与乐队少女**均可训练，且不受章节/个人线约束**（随时可训，只受星石限制）【已定稿 2026-09-07】
- 与"工作成长"并行：训练快但花星石，工作慢但免费。

### 4.4 崩溃与恢复
- hp 或 sp 归零 → 崩溃退场 **3 天** [草案，GDD §3.11]，`recoveryRate` 决定恢复速度，回复室设施可加速（消耗星石）。
- 连续多名核心角色崩溃 → 触发"设施危机"剧情事件（**非 Game Over**）。

### 4.5 验收点
- [ ] 招录扣星石、槽位上限生效
- [ ] 捏人参数写入 `appearance`；乐队少女外观不可改
- [ ] 训练按档位计费且属性 +N，不超上限 120
- [ ] 崩溃 3 天后自动恢复，回复室可加速

---

## FDD-05 考验系统

### 5.1 状态机
```
LOCKED（未解锁）
  --乐队完成对应任务--> UNLOCKED
UNLOCKED --玩家发起--> IN_PROGRESS（全员参与大型工作/演出）
IN_PROGRESS --达成目标--> COMPLETED
IN_PROGRESS --失败--> FAILED（可重试，无永久损失）
```

### 5.2 规则
- 每支乐队一次考验；**与下一支乐队解锁解耦**（解锁不要求通过上一支考验）[GDD §3.10]
- 考验与任务**无倒计时**；唯一硬性截止点是真结局窗口
- 全员参与；失败可重试

### 5.3 结局判定 🔬
```csharp
// Day46 前完成全部考验 → 真结局
bool IsTrueEndingUnlocked(int currentDay, int completedTrials, int totalTrials)
    => currentDay <= 46 && completedTrials >= totalTrials;
    // "归途之音充满" == 全部考验完成（非独立资源条件）[GDD §2.3 / §3.11]
```
- Day50：真 → 真结局演出；否则 → 假结局（**可回档补齐后重走**）

### 5.4 验收点
- [ ] 考验解锁不阻塞乐队解锁
- [ ] 失败可重试且无永久损失
- [ ] Day46 判定与 Day50 结局分支正确

---

## FDD-06 记忆日与存档系统

### 6.1 职责
存档读写、记忆日快照锚定、回滚到最近记忆日。

### 6.2 流程
```
记忆日（Day 5/10/15/20/25/30/35/40/45）进入编成界面
  → SaveSystem.AnchorMemoryDay(day)  // 另存 snapshot_dayN.json
玩家在暂停菜单选「回到记忆库」
  → RollbackToMemoryDay() → 恢复最近快照 → 回到该日编成
```

### 6.3 存档内容（GDD §4.4）
`当前天数 / 情感粒子·星石·异想体点数 / 所有角色四维与状态 / 异想体状态（点数与出逃情报）/ 已解锁剧情进度 / 今日编成`

### 6.4 规则
- 路径：`%USERPROFILE%/AppData/LocalLow/ResonanceShelter/Saves/`【已定稿】
- **仅记忆日写盘锚定**（Day 5/10/…/45）；**不做每日快照**【已定稿 2026-09-07】。
- "重新开始这一天"使用**进入日循环时的内存态**重置（不写盘）：在玩家点「开始这一天」时于内存中保留当日初始状态，重开当天即用其恢复；因此退出游戏后当天进度不保留，重进回到最近记忆日（最多损失约 4 天）。
- 回滚保留：图鉴、已解锁乐队、异想体点数、星石、任务进度
- 回滚**移除**：快照之后新招录的员工、快照之后的属性成长 [GDD §3.12 记忆日规则]

### 6.5 验收点
- [ ] 记忆日自动锚定（9 个记忆日全部覆盖）
- [ ] 回滚后数据符合"保留/移除"规则
- [ ] 存档含版本号字段，便于迁移

---

## FDD-07 剧情触发系统

### 7.1 剧情分级（GDD §3.5）
| 层级 | 触发条件 | 表现 |
|---|---|---|
| 日常反馈 | 每次工作完成 | 短反馈（一行台词/表情差分/小演出 ≤5 秒） |
| 节点剧情 | ①同一异想体工作累计第 1/3/5/10 次 ②异想体点数跨档 ③特定角色组合对特定异想体工作 ④章节/考验节点 | 完整演出（立绘+对话+场景） |
| 组合触发 | 特定角色组合 + 特定异想体 | 专属隐藏互动剧情 |

### 7.2 实现要点
- `DialogueTrigger`（自研状态层）负责判定"是否可触发"；`DialogueManager`（Yarn）负责播放
- 剧情**不修改数值**（数值由 System 结算后通过事件传递）

### 7.3 验收点
- [ ] 日常反馈不打断管理节奏（≤5 秒）
- [ ] 四类节点触发条件均可命中
- [ ] 剧情播放期间游戏状态正确（暂停/可跳过）

---

## FDD-08 编成与部门节点系统

### 8.1 编成（L1）
- 从"可用员工"（普通员工 + 已解锁乐队少女）中选当日上场阵容
- 显示当日能源配额、异想体候选、剧情预告
- 右上角「开始这一天」→ 进入 L2

### 8.2 部门节点（L2）
- 节点 = 乐队；状态 `LOCKED / OPENING / OPEN`
- 点击 OPEN 节点 → 进入该部门场景（L3）
- 映射：Poppin'Party = 节点 1（≈控制部），Day6 解锁节点 2，依 GDD §3.14 的 10×3 层级表

### 8.3 验收点
- [ ] 编成可保存/读取预设
- [ ] 节点状态正确，LOCKED 显示解锁条件、不可进入
- [ ] 部门场景能正确加载该部门的异想体与员工

---

---

## FDD-09 员工移动与寻路（技术决策，已定）

### 9.1 决策结论
采用 **路点（Waypoint）方案**：部门场景预设路点图（走廊节点 + 各收容单元门口节点），员工沿路点连成的路径移动。

### 9.2 决策理由
1. **贴合脑叶原版表现**：脑叶部门场景本质是"走廊 + 房间"的线性/分支结构，员工沿走廊移动到收容单元门口，**并不做复杂寻路**——路点方案在观感上与之最接近。
2. **性能**：无运行时寻路计算（不像 A*/NavMesh 需每帧或每次下令时计算路径），2D 场景开销极低，稳守 60fps。
3. **实现与调试成本**：单人开发首选；路点可在编辑器中可视化摆放，路径走向可控、易调，不会出现"员工卡墙/走位诡异"。

### 9.3 实现方式（建议）
```
// 数据结构：路点图
class WaypointGraph {
    List<Waypoint> nodes;      // 含 id、位置、邻接节点 id 列表
    // 查询：给定起点/终点，返回路点序列（预计算或简单 BFS，规模小无需 A*）
    List<Vector2> FindPath(int fromNodeId, int toNodeId);
}
```
- 员工实际移动：沿 `FindPath` 返回的折线依次插值移动（可用 DoTween 或自写 Lerp）。
- 收容单元门口为终点路点，到达后进入 READY 状态（见 FDD-02）。

### 9.4 后续可扩展
若将来部门场景变为开放地形（非走廊结构），可升级为网格 A*；路点接口（`FindPath`）保持不变即可平滑替换。

### 9.5 验收点
- [ ] 员工能沿走廊正确走到目标收容单元门口
- [ ] 无穿墙/抖动/卡死
- [ ] 多个员工同时移动不互相阻塞（或按规则排队）

---

## 9. 数值配置汇总（占位项一览）

| 配置项 | 值 | 状态 |
|---|---|---|
| `quotaBase` | 300 | 占位 |
| `quotaGrowthPer10Days` | 2.0（每 10 天） | 占位 |
| 考验日配额系数 | **×1.3** | ✅ **已定稿**（2026-09-07） |
| 未达标处理 | **无法结束当天，只能重开当天** | ✅ **已定稿**（GDD §3.11） |
| 失败产出比例 | 25% | 占位 |
| 收益递减 | **`1/(1+0.2×(n-1))`** | ✅ **已定稿** |
| 成长系数 | **成功 `消耗×0.10` / 失败 `消耗×0.03`** | ✅ **已定稿** |
| 崩溃天数 | 3 天 | 草案 |
| 星石转化 | 10:1 | 占位 |
| 训练费用档位 | 10/30/100/300/800/2000，+2 点 | 草案 |
| 训练约束 | **无约束（普通员工与乐队少女随时可训）** | ✅ **已定稿** |
| 招录费用 | 50 星石，槽位 2/4/6/8 | 草案 |
| 寻路方案 | **路点 Waypoint** | ✅ **已定稿**（FDD-09） |
| 存档目录名 | **ResonanceShelter** | ✅ **已定稿** |

> 所有占位值写入 `GameConfig.json`，注释 `// TODO(设计待确认): 关联 GDD §X.X`（agent.md §4.2）。

---

## 10. 待确认清单

> 2026-09-07 已回答 7 项中的 6 项（见 §9 标记 ✅），剩余如下。

| # | 问题 | 关联 | 状态 |
|---|---|---|---|
| 1 | 失败成长是否设**保底 +1**（避免小消耗时取整为 0） | FDD-02 | 待确认 |
| 2 | `quotaBase=300` 与 `每10天×2.0` 的最终曲线是否合理 | GDD §3.12 | 待实测调参 |
| 3 | 崩溃天数 3 天 与 `recoveryRate=5.0` 的匹配 | GDD §3.11 | 待实测调参 |
| 4 | 招录费用 50 星石 / 槽位 2-4-6-8 是否合适 | GDD §3.3 | 待实测调参 |

### 10.1 已定稿决策记录（2026-09-07）

| 原问题 | 决策 |
|---|---|
| 单日未达标的处理 | **无法结束当天**，须继续达标或重开当天（不提供提前结算） |
| 考验日配额 | **上调 30%** |
| 收益递减曲线 | **`1/(1+0.2×(n-1))`** |
| 成长系数 | **成功 `消耗×0.10` / 失败 `消耗×0.03`** |
| 乐队少女训练约束 | **无约束，随时可训** |
| 寻路方案 | **路点 Waypoint**（脑叶亦为走廊结构，非复杂寻路） |
| 存档目录名 | **ResonanceShelter** |

---

---

## FDD-10 异想体数据规格（含"未奏响的乐章"定稿）

### 10.1 workModifiers 语义（**乘数**，基准 1.0）
- 依据 GDD §3.4：`最终成功率 = min(1, 基础成功率 × (判定属性值/120) × 异想体修正)`
- `workModifiers` 一律以**乘数**存储，基准 **1.0**（1.0=无修正，>1 提升，<1 降低）。
- **禁止负值**（负乘数会产生负成功率）；极低下限取 `0.1`。
- GDD 样例中写作 `+0.2 / 0 / -0.1 / -0.3` 的加值写法，实际存储为 `1.2 / 1.0 / 0.9 / 0.7`。

### 10.2 新增字段：specialEffects（因"未奏响的乐章"引入）
承载"工作结果为优/满产出"等异想体专属效果，由 `WorkSystem` 在结算后读取执行。

```jsonc
"specialEffects": [
  { "trigger": "workResultExcellent", "effect": "restoreSp", "target": "worker",     "value": 8 },
  { "trigger": "fullOutput",          "effect": "restoreSp", "target": "department", "value": 5 }
]
```
- `trigger`：`workResultExcellent`（结果为优） / `fullOutput`（本次满产出） / 后续可扩展
- `effect`：`restoreSp`（恢复精神） / `restoreHp` / 其他（按需扩展）
- `target`：`worker`（执行工作的员工） / `department`（该部门所有员工）
- `value`：**占位值，待实测调参**（关联 GDD §3.4）

### 10.3 "未奏响的乐章" 完整数据（供填入 Aberrations.json）
> 定稿 2026-09-08；由程序 AI/开发者填入 `Assets/Data/Aberrations.json`，本文档只给规格、不直接创建工程文件。

```jsonc
{
  "id": "aber_score_01",
  "name": "未奏响的乐章",
  "type": "回忆型",
  "riskLevel": 1,                       // ZAYIN：完全无害，无派遣门槛
  "mood": 50,
  "moodDangerMin": 0,                   // 无危险窗口（ZAYIN 不触发出逃）
  "moodDangerMax": 100,
  "linkedCharacterId": "anon_chihaya",  // 千早爱音（MyGO!!!!!）
  "workModifiers": [                    // 乘数，基准 1.0
    { "workType": "Performance", "modifier": 1.3 },
    { "workType": "Creation",    "modifier": 1.1 },
    { "workType": "Talk",        "modifier": 1.0 },
    { "workType": "Combat",      "modifier": 0.5 }
  ],
  "pointsPerSuccess": 10,               // 情感粒子（成功基准值）；失败约 25%
  "storyUnlockThresholds": [0, 20, 50],
  "escapeTrigger": null,                // ZAYIN 无出逃
  "escapeBehavior": null,
  "suppressMethod": null,
  "specialEffects": [
    { "trigger": "workResultExcellent", "effect": "restoreSp", "target": "worker",     "value": 8 },
    { "trigger": "fullOutput",          "effect": "restoreSp", "target": "department", "value": 5 }
  ],
  "background": "一段从未被奏响的旋律，凝固成了等待知音的空谱。它没有过去，只在等待一个能赋予它意义的人。"
}
```

### 10.4 验收点
- [ ] 该异想体可被收容、可对它执行 4 种工作
- [ ] 演奏成功率最高、比武极低（乘数生效，无负值）
- [ ] 工作结果为"优"时，执行员工精神值恢复
- [ ] 满产出时，该部门所有员工精神值恢复
- [ ] ZAYIN 无出逃：情绪值归零/爆满均不触发出逃事件

---

## 11. 测试场景清单（Phase 0-2 需创建）

> 依据 agent.md §6 测试规范：纯逻辑用单元测试（EditMode），系统流程用最小测试场景。

| 测试场景 | 覆盖模块 | 测试内容 | 阶段 |
|---|---|---|---|
| `Test_WorkSystem.unity` | FDD-02 | 4 种工作的判定属性映射；成功率边界（0 / 120 / 超上限）；成功与失败两条结算路径；收益递减生效 | Phase 1-2 |
| `Test_DayLoop.unity` | FDD-01 | 日循环状态迁移（START→RUNNING→SETTLE→次日）；配额随天数递增；未达标无法结束；考验日 ×1.3 | Phase 1-2 |
| `Test_SaveSystem.unity` | FDD-06 | 存档写入/读取；记忆日（Day 5/10/…）自动锚定；回滚后"保留/移除"规则正确 | Phase 1 |
| `Test_AberrationMood.unity` | FDD-03 | 情绪值归零→出逃；情绪值爆满→突破事件（正向）；镇压成功回归+奖励点数；候选池抽取与回池 | Phase 2 |
| `Test_RecruitTrain.unity` | FDD-04 | 招录扣星石与槽位上限；捏人参数写入；训练按档计费与加点不超上限 | Phase 1 |
| `Test_TrialSystem.unity` | FDD-05 | 考验解锁不阻塞乐队解锁；失败可重试；Day46 真结局判定与 Day50 结局分支 | Phase 2 |

**单元测试（EditMode，纯函数）**

| 测试目标 | 用例 |
|---|---|
| `WorkRules.CalcSuccessRate` | 边界：statValue=0 → 0；statValue=120 且 mod=1 → baseRate；上限截断 min(1, …) |
| `WorkRules.GetJudgeStat` | 4 种工作分别取对应属性 |
| `WorkRules.Diminishing` | n=1→1.0；n=2→≈0.83；n=10→≈0.36 |
| `WorkRules.CalcGrowth` | 成功/失败系数；消耗为 0 时返回 0 |
| `Quota` 计算 | 普通日与考验日（×1.3）对比 |

---

## 变更记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-09-07 | v1.0 | 初版：8 个模块 FDD（日循环/工作/异想体/员工/考验/存档/剧情/编成节点），含状态机、关键逻辑、验收点；数值配置汇总与 7 项待确认清单。**不含美术风格**（项目美术不基于脑叶） |
| 2026-09-08 | v1.3 | 新增 FDD-10 异想体数据规格：workModifiers 明确为**乘数**（基准1.0，禁负值）；新增 specialEffects 字段（工作结果为优/满产出触发效果）；收录「未奏响的乐章」完整 JSON 规格（aber_score_01，ZAYIN/回忆型/对应千早爱音/产出情感粒子）与验收点
| 2026-09-07 | v1.2 | 明确与 GDD 的分工（GDD=规则权威，FDD=实现规格，去除重复定义）；FDD-02 判定规则改为引用 GDD；新增 §11 测试场景清单（6 个场景 + 5 组单测）；存档目录名统一 ResonanceShelter（同步 GDD/ARCHITECTURE/agent）
| 2026-09-07 | v1.1 | 落实 7 项待确认决策（用户拍板）：①未达标**无法结束当天**须达标或重开（GDD §3.11 同步修订）；②考验日配额 **×1.3**；③递减曲线 `1/(1+0.2(n-1))`；④成长系数 成功 `×0.10` / 失败 `×0.03`；⑤乐队少女训练**无约束**；⑥寻路采用**路点 Waypoint**（新增 FDD-09，含决策理由与实现方式）；⑦存档目录名 **ResonanceShelter**。§9 标记已定稿项，§10 保留 4 项待实测调参 |
