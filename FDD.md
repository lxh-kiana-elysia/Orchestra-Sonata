# FDD.md — 乐团鸣曲 详细功能设计文档

> 文档版本：v2.7（2026-09-12）
> 上游依据：`GDD.docx` v1.11 · `ARCHITECTURE.md` v1.3 · `agent.md` v3.4
> 定位：把 GDD 中的规则**拆解为可实现的技术规格**——状态机、流程图、数据结构、配置项、边界条件。
>
> **与 GDD 的分工（重要，避免重复维护）**：
> - **GDD = 规则的唯一权威**：玩法描述、数值、公式**只在 GDD 定稿**（如判定公式见 GDD §3.4）。
> - **FDD = 实现规格**：不重新定义规则，只描述"怎么实现"——函数签名、状态机、边界条件、配置项来源、验收点。
> - 本文档中出现的公式/数值一律标注 GDD 出处（如 `[GDD §3.4]`）；**若与 GDD 冲突，以 GDD 为准并回报修正本文档**。
> - 维护原则：GDD 改规则 → 同步 FDD 的"实现"部分；FDD 只调实现方式时不改 GDD。
>
> ⚠️ 数值说明：本文所有数值**均来自 GDD v1.11**。GDD 标注为"占位/草案/待定"的，此处一律标 `TODO(设计待确认)` 并注明 GDD 出处，**不擅自填值**。
> ⚠️ 美术说明：本文**不涉及美术风格**（项目美术不基于脑叶参考图），仅定义 UI 的**结构与交互**。

---

## 0. 通用约定

- **状态机图例**：`状态 A --事件/条件--> 状态 B`
- **数值来源标注**：`[GDD §X.X]` = 已定稿；`[GDD §X.X 占位]` = 草案待确认
- **接口签名**：与 `ARCHITECTURE.md §4` 保持一致
- **纯函数**：标记 🔬 的逻辑必须实现为不依赖 MonoBehaviour 的纯函数（可单测）

---

## FDD-01 日循环与能源系统

### 1.1 职责
管理"一天"的开始、进行、结算与结束；维护当日能源（情感粒子）配额与收集进度。

### 1.2 状态机
```
CUTSCENE（每日剧情 / 序章）
  --剧情播放结束--> TREE_OVERVIEW（L1 生命树·部门开放）
  --开放部门 + 指派今日异想体的收容部门 & 点「继续」--> LOADING
LOADING --加载完成--> DEPLOY（L2 编成·部署员工）
DEPLOY --玩家点「开始这一天」--> DAY_RUNNING（L3 部门场景实时）
  --能源达标 & 玩家点「结束当天」--> DAY_SETTLE（结算）
  ✗ 未达标时【不允许】进入 DAY_SETTLE（无强制结束分支，见 GDD §3.11）
DAY_SETTLE --结算完成 & 天数<50--> CUTSCENE（次日）
DAY_SETTLE --天数=50--> ENDING（真/假结局）
任意状态 --暂停--> PAUSE
  ├─「重新开始这一天」--> 用当日内存态重置 --> TREE_OVERVIEW（回到当日 L1 生命树，不写盘）
  └─「回到记忆日」------> 读最近记忆日快照 --> TREE_OVERVIEW（回滚到该日 L1 生命树）
```
> 注：早期草案曾有"未达标强制结束（forced=true）"分支，**已废止**——现规则为未达标必须继续工作或重开当天。
> 注（v2.6）：日循环入口为 **L1 生命树**（每日必经，GDD §3.1 三），不再直接进编成；「开始这一天」按钮归 **L2 编成**。

### 1.3 关键数据
| 字段 | 类型 | 说明 |
|---|---|---|
| `currentDay` | int | 当前天数（1-50） |
| `todayQuota` | int | 当日能源配额 |
| `todayEnergy` | int | 当日已收集情感粒子 |
| `starStones` | int | 星石（持久） |
| `isDayEnded` | bool | 当日是否已结算 |

### 1.4 关键逻辑 🔬
```csharp
// 当日配额（考验日上调 30%）
int CalcQuota(int day, bool isTrialDay) {
    float q = config.quotaBase * Mathf.Pow(config.quotaGrowthPer10Days, day / 10f);
    if (isTrialDay) q *= 1.3f;          // 考验日上调 30%【已定稿】
    return (int)q;
}

// 是否达标
bool IsQuotaMet(int todayEnergy, int quota) => todayEnergy >= quota;

// 日终结算：结余转星石
int ConvertSurplusToStarStone(int surplus) => surplus / 10;   // [GDD §3.7 占位 10:1]
```

### 1.5 边界与异常
- **未达标不得结束当天**【已定稿，GDD §3.11】：玩家须继续工作直至达标；若局面无法挽回（员工大量崩溃等），只能「重新开始这一天」回到当日编成重打。**不提供"提前结算/强行结束"选项**。
- 配额必须 > 0；`day > 50` 不允许进入 DAY_RUNNING。
- 记忆日（Day 5/10/…/45）在进入 DAY_START 时**自动锚定快照**（见 FDD-06）。

### 1.6 验收点
- [ ] 能从 Day1 连续跑到 Day50 不崩
- [ ] 配额随天数正确递增
- [ ] 达标/未达标两条结算路径均可走通
- [ ] 记忆日自动锚定

---

## FDD-02 工作系统（核心）

### 2.1 职责
处理员工对异想体执行 4 种工作的完整链路：校验 → 移动 → 进度 → 判定 → 结算 → 成长。

### 2.2 状态机（单个员工）
```
IDLE（空闲/在场景中）
  --玩家选中并下达移动--> MOVING
  --到达收容单元--> READY
  --玩家选择工作类型--> WORKING（进度条推进）
  --进度完成--> RESOLVING（判定）
  --判定完成--> IDLE（回到空闲）
  --玩家中断/员工崩溃/异想体出逃--> IDLE（中断无进度收益）
```

### 2.3 判定规则
> 规则权威见 **GDD §3.4**，此处仅为实现映射。

| 工作类型 | 判定属性 | 出处 |
|---|---|---|
| 演奏 | 演奏水平 `performance` | GDD §3.4 |
| 谈话 | 精神 `sp` | GDD §3.4 |
| 创作 | 共感 `empathy` | GDD §3.4 |
| 比武 | 血量 `hp` | GDD §3.4 |

实现时通过 `WorkRules.GetJudgeStat()` 与 `WorkRules.CalcSuccessRate()` 两个纯函数完成，公式与系数以 GDD 为准。

### 2.4 关键逻辑 🔬
```csharp
// 判定属性
int GetJudgeStat(Character c, WorkType t) => t switch {
    WorkType.Performance => c.performance,
    WorkType.Talk        => c.sp,
    WorkType.Creation    => c.empathy,
    WorkType.Combat      => c.hp,
};

// 成功率
float CalcSuccessRate(float baseRate, int statValue, float mod)
    => Mathf.Min(1f, baseRate * (statValue / 120f) * mod);

// 收益递减（同日对同一异想体第 n 次工作）【已定稿】
float Diminishing(int n) => 1f / (1f + 0.2f * Mathf.Max(0, n - 1));
// 效果：第1次100% → 第2次83% → 第3次71% → 第5次56% → 第10次36%

// 成长值（成功/失败都成长）【已定稿系数】
int CalcGrowth(int consumed, bool success)
    => Mathf.RoundToInt(consumed * (success ? 0.10f : 0.03f));
// 例：消耗10点 → 成功 +1、失败 +0.3（取整后为 0，建议失败保底 +1 待确认）
```

### 2.5 产出规则
| 结果 | 情感粒子 | 异想体点数 | 消耗 | 成长 |
|---|---|---|---|---|
| 成功 | 全额 | 全额 | 按工作类型扣血/精 | 较大 |
| 失败 | 少量（约 25% [占位]） | 少量 | 按工作类型扣血/精 | 较小 |

### 2.6 边界与异常
- 员工 hp 或 sp 为 0 → 不可派遣（崩溃状态）。
- 门槛校验：异想体危险等级要求判定属性达指定档（GDD §3.8）。
- 中断工作：不发放任何进度收益，但已扣消耗不返还。
- 同一员工同日可多次工作（**不禁止**），仅产出递减。

### 2.7 验收点
- [ ] 4 种工作均能正确取到对应判定属性
- [ ] 成功率公式与 GDD 一致（单测覆盖边界：0/120/超上限）
- [ ] 递减生效且不影响派遣本身
- [ ] 中断无收益、消耗不返还

---

## FDD-03 异想体与情绪值系统（含出逃 / 突破）

### 3.1 职责
维护异想体情绪值，驱动出逃（归零）与突破事件（爆满）两条分支。

### 3.2 状态机
```
CONTAINED（收容中）
  --情绪值 <= 0--> ESCAPED（出逃，具象化活动）
  --情绪值 >= 100--> BREAKTHROUGH（突破事件·正向）
  --工作影响--> CONTAINED（情绪值变化）
ESCAPED --玩家派员工镇压/安抚成功--> CONTAINED（+异想体点数奖励）
ESCAPED --长时间未处理--> 持续负面影响（能源流失/精神下降/连锁阈值）
BREAKTHROUGH --播完剧情--> CONTAINED（解锁个人线下一章）
```

### 3.3 关键数据
| 字段 | 来源 |
|---|---|
| `mood` 0-100 | GDD §3.2 |
| `moodDangerMin/Max` | GDD §3.2（危险窗口） |
| `escapeTrigger` / `escapeBehavior` / `suppressMethod` | 异想体数据表 |
| `points` / `storyUnlockThresholds` | GDD §3.2 |

### 3.4 规则要点
- **出逃（归零）**：离开收容单元、按 `escapeBehavior` 持续影响设施；镇压成功 → 回归 + 点数奖励；长期未处理 → 能源流失/精神下降/连锁触发其他异想体阈值变化。**可恢复，无永久损失**。
- **突破（爆满 100）**：**正向事件** —— 揭示深层故事、解锁对应角色个人线下一章、额外奖励。与出逃形成正负两极。
- 每日候选池：从池中抽 `3 + 天数/3`（向下取整，上限 5）个候选，选 1 个收容，**未选回池** [GDD §1.3(1)]。

### 3.5 验收点
- [ ] 情绪值归零触发出逃、达 100 触发突破
- [ ] 镇压成功回归并奖励点数
- [ ] 候选池抽取数量符合公式，未选回池
- [ ] 无任何"永久锁死"逻辑

---

## FDD-04 员工系统（招录 / 捏人 / 训练 / 崩溃）

### 4.1 状态机（员工生命周期）
```
CANDIDATE（候选，未录用） --玩家调整外观并确认录用--> ACTIVE
ACTIVE --被编成--> DEPLOYED
ACTIVE/DEPLOYED --hp或sp归零--> COLLAPSED（崩溃退场 N 天）
COLLAPSED --恢复计时结束 或 回复室加速--> ACTIVE
```

### 4.2 招录与捏人
- 消耗星石生成候选：姓名 / 发型 / 发色 / 眼睛样式 / 表情 / 初始属性倾向**随机** [GDD §3.3]
- 录用前可自由调整外观与姓名（普通员工）；**乐队少女为原著形象，不可改外观**
- 招录数量受收容室设施等级限制 [GDD §3.6]

### 4.3 训练（星石养成）🔬 —— **按"级"提升，不按"点"**
```csharp
// 等级下限（提升后落到新等级的最低边缘）
int LevelMin(int level) => level switch {
    1 => 1, 2 => 21, 3 => 41, 4 => 61, 5 => 81, _ => 101   // I / II / III / IV / V / EX
};

// 训练：提升一个等级，属性值 = 新等级下限
// 返回是否成功（已达 EX 或星石不足则失败）
bool Train(Character c, string stat) {
    int cur  = GetStat(c, stat);
    int lv   = GetLevel(cur);              // 1..6（6 = EX）
    if (lv >= 6) return false;             // EX 后不再训练
    int cost = TrainCost(lv);              // 按"目标等级"计费
    if (!SpendStarStones(cost)) return false;
    SetStat(c, stat, LevelMin(lv + 1));    // 例：15(I) → 21(II)
    return true;
}

// 费用：参考脑叶，随目标等级递增（占位值）
int TrainCost(int currentLevel) => currentLevel switch {
    1 => 10, 2 => 30, 3 => 100, 4 => 300, _ => 800,   // I→II / II→III / III→IV / IV→V / V→EX
};
```
- 普通员工与乐队少女**均可训练，且不受章节/个人线约束**（随时可训，只受星石限制）【已定稿 2026-09-07】
- 与"工作成长"并行：训练快但花星石（一次跨一级），工作慢但免费（数值缓慢累积）。
- **UI 只显示等级**：内部用 1-120 数值计算，展示层转成 I~EX（GDD §2 显示规则）。

### 4.3.1 属性显示转换 🔬
```csharp
// 数值 → 等级名（UI 专用，不参与计算）
string StatDisplay(int value) => value switch {
    <= 20  => "I",  <= 40 => "II", <= 60 => "III",
    <= 80  => "IV", <= 100 => "V", _ => "EX"
};
```

### 4.4 崩溃与死亡（v1.5 定稿，两类员工不同）
```
血量或精神归零
   ├─ 乐队少女 → 崩溃：退场 3 天（草案），装备/成长/个人线保留，恢复后归队
   └─ 普通员工 → 死亡：永久失去（移出名册），装备全部掉落消失（需重新研发），可招录补充
```
- 普通员工死亡**不视为 Game Over**，不产生连锁恐慌伤害（简化设计）。
- 死亡时流程：移除角色 → 清除其装备数据 → 发布 `StaffDied` 事件（UI 提示）→ 允许玩家重新招录。

### 4.5 验收点
- [ ] 招录扣星石、槽位上限生效
- [ ] 捏人参数写入 `appearance`；乐队少女外观不可改
- [ ] 训练一次提升**一个等级**，属性值 = 新等级下限（如 15→21）
- [ ] 已达 EX（101）后不再提供训练按钮
- [ ] 星石不足时训练失败并提示
- [ ] **UI 只显示等级**（I~EX），不出现 1-120 的具体数值
- [ ] **乐队少女**：崩溃 3 天后自动恢复，回复室可加速，装备保留
- [ ] **普通员工**：死亡后移出名册、装备清空，可重新招录

---

## FDD-05 考验系统

### 5.1 状态机
```
LOCKED（未解锁）
  --乐队完成对应任务--> UNLOCKED
UNLOCKED --玩家发起--> IN_PROGRESS（全员参与大型工作/演出）
IN_PROGRESS --达成目标--> COMPLETED
IN_PROGRESS --失败--> FAILED（可重试，无永久损失）
```

### 5.2 规则
- 每支乐队一次考验；**与下一支乐队解锁解耦**（解锁不要求通过上一支考验）[GDD §3.10]
- 考验与任务**无倒计时**；唯一硬性截止点是真结局窗口
- 全员参与；失败可重试

### 5.3 结局判定 🔬
```csharp
// Day46 前完成全部考验 → 真结局
bool IsTrueEndingUnlocked(int currentDay, int completedTrials, int totalTrials)
    => currentDay <= 46 && completedTrials >= totalTrials;
    // "归途之音充满" == 全部考验完成（非独立资源条件）[GDD §2.3 / §3.11]
```
- Day50：真 → 真结局演出；否则 → 假结局（**可回档补齐后重走**）

### 5.4 验收点
- [ ] 考验解锁不阻塞乐队解锁
- [ ] 失败可重试且无永久损失
- [ ] Day46 判定与 Day50 结局分支正确

---

## FDD-06 记忆日与存档系统

### 6.1 职责
存档读写、记忆日快照锚定、回滚到最近记忆日。

### 6.2 流程
```
记忆日（Day 5/10/15/20/25/30/35/40/45）进入编成界面
  → SaveSystem.AnchorMemoryDay(day)  // 另存 snapshot_dayN.json
玩家在暂停菜单选「回到记忆日」
  → RollbackToMemoryDay() → 恢复最近快照 → 回到该日编成
```

### 6.3 存档内容（GDD §4.4）
`当前天数 / 情感粒子·星石·异想体点数 / 所有角色四维与状态 / 异想体状态（点数与出逃情报）/ 已解锁剧情进度 / 今日编成`

### 6.4 规则
- 路径：`%USERPROFILE%/AppData/LocalLow/ResonanceShelter/Saves/`【已定稿】
- **仅记忆日写盘锚定**（Day 5/10/…/45）；**不做每日快照**【已定稿 2026-09-07】。
- "重新开始这一天"使用**进入日循环时的内存态**重置（不写盘）：在玩家点「开始这一天」时于内存中保留当日初始状态，重开当天即用其恢复；因此退出游戏后当天进度不保留，重进回到最近记忆日（最多损失约 4 天）。
- 回滚保留：图鉴、已解锁乐队、异想体点数、星石、任务进度
- 回滚**移除**：快照之后新招录的员工、快照之后的属性成长 [GDD §3.12 记忆日规则]

### 6.5 验收点
- [ ] 记忆日自动锚定（9 个记忆日全部覆盖）
- [ ] 回滚后数据符合"保留/移除"规则
- [ ] 存档含版本号字段，便于迁移

---

## FDD-07 剧情触发系统

### 7.1 剧情分级（GDD §3.5）
| 层级 | 触发条件 | 表现 |
|---|---|---|
| 日常反馈 | 每次工作完成 | 短反馈（一行台词/表情差分/小演出 ≤5 秒） |
| 节点剧情 | ①同一异想体工作累计第 1/3/5/10 次 ②异想体点数跨档 ③特定角色组合对特定异想体工作 ④章节/考验节点 | 完整演出（立绘+对话+场景） |
| 组合触发 | 特定角色组合 + 特定异想体 | 专属隐藏互动剧情 |

### 7.2 实现要点
- `DialogueTrigger`（自研状态层）负责判定"是否可触发"；`DialogueManager`（Yarn）负责播放
- 剧情**不修改数值**（数值由 System 结算后通过事件传递）

### 7.3 验收点
- [ ] 日常反馈不打断管理节奏（≤5 秒）
- [ ] 四类节点触发条件均可命中
- [ ] 剧情播放期间游戏状态正确（暂停/可跳过）

---

## FDD-08 生命树（部门开放）与编成系统

> **流程顺序（2026-09-12 修订，依据 GDD §3.1）**：
> 每日剧情 → **L1 生命树（部门开放 + 今日异想体收容分配）** → 加载 → **L2 编成（部署员工）** → L3 部门场景。
> 生命树在编成**之前**，且**每天都要经过**（不再是从编成点「开始这一天」之后才进入）。

### 8.1 生命树·部门开放（L1，每日必经）

**职责**：选「开放哪个部门」+ 决定「今日异想体收容进哪个部门」。

| 项 | 规则 | 出处 |
|---|---|---|
| 入口 | 每日剧情（F11）结束后自动进入；「新游戏」为序章剧情后 | GDD §3.1 二 |
| 节点 | 节点 = 乐队（部门）；状态 `LOCKED / OPENING / OPEN` | GDD §3.1 四 |
| 开放 | 到达解锁天数且有开放机会 → 点选 LOCKED 节点 → F5 确认弹窗 → `OPENING` → `OPEN` | GDD §3.1 四 / F5 |
| 收容分配 | 当日抽到的异想体（池抽 `3 + 天数/3`，上限 5，选 1，未选回池）在此决定放进哪个已开放且未满的部门 | GDD §1.3(1) / §3.1 四 |
| 部门容量 | 每部门异想体上限 **4**（`[GDD §3.1 四 草案]`），已满的部门不可再放入 | GDD §3.1 四 |
| 跳过分配 | **5 的倍数天不抽新异想体**，跳过分配直接继续 | 用户定稿 2026-09-12 |
| 警示 | 有出逃 / 考验 / 剧情事件的节点带警示角标，点击可查看详情（仍可进入） | GDD §3.1 四 |
| 顶部信息 | Day N、当日能源配额与已收集、今日待分配异想体 | UI Spec F4 |
| 出口 | 点「继续」→ **加载（F2）** → 进入 L2 编成 | GDD §3.1 四 |

### 8.2 编成·部署员工（L2）

> 界面交互规范：`UI_Interaction_Spec.md` **F15 编成界面（L2 部署员工）**（参考图 `进入正式管理前的游戏准备界面.png`）。

- 从"可用员工"（普通员工 + 已解锁乐队少女）中选当日上场阵容；左侧列表 / 中间阵容预览 / 右侧异想体收容状态
- 员工可在此训练（消耗星石，按级提升，见 FDD-04 4.3）
- 显示当日能源配额与剧情预告
- 右上角「开始这一天」→ 进入 **L3 部门场景**（不再经过 L1 生命树）
- 编成可保存/读取预设

### 8.3 部门节点状态机

```
LOCKED（未解锁，暗色，显示解锁条件）
  --到达解锁天数 & 玩家点选 & F5 确认--> OPENING（播放解锁演出，不可点击）
  --演出结束--> OPEN（点亮，可接收异想体 / 可进入）
OPEN --部门异想体数达上限 4--> OPEN（满，仍可进入但不可再放入新异想体）
```
- `LOCKED` 点击 **不跳转**，只显示解锁条件 tooltip（如"Day 6 解锁"）。
- `OPENING` 为**临时过渡态**，演出结束后必须落到 `OPEN`（不允许卡死）。

### 8.4 数据结构（`Bands.json`，已定稿）

> ⚠️ **当前工程 `Assets/Data/` 尚无部门节点数据源**（只有 Characters / BandMembers / Aberrations / GameConfig 四件套）。
> M3 生命树开工前**必须先补此文件**，否则无数据可渲染。

```jsonc
[
  { "id": "band_ppp",  "band": "Poppin'Party",        "leader": "户山香澄",   "leaderId": "band_ppp_kasumi",   "layer": "Asiyah",   "nodeIndex": 1,  "unlockDay": 1,  "defaultState": "OPEN"   },
  { "id": "band_pasp", "band": "Pastel*Palettes",     "leader": "丸山彩",     "leaderId": "band_pasp_aya",     "layer": "Asiyah",   "nodeIndex": 2,  "unlockDay": 6,  "defaultState": "LOCKED" },
  { "id": "band_aglw", "band": "Afterglow",           "leader": "美竹兰",     "leaderId": "band_aglw_ran",     "layer": "Asiyah",   "nodeIndex": 3,  "unlockDay": 11, "defaultState": "LOCKED" },
  { "id": "band_hhw",  "band": "Hello, Happy World!", "leader": "弦卷心",     "leaderId": "band_hhw_kokoro",   "layer": "Asiyah",   "nodeIndex": 4,  "unlockDay": 16, "defaultState": "LOCKED" },
  { "id": "band_ros",  "band": "Roselia",             "leader": "凑友希那",   "leaderId": "band_ros_yukina",   "layer": "Briah",    "nodeIndex": 5,  "unlockDay": 20, "defaultState": "LOCKED" },
  { "id": "band_ras",  "band": "RAISE A SUILEN",      "leader": "CHU²",      "leaderId": "band_ras_chu2",     "layer": "Briah",    "nodeIndex": 6,  "unlockDay": 25, "defaultState": "LOCKED" },
  { "id": "band_moni", "band": "Morfonica",           "leader": "仓田真白",   "leaderId": "band_moni_mashiro", "layer": "Briah",    "nodeIndex": 7,  "unlockDay": 30, "defaultState": "LOCKED" },
  { "id": "band_mygo", "band": "MyGO!!!!!",           "leader": "高松灯",     "leaderId": "band_mygo_tomori",  "layer": "Atziluth", "nodeIndex": 8,  "unlockDay": 36, "defaultState": "LOCKED" },
  { "id": "band_amu",  "band": "Ave Mujica",          "leader": "丰川祥子",   "leaderId": "band_amu_sakiko",   "layer": "Atziluth", "nodeIndex": 9,  "unlockDay": 41, "defaultState": "LOCKED" },
  { "id": "band_mwt",  "band": "梦限大 MewType",       "leader": "仲町阿拉蕾", "leaderId": "band_mwt_arale",    "layer": "Atziluth", "nodeIndex": 10, "unlockDay": 46, "defaultState": "LOCKED" }
]
```
- 键名一律 **camelCase**（JsonUtility 静默失败风险，见 §4.4）。
- `aberrationCapacity`（每部门上限，草案 4）建议写入 `GameConfig.json` 而非逐节点配置。
- 运行时状态（当前已收容异想体列表、节点实际状态）**不写进配置**，运行时生成并入存档。
- ⚠️ **`id` 的乐队缩写为建议值，待策划确认**（仅 `PPP` = Poppin'Party、`mygo` = MyGO!!!!! 已有先例，见 `Band_Members.md`）。
- ✅ **部门顺序与解锁天数已全面对齐脑叶（GDD v1.8）**：中层 = 中央本部 Day20 → 惩戒部 Day25 → 福利部 Day30
  （原为 福利 Day21 / 中央本部 Day26 / 惩戒 Day31）。
  ⚠️ 副作用：**三个中层部门都在 5 的倍数天开放**，与记忆日重合 → 当天不抽新异想体，生命树只做"开放部门"一步；
  好处是部门开放失败可借记忆日回滚。若不接受此副作用，请把天数改回 Day21/26/31。
- ⚠️ **队长更正（策划 2026-09-12 拍板）**：Afterglow 队长 = **美竹兰**（主唱兼吉他），**不是贝斯手上原绯玛丽**；
  MyGO!!!!! 原著未设队长，本作**自定高松灯**。

### 8.4.1 队长机制（GDD v1.7 定稿）

- **队长 = 该乐队的乐队少女之一**，不是独立 NPC，**不新增数据实体**。
  程序只需在 `BandMembers.json` 的对应角色上加 `isLeader: true`（见 `Band_Members.md`），
  再从 `Bands.json.leaderId` 反查即可。
- 队长**可正常派遣**执行 4 种工作，遵循**乐队少女通用规则**：崩溃退场 3 天、**不死亡**、成长与装备保留。
- 「队长」身份**仅额外提供**三项，均属剧情/表现层，**不影响数值与流程**（与"异想体绑定 = 彩蛋"同一处理原则）：
  1. 该部门的被动加成 `TODO(设计待确认): 关联 GDD §3.14`（数值待 Phase 2 数值表）；
  2. 专属台词与剧情权重（该部门考验的主要叙事视角）；
  3. 生命树节点上显示队长名（原脑叶 Sephirot 名的位置）。
- 队长**不额外占用派遣名额**，与普通乐队少女一视同仁。
- 队长**崩溃退场期间**，部门仍正常运作（不触发脑叶式的部门失效），仅失去上述加成与专属台词。

### 8.5 边界与异常

- 所有已开放部门**均已满**且当天仍抽到新异想体 → 必须先开放新部门；若无部门可开放 → `TODO(设计待确认): 关联 GDD §3.1 四`（建议：允许暂不收容，该异想体回池）。
- 5 的倍数天不抽异想体 → 生命树只做"开放部门"这一步，可直接继续。
- 当日进行中**不可返回 L1**（UI Spec F4）；只可通过暂停菜单「重新开始这一天」回到当日 L1。
- **L3 内如何切换到另一个已开放部门** → `TODO(设计待确认): 关联 GDD §3.1 五`（建议：L3 侧栏加部门切换入口，与"编成快捷入口"同区）。
- **异想体池未补全不影响生命树与日循环运行**（GDD §3.2「绑定 = 彩蛋」）：异想体与角色**不要求一一对应**，
  池中已有异想体即可继续工作与推进任务。**不得把"异想体补齐"作为 M3 或任何里程碑的前置条件。**

### 8.6 验收点

- [ ] 每日剧情结束后自动进入 L1 生命树（不是先进编成）
- [ ] 生命树点「继续」→ 播放加载 → 进入 L2 编成
- [ ] 编成点「开始这一天」→ 直接进入 L3 部门场景
- [ ] 节点状态正确：`LOCKED` 显示解锁条件且不可进入；`OPENING` 不可点击且会落到 `OPEN`
- [ ] 今日异想体可被分配到指定部门；已满（4/4）的部门拒绝放入
- [ ] 5 的倍数天跳过异想体分配
- [ ] 部门场景能正确加载该部门的异想体与员工
- [ ] 编成可保存/读取预设
- [ ] 生命树节点上显示**队长名**（不是脑叶 Sephirot 名）
- [ ] 10 个节点按 `unlockDay` 升序解锁：**Day 1 / 6 / 11 / 16 / 20 / 25 / 30 / 36 / 41 / 46**（中层 Day20/25/30 已对齐脑叶）
- [ ] UI 容错：`leader` 字段为空时显示「队长未定」而非报错（当前 10 节点均已填，属健壮性检查）
- [ ] 队长可正常被派遣工作，且遵循乐队少女规则（崩溃退场、**不死亡**）

---

---

## FDD-09 员工移动与寻路（技术决策，已定）

### 9.1 决策结论
采用 **路点（Waypoint）方案**：部门场景预设路点图（走廊节点 + 各收容单元门口节点），员工沿路点连成的路径移动。

### 9.2 决策理由
1. **贴合脑叶原版表现**：脑叶部门场景本质是"走廊 + 房间"的线性/分支结构，员工沿走廊移动到收容单元门口，**并不做复杂寻路**——路点方案在观感上与之最接近。
2. **性能**：无运行时寻路计算（不像 A*/NavMesh 需每帧或每次下令时计算路径），2D 场景开销极低，稳守 60fps。
3. **实现与调试成本**：单人开发首选；路点可在编辑器中可视化摆放，路径走向可控、易调，不会出现"员工卡墙/走位诡异"。

### 9.3 实现方式（建议）
```
// 数据结构：路点图
class WaypointGraph {
    List<Waypoint> nodes;      // 含 id、位置、邻接节点 id 列表
    // 查询：给定起点/终点，返回路点序列（预计算或简单 BFS，规模小无需 A*）
    List<Vector2> FindPath(int fromNodeId, int toNodeId);
}
```
- 员工实际移动：沿 `FindPath` 返回的折线依次插值移动（可用 DoTween 或自写 Lerp）。
- 收容单元门口为终点路点，到达后进入 READY 状态（见 FDD-02）。

### 9.4 后续可扩展
若将来部门场景变为开放地形（非走廊结构），可升级为网格 A*；路点接口（`FindPath`）保持不变即可平滑替换。

### 9.5 验收点
- [ ] 员工能沿走廊正确走到目标收容单元门口
- [ ] 无穿墙/抖动/卡死
- [ ] 多个员工同时移动不互相阻塞（或按规则排队）

---

## 附录 A：数值配置汇总（占位项一览）

| 配置项 | 值 | 状态 |
|---|---|---|
| `quotaBase` | 300 | 占位 |
| `quotaGrowthPer10Days` | 2.0（每 10 天） | 占位 |
| 考验日配额系数 | **×1.3** | ✅ **已定稿**（2026-09-07） |
| 未达标处理 | **无法结束当天，只能重开当天** | ✅ **已定稿**（GDD §3.11） |
| 失败产出比例 | 25% | 占位 |
| 收益递减 | **`1/(1+0.2×(n-1))`** | ✅ **已定稿** |
| 成长系数 | **成功 `消耗×0.10` / 失败 `消耗×0.03`** | ✅ **已定稿** |
| 崩溃天数 | 3 天 | 草案 |
| 星石转化 | 10:1 | 占位 |
| 训练费用（按目标等级） | I→II 10 / II→III 30 / III→IV 100 / IV→V 300 / V→EX 800 星石 | 草案 |
| 训练约束 | **无约束（普通员工与乐队少女随时可训）** | ✅ **已定稿** |
| 训练方式 | **按"级"提升**——每次升一级，属性值=新等级下限（21/41/61/81/101）；EX 后停训 | ✅ **已定稿** |
| 属性显示 | **只显示等级**（I~EX），不显示具体数值；内部仍用 1-120 计算 | ✅ **已定稿** |
| 招录费用 | 50 星石，槽位 2/4/6/8 | 草案 |
| 寻路方案 | **路点 Waypoint** | ✅ **已定稿**（FDD-09） |
| 存档目录名 | **ResonanceShelter** | ✅ **已定稿** |

> 所有占位值写入 `GameConfig.json`，注释 `// TODO(设计待确认): 关联 GDD §X.X`（agent.md §4.2）。

---

## 附录 B：待确认清单

> 2026-09-07 已回答 7 项中的 6 项（见 附录 A 标记 ✅），剩余如下。

| # | 问题 | 关联 | 状态 |
|---|---|---|---|
| 1 | 失败成长是否设**保底 +1**（避免小消耗时取整为 0） | FDD-02 | 待确认 |
| 2 | `quotaBase=300` 与 `每10天×2.0` 的最终曲线是否合理 | GDD §3.12 | 待实测调参 |
| 3 | 崩溃天数 3 天 与 `recoveryRate=5.0` 的匹配 | GDD §3.11 | 待实测调参 |
| 4 | 招录费用 50 星石 / 槽位 2-4-6-8 是否合适 | GDD §3.3 | 待实测调参 |

### B.1 已定稿决策记录（2026-09-07）

| 原问题 | 决策 |
|---|---|
| 单日未达标的处理 | **无法结束当天**，须继续达标或重开当天（不提供提前结算） |
| 考验日配额 | **上调 30%** |
| 收益递减曲线 | **`1/(1+0.2×(n-1))`** |
| 成长系数 | **成功 `消耗×0.10` / 失败 `消耗×0.03`** |
| 乐队少女训练约束 | **无约束，随时可训** |
| 寻路方案 | **路点 Waypoint**（脑叶亦为走廊结构，非复杂寻路） |
| 存档目录名 | **ResonanceShelter** |

---

---

## FDD-10 异想体数据规格（含"未奏响的乐章"定稿）

### 10.1 workModifiers 语义（**乘数**，基准 1.0）
- 依据 GDD §3.4：`最终成功率 = min(1, 基础成功率 × (判定属性值/120) × 异想体修正)`
- `workModifiers` 一律以**乘数**存储，基准 **1.0**（1.0=无修正，>1 提升，<1 降低）。
- **禁止负值**（负乘数会产生负成功率）；极低下限取 `0.1`。
- GDD 样例中写作 `+0.2 / 0 / -0.1 / -0.3` 的加值写法，实际存储为 `1.2 / 1.0 / 0.9 / 0.7`。

### 10.2 新增字段：specialEffects（因"未奏响的乐章"引入）
承载"工作结果为优/满产出"等异想体专属效果，由 `WorkSystem` 在结算后读取执行。

```jsonc
"specialEffects": [
  { "trigger": "workResultExcellent", "effect": "restoreSp", "target": "worker",     "value": 8 },
  { "trigger": "fullOutput",          "effect": "restoreSp", "target": "department", "value": 5 }
]
```
- `trigger`：`workResultExcellent`（结果为优） / `fullOutput`（本次满产出） / 后续可扩展
- `effect`：`restoreSp`（恢复精神） / `restoreHp` / 其他（按需扩展）
- `target`：`worker`（执行工作的员工） / `department`（该部门所有员工）
- `value`：**占位值，待实测调参**（关联 GDD §3.4）

### 10.3 "未奏响的乐章" 完整数据（供填入 Aberrations.json）
> 定稿 2026-09-08；由程序 AI/开发者填入 `Assets/Data/Aberrations.json`，本文档只给规格、不直接创建工程文件。

```jsonc
{
  "id": "aber_score_01",
  "name": "未奏响的乐章",
  "type": "回忆型",
  "riskLevel": 1,                       // ZAYIN：完全无害，无派遣门槛
  "mood": 50,
  "moodDangerMin": 0,                   // 无危险窗口（ZAYIN 不触发出逃）
  "moodDangerMax": 100,
  "linkedCharacterId": "band_mygo_anon",  // 千早爱音（MyGO!!!!!）
  "workModifiers": [                    // 乘数，基准 1.0
    { "workType": "Performance", "modifier": 1.3 },
    { "workType": "Creation",    "modifier": 1.1 },
    { "workType": "Talk",        "modifier": 1.0 },
    { "workType": "Combat",      "modifier": 0.5 }
  ],
  "energyPerSuccess": 10,               // 每次成功产出的情感粒子（失败约 25%）
  "pointsPerSuccess": 5,                // 每次成功产出的异想体点数（草案）
  "storyUnlockThresholds": [0, 20, 50],
  "escapeTrigger": null,                // ZAYIN 无出逃
  "escapeBehavior": null,
  "suppressMethod": null,
  "specialEffects": [
    { "trigger": "workResultExcellent", "effect": "restoreSp", "target": "worker",     "value": 8 },
    { "trigger": "fullOutput",          "effect": "restoreSp", "target": "department", "value": 5 }
  ],
  "background": "一段从未被奏响的旋律，凝固成了等待知音的空谱。它没有过去，只在等待一个能赋予它意义的人。"
}
```

### 10.4 验收点
- [ ] 该异想体可被收容、可对它执行 4 种工作
- [ ] 演奏成功率最高、比武极低（乘数生效，无负值）
- [ ] 工作结果为"优"时，执行员工精神值恢复
- [ ] 满产出时，该部门所有员工精神值恢复
- [ ] ZAYIN 无出逃：情绪值归零/爆满均不触发出逃事件

---

---

## FDD-11 装备系统（E.G.O 式，参考脑叶）

### 11.1 设计定位
装备系统**只影响数值与表现**，不解锁/阻塞任何流程（与"异想体绑定=彩蛋"原则一致）。装备分三类，与脑叶 E.G.O 一一对应：

| 类型 | 脑叶对应 | 作用 | 获取方式 |
|---|---|---|---|
| **武器** Weapon | E.G.O 武器 | 镇压出逃异想体时造成伤害；附带工作成功率修正 | 消耗该异想体的**异想体点数**研发 |
| **护甲** Armor | E.G.O 护甲 | 减免员工受到的伤害（四种伤害类型各有抗性） | 同上（点数要求更高） |
| **饰品** Accessory | E.G.O 饰品 | 属性加成（HP/SP/演奏/共感等） | 对该异想体**工作完成时随机掉落** |

### 11.2 伤害类型（沿用脑叶四色，便于开发对照）
| 类型 | 键名 | 效果 | 本作表现（美术自创，不血腥） |
|---|---|---|---|
| 红（物理） | `RED` | 减少 HP | 冲击 |
| 白（精神） | `WHITE` | 减少 SP | 音波/情绪冲击 |
| 黑（侵蚀） | `BLACK` | 同时减少 HP 与 SP | 复合干扰 |
| 蓝（灵魂） | `PALE` | 按最大 HP 百分比扣血 | 异质侵蚀（稀有、高危） |

### 11.3 武器属性
| 字段 | 说明 |
|---|---|
| `id` / `name` | 标识与名称 |
| `type` | `weapon` |
| `grade` | 装备等级 `ZAYIN/TETH/HE/WAW/ALEPH`（源自异想体等级） |
| `sourceAberrationId` | 来源异想体（装备与该异想体绑定） |
| `damageType` | `RED/WHITE/BLACK/PALE` |
| `damage` | 伤害区间 `[min, max]` |
| `attackSpeed` | `fast / normal / slow` |
| `attackRange` | `short / medium / long` |
| `requirement` | 装备要求：`{ stat, level }`（如演奏水平 ≥ II） |
| `effects` | 附加效果（如"演奏成功率 +5%"） |
| `unlockCost` | 研发所需该异想体点数 |

### 11.4 护甲属性
| 字段 | 说明 |
|---|---|
| `resistances` | 四色抗性 `RED/WHITE/BLACK/PALE`，**1.0 = 普通，<1 = 抗性，>1 = 弱点**（如 0.5 强抗、2.0 极弱） |
| `requirement` | 装备要求（属性等级） |
| `unlockCost` | 研发所需点数（通常高于武器） |
| `effects` | 附加效果（如"移动速度 +5%"） |

### 11.5 饰品与槽位
- **槽位（简化自脑叶 14 槽）**：`head`（头部）/ `face`（面部）/ `neck`（颈部）/ `hand`（手部）/ `back`（背部）/ `charm`（挂饰）—— 共 **6 槽**。
- 同槽位新饰品**覆盖**旧饰品（脑叶规则）；等级 ≥ IV 的角色可**锁定**饰品不被覆盖（可选功能，Phase 2 后）。
- 饰品无等级要求，任何角色可获得；提供属性加成。

### 11.6 装备规则
1. **装备要求只在装备前检查**：角色对应属性等级不足时**无法装备**；一旦装备成功，**不再因属性下降（崩溃等）而卸下**（与脑叶"次日强制卸下"不同，本作采用更宽松规则）。
2. **武器/护甲数量**：每名角色同时最多 1 武器 + 1 护甲 + 6 饰品（每槽 1 件）。
3. **乐队少女（绑定获得）**：其专属武器/护甲/饰品**随该少女加入而出现，并绑定该少女**——
   - 不可转让给其他角色，也不可由他人装备；
   - 与该少女的同位体异想体在设定上呼应（彩蛋性质，不影响数值）；
   - 少女加入时自动获得（不需要研发）。
   - ⚠️ **不是"第二套"**：绑定少女获得的就是该异想体的那一套装备，只是获得方式不同（见下条）。
4. **普通员工 / 其他角色（研发获得）**：**所有装备均由异想体研发而来**——
   - 每个异想体产出**一套**装备（武器 1 + 护甲 1 + 饰品 1），见 `Equipments.md`；
   - 消耗该异想体的**异想体点数**研发 → **解锁该装备型号**（非单件），可装备给**任意数量**满足条件者（与脑叶一致）；
   - 饰品例外：不研发，从对该异想体工作完成时**随机掉落**（每名角色独立获得）。
5. **装备丢失（死亡时）**【v1.5 定稿】：
   - **普通员工死亡** → 其装备（武器/护甲/饰品）**全部掉落消失**，需重新研发（消耗对应异想体点数）。
   - **乐队少女不会死亡**（仅崩溃退场 3 天），**崩溃时装备保留**，恢复后照常使用。
   - 死亡不产生连锁伤害，也不视为 Game Over（GDD §3.11）。
6. **不阻塞流程**：装备缺失/丢失不影响任何任务与剧情解锁。

> 依据：GDD §3.11「角色倒下规则」——普通员工死亡（永久失去、装备掉落、可招录补充）；乐队少女不死亡（崩溃退场、装备与成长保留）。

### 11.7 数据结构（Equipments.json）
```jsonc
[
  {
    "id": "ego_weapon_sun_01",
    "name": "未熄的拨片",
    "type": "weapon",
    "grade": "HE",
    "sourceAberrationId": "aber_sun_01",
    "damageType": "RED",
    "damage": [5, 8],
    "attackSpeed": "normal",
    "attackRange": "short",
    "requirement": { "stat": "performance", "level": 2 },
    "effects": [ { "type": "workSuccessRate", "workType": "Performance", "value": 0.05 } ],
    "unlockCost": 40
  },
  {
    "id": "ego_armor_sun_01",
    "name": "余晖外套",
    "type": "armor",
    "grade": "HE",
    "sourceAberrationId": "aber_sun_01",
    "resistances": { "RED": 0.8, "WHITE": 0.6, "BLACK": 1.2, "PALE": 1.5 },
    "requirement": { "stat": "empathy", "level": 2 },
    "effects": [],
    "unlockCost": 120
  },
  {
    "id": "ego_gift_sun_01",
    "name": "星形发夹",
    "type": "accessory",
    "slot": "head",
    "sourceAberrationId": "aber_sun_01",
    "bonus": { "empathy": 3 },
    "dropWeight": 30
  }
]
```
> 说明：`unlockCost` / `damage` / `bonus` / `dropWeight` 均为**占位值**，待 Phase 2 数值表统一（关联 GDD §3.2 异想体点数）。

### 11.8 关键逻辑 🔬
```csharp
// 装备要求校验
bool CanEquip(Character c, Equipment e)
    => GetStatLevel(c, e.requirement.stat) >= e.requirement.level;

// 实际伤害（护甲抗性）
float CalcDamage(float raw, Equipment armor, string dmgType)
    => raw * (armor == null ? 1f : armor.resistances[dmgType]);

// 工作成功率受武器效果影响（叠加进异想体修正之外）
float ApplyEquipmentBonus(float rate, Character c, WorkType t)
    => rate + SumEffects(c.weapon, t);
```

### 11.9 验收点
- [ ] 武器/护甲/饰品三类均可被正确加载与装备
- [ ] 属性等级不足时**无法装备**；**装备后不因属性下降而卸下**（仅装备前检查，GDD §3.2/FDD-11.6）
- [ ] 四色抗性生效（护甲减伤按类型计算）
- [ ] 饰品按槽位覆盖，同槽位不叠加
- [ ] 研发消耗对应异想体点数；点数不足不可研发
- [ ] 装备缺失**不阻塞**任何任务/剧情（彩蛋原则）

---

---

## FDD-12 相机控制系统（v2.2 新增，参考脑叶）

### 12.1 职责
管理部门场景（L3）的**正交相机**：平移输入、滚轮缩放、边界钳制、平滑移动。
不参与玩法计算，只负责"玩家看哪里、看多广"。

### 12.2 相机规格（草案，待调参）
| 项 | 值 | 说明 |
|---|---|---|
| 相机类型 | **正交相机**（Orthographic） | URP 2D 项目；不使用透视 |
| 默认 orthographicSize | 6（`[占位]`） | 对应视野高度 12 单位 |
| 缩放范围 | `0.5x ~ 2.0x` | 通过修改 orthographicSize 实现 |
| 默认缩放 | `1.0x` | 视野约 21 x 12 单位（16:9） |
| 部门场景尺寸 | 约 `40 x 12` 单位（`[占位]`） | 横向布局：主房间 + 收容单元 + 走廊 |
| 默认视野占比 | 约可见部门的 **1/2 宽度** | 与脑叶一致：默认看不到整个部门 |

### 12.3 输入映射
| 输入 | 行为 |
|---|---|
| `WASD` / 方向键 | 平移（速度 `panSpeed`，`[占位]`） |
| 鼠标中键拖动 | 平移（拖拽增量反向映射） |
| 滚轮 | 缩放（步进 `zoomStep`，`[占位]`；带插值平滑） |
| 中键单击 / `Home` | 重置到默认位置与缩放 |

> 使用 **Input System**（工程已启用），在 Input Actions 中定义 `Camera/Pan`、`Camera/Zoom`、`Camera/Reset`。

### 12.4 关键逻辑 🔬
```csharp
// 缩放：修改正交尺寸并钳制
void SetZoom(float factor) {                       // factor: 0.5 ~ 2.0
    float size = defaultSize / Mathf.Clamp(factor, minZoom, maxZoom);
    cam.orthographicSize = Mathf.Lerp(cam.orthographicSize, size, zoomLerp);
}

// 平移 + 边界钳制
void MoveCamera(Vector2 delta) {
    Vector3 pos = transform.position + (Vector3)delta * panSpeed * Time.deltaTime;
    pos.x = Mathf.Clamp(pos.x, bounds.min.x + halfW, bounds.max.x - halfW);
    pos.y = Mathf.Clamp(pos.y, bounds.min.y + halfH, bounds.max.y - halfH);
    transform.position = pos;
}
```
- 相机移动与缩放均做**插值平滑**（避免突兀跳转）。
- 边界 `bounds` 由部门场景的碰撞盒/配置给出，随部门切换更新。
- **性能**：相机更新放 `LateUpdate`（在所有目标移动之后），避免画面抖动。

### 12.5 验收点
- [ ] WASD / 方向键可上下左右平移视角
- [ ] 鼠标中键拖动可平移（不与右键下令冲突）
- [ ] 滚轮可缩放，且在 `0.5x~2.0x` 之间钳制
- [ ] 相机不会移出部门场景边界（无空白露馅）
- [ ] 缩放/平移平滑，无抖动或跳变
- [ ] 出逃事件发生时，玩家可通过移动视角找到出逃体（或警报点击定位）
- [ ] 与工作/下令操作**不冲突**（右键仍为命令键）

---

## 附录 C：测试场景清单（Phase 0-2 需创建）

> 依据 agent.md §6 测试规范：纯逻辑用单元测试（EditMode），系统流程用最小测试场景。

| 测试场景 | 覆盖模块 | 测试内容 | 阶段 |
|---|---|---|---|
| `Test_WorkSystem.unity` | FDD-02 | 4 种工作的判定属性映射；成功率边界（0 / 120 / 超上限）；成功与失败两条结算路径；收益递减生效 | Phase 1-2 |
| `Test_DayLoop.unity` | FDD-01 | 日循环状态迁移（START→RUNNING→SETTLE→次日）；配额随天数递增；未达标无法结束；考验日 ×1.3 | Phase 1-2 |
| `Test_SaveSystem.unity` | FDD-06 | 存档写入/读取；记忆日（Day 5/10/…）自动锚定；回滚后"保留/移除"规则正确 | Phase 1 |
| `Test_AberrationMood.unity` | FDD-03 | 情绪值归零→出逃；情绪值爆满→突破事件（正向）；镇压成功回归+奖励点数；候选池抽取与回池 | Phase 2 |
| `Test_CameraSystem.unity` | FDD-12 | 平移（WASD/中键拖动）与滚轮缩放；边界钳制不露空白；缩放档位 0.5x~2.0x；与右键下令不冲突 | Phase 2 |
| `Test_RecruitTrain.unity` | FDD-04 | 招录扣星石与槽位上限；捏人参数写入；训练按档计费与加点不超上限 | Phase 1 |
| `Test_TrialSystem.unity` | FDD-05 | 考验解锁不阻塞乐队解锁；失败可重试；Day46 真结局判定与 Day50 结局分支 | Phase 2 |

**单元测试（EditMode，纯函数）**

| 测试目标 | 用例 |
|---|---|
| `WorkRules.CalcSuccessRate` | 边界：statValue=0 → 0；statValue=120 且 mod=1 → baseRate；上限截断 min(1, …) |
| `WorkRules.GetJudgeStat` | 4 种工作分别取对应属性 |
| `WorkRules.Diminishing` | n=1→1.0；n=2→≈0.83；n=10→≈0.36 |
| `WorkRules.CalcGrowth` | 成功/失败系数；消耗为 0 时返回 0 |
| `Quota` 计算 | 普通日与考验日（×1.3）对比 |

---

## 变更记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-09-07 | v1.0 | 初版：8 个模块 FDD（日循环/工作/异想体/员工/考验/存档/剧情/编成节点），含状态机、关键逻辑、验收点；数值配置汇总与 7 项待确认清单。**不含美术风格**（项目美术不基于脑叶） |
| 2026-09-12 | v2.7 | **纠正误判**：明确"异想体绑定 = 可选彩蛋、不要求一一对应、不阻塞流程"，并在 §8.5 增加"**异想体池未补全不影响生命树与日循环运行，不得作为任何里程碑前置条件**"（GDD §3.2）。依据升 GDD v1.10 |
| 2026-09-12 | v2.6 | **全文档质检修复**：①**修 FDD-01 §1.2 状态机**——补上 `TREE_OVERVIEW(L1 生命树)` 节点，入口由「编成」改为「剧情 → 生命树 → 加载 → 编成」，两条回滚路径改回当日 L1 生命树（原文仍写「回到当日编成」，与 GDD/ARCHITECTURE 冲突）；②**修章节编号重号**：`§9 数值配置汇总` → `附录 A`、`§10 待确认清单` → `附录 B`（含 `10.1` → `B.1`）、`§12 测试场景清单` → `附录 C`，消除与 FDD-09/10/12 的撞号及「两个 10.1」；③§8.4 标题去掉「草案，待策划确认」（已定稿）；④§8.2 补 F15 交互规范引用；⑤数值来源引用升为 GDD v1.9 |
| 2026-09-12 | v2.5 | **全面对齐脑叶（GDD v1.8 同步）**：①中层部门顺序与天数改为 **中央本部 Day20 → 惩戒部 Day25 → 福利部 Day30**（原 福利21/中央本部26/惩戒31），`Bands.json` 的 `nodeIndex`/`unlockDay` 随之重排（RAS 7→6、Morfonica 6→7）；②**队长更正**：Afterglow 队长 = **美竹兰**（原误写为贝斯手上原绯玛丽）、MyGO!!!!! 队长 = **高松灯**（原留空）；③节点 2/3 对调（情报部 = Pastel*Palettes 丸山彩、安保部 = Afterglow 美竹兰）；④§8.6 验收点改为按 10 个 `unlockDay` 升序校验。⚠️ 副作用已记录：三个中层部门都在 5 的倍数天开放，与记忆日重合 |
| 2026-09-12 | v2.4 | **部门部长改为乐队队长（GDD v1.7 同步）**：①FDD-08 §8.4 `Bands.json` 补全 **10 个节点**（`sephirot` 字段删除，改为 `leader` + `leaderId`），10 支乐队按剧情/情感深度递进定序：Poppin'Party→Afterglow→Pastel*Palettes→Hello Happy World→Roselia→Morfonica→RAISE A SUILEN→MyGO!!!!!→Ave Mujica→梦限大MewType；②新增 §8.4.1 队长机制（队长=可派遣的乐队少女之一，不新增 NPC 实体，仅 `isLeader` 标记；可工作、崩溃退场不死亡；队长身份只给部门加成+专属台词+节点显示名）；③§8.6 补 3 条验收点（节点显示队长名、MyGO 队长为空时 UI 容错、队长可派遣）。⚠️ 待确认：乐队 id 缩写建议值、MyGO!!!!! 队长（原著未设） |
| 2026-09-12 | v2.3 | **流程顺序修订（用户定稿）**：生命树（L1）移到编成（L2）**之前**，且**每天都经过**——每日剧情 → L1 生命树（开放部门 + 指定今日异想体收容进哪个部门，每部门上限 4，5 的倍数天不抽新异想体）→ 加载 → L2 编成（部署员工）→ 「开始这一天」→ L3。FDD-08 重写为 8 小节（职责表/编成/节点状态机/`Bands.json` 草案/边界与待确认/验收点）；GDD §3.14 上层顺序更正为 控制部→情报部→安保部→培训部（对齐脑叶） |
| 2026-09-10 | v2.2 | 新增 FDD-12 相机控制系统（参考脑叶）：正交相机、平移（WASD/中键拖动）、滚轮缩放（0.5x~2.0x 钳制）、边界限制、平滑插值、部门场景尺寸草案（40x12 单位，默认视野约 1/2）、7 条验收点
| 2026-09-10 | v2.1 | FDD-01 状态机修正：删除残留的「未达标强制结束（forced=true）」分支（与 GDD §3.11「未达标无法结束当天」冲突）；补「重新开始这一天」与「回到记忆日」两条回滚路径的区别（内存态 vs 快照）
| 2026-09-08 | v2.0 | **全文档自检修复**：①装备验收点残留旧规则「属性下降后次日强制卸下」→改为「仅装备前检查、装备后不卸下」；②JSON 示例的 linkedCharacterId 旧 id `anon_chihaya` → `band_mygo_anon`
| 2026-09-08 | v1.9 | **修复命名 bug**：`pointsPerSuccess` 原被误标为"情感粒子产出"，与其中文字面义（异想体点数）冲突。现拆分并正名：`energyPerSuccess`=情感粒子产出、`pointsPerSuccess`=异想体点数产出（回归本义）、`accumulatedPoints`=运行时累积点数（原 `points`）。同步 ARCHITECTURE（Aberration 字段加 ★/☆ 注释）、Aberrations.md（字段速查+JSON）、GDD §3.2（两种产出的中文说明）
| 2026-09-08 | v1.8 | 属性显示与训练机制变更：**四维只显示等级（I~EX）不显示数值**（内部仍用 1-120 计算，UI 层转换，悬停可看区间）；训练改为**按"级"提升**——每次升一级且属性值=新等级下限（21/41/61/81/101），EX 后停训；费用按目标等级递增（10/30/100/300/800，参考脑叶）
| 2026-09-08 | v1.7 | 装备来源规则明确：**所有装备均由异想体研发而来**，每个异想体产出一套（武器+护甲+饰品）；研发=解锁型号可多人装备；绑定少女自动获得同一套（非第二套，仅获得方式不同）；饰品靠掉落不研发
| 2026-09-08 | v1.6 | **死亡规则定稿（方案 B）**：普通员工血量/精神归零=**死亡**（永久失去、装备掉落需重新研发、可招录补充）；乐队少女**不死亡**（崩溃退场3天、装备与成长保留）。同步 FDD-04（新增 4.4 崩溃与死亡流程、验收点）与 FDD-11.6（装备丢失定稿，去掉待确认）
| 2026-09-08 | v1.5 | FDD-11 装备规则修订：①乐队少女专属装备**随该少女加入而出现并绑定**（不可转让、无需研发）；②装备要求**仅装备前检查**（装备后不因属性下降卸下，与脑叶不同）；③新增"员工死亡→装备掉落需重新研发"（死亡机制待策划确认，暂按"崩溃不掉落"实现）
| 2026-09-08 | v1.4 | 新增 FDD-11 装备系统（E.G.O 式，完整参考脑叶）：武器/护甲/饰品三类、四色伤害与抗性、6 槽位饰品、装备要求与强制卸下、Equipments.json 结构、纯函数与验收点；原测试清单顺延为 §12
| 2026-09-08 | v1.3 | 新增 FDD-10 异想体数据规格：workModifiers 明确为**乘数**（基准1.0，禁负值）；新增 specialEffects 字段（工作结果为优/满产出触发效果）；收录「未奏响的乐章」完整 JSON 规格（aber_score_01，ZAYIN/回忆型/对应千早爱音/产出情感粒子）与验收点
| 2026-09-07 | v1.2 | 明确与 GDD 的分工（GDD=规则权威，FDD=实现规格，去除重复定义）；FDD-02 判定规则改为引用 GDD；新增 §11 测试场景清单（6 个场景 + 5 组单测）；存档目录名统一 ResonanceShelter（同步 GDD/ARCHITECTURE/agent）
| 2026-09-07 | v1.1 | 落实 7 项待确认决策（用户拍板）：①未达标**无法结束当天**须达标或重开（GDD §3.11 同步修订）；②考验日配额 **×1.3**；③递减曲线 `1/(1+0.2(n-1))`；④成长系数 成功 `×0.10` / 失败 `×0.03`；⑤乐队少女训练**无约束**；⑥寻路采用**路点 Waypoint**（新增 FDD-09，含决策理由与实现方式）；⑦存档目录名 **ResonanceShelter**。§9 标记已定稿项，§10 保留 4 项待实测调参 |
